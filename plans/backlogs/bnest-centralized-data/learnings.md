# Learnings

Record delivery-time observations here only when they need a durable destination. Before this plan moves to `done/`, route each entry to the relevant specification, governance rule, README, or follow-up plan and replace it with the destination link.

## Capture Approach

Capture a safe, concise learning after the storage contract, authentication boundary, each import/restore rehearsal, and any failed recovery attempt. Focus on migration safety, browser-storage compatibility, multi-role authorization, and rollback usability. Record the observation, its safe evidence (test name, synthetic fixture, or redacted result), and the decision it changes; never record credentials, user data, private identifiers, or runtime values.

Before creating a follow-up idea, search every `plans/ideas/` quadrant and its linked briefs. Merge an overlapping learning into the existing brief; create a new two-pager only when its problem and proposed direction are distinct. Link the selected idea—or the permanent specification, governance document, or discard rationale—back from the entry.

## Entries

No execution observations yet.
