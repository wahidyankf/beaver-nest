# Delivery

All work below is pending. Each checkbox is independently verifiable, and a failed phase checkpoint blocks every later phase.

Every phase applies [live-service continuity](../../../repo-governance/development/live-service-continuity.md): Bnest stays routed throughout, the sole backend is never stopped, Tailscale is never repointed for a normal release, only one release slot may write at a time, and no compatible client is required to refresh. A failed active-route health check stops all other work until the route is restored.

Commands run through the workspace resource guard. `GUARD` below stands for `apps/resource-guard/resource-guard run --class ephemeral -- npm exec --`. Exit 75 is transient capacity: wait and retry the same guarded command serially. Exit 73 requires storage cleanup first. Exit 78 requires replanning, never a bypass.

## Phase 1 — Baseline, contracts, and dependency approval

- [ ] `[AI] [AC-12]` Input: the active Bnest route, Caddy state, current revision, and this plan. Action: run the plan quality gate, record value-free local and routed health, revision, and at least 12 exact-origin responsiveness samples, and stop if the baseline is unhealthy. Outcome: work starts from a known-healthy routed baseline with a numeric budget of zero failures, p95 at or below 500 ms, and every sample at or below 2 seconds. Proof: safe checkpoint note, `proxy:status`, ready endpoint, and the sample summary in `learnings.md`.
- [ ] `[AI] [AC-08]` Input: current primary sources for `absinthe` and `absinthe_plug`. Action: verify release recency, supported Elixir and OTP range against this repository's toolchain, licence, and open advisories; pin both in `mix.exs` and `mix.lock`; record the decision, the rejected built-in alternatives, and ownership impact in `learnings.md`. Outcome: one approved GraphQL stack, or a recorded switch to the Phoenix JSON fallback named in [the GraphQL document](tech-docs/03-graphql-api.md#dependency-decision). Proof: dependency tree, `mix deps.unlock --check-unused`, and a clean `mix compile --warnings-as-errors`.
- [ ] `[AI] [AC-01] [AC-02] [AC-03] [AC-04] [AC-05] [AC-06] [AC-08] [AC-09]` Input: [specification changes](tech-docs/06-specification-changes.md). Action: write `specs/apps/bnest/app/behaviours/learning.feature` and update the behaviours directory map before any production code. Outcome: the canonical corpus exists and describes behaviour that does not yet work. Proof: `GUARD nx run -p bnest-app -t test:coverage:behaviour` reports the new feature with undefined bindings.
- [ ] `[AI] [AC-11]` Input: the current `sifat-allah-progress` records. Action: inventory every stored record's structural shape, version, and owner count without copying a value into Git, evidence, or a log. Outcome: the migration source set is known and safe. Proof: structural counts and version categories only, recorded in `learnings.md`.
- [ ] **Blocking checkpoint — Phase 1:** `[AI] [AC-08] [AC-11] [AC-12]` Confirm the routed baseline is healthy and measured, the dependency decision is recorded with primary-source evidence, the corpus exists and is red, and the migration inventory contains no private value.

## Phase 2 — Event log, projections, and content

- [ ] `[AI] [AC-07]` **RED** — Input: [the data model](tech-docs/02-data-model.md). Action: write `apps/bnest-app/test/integration/bnest_app/event_log/event_log_test.exs` and `apps/bnest-app/test/integration/bnest_app/learning/schema_test.exs` asserting every table and index, a second idempotent apply, refusal of an `UPDATE` or `DELETE` on a stored event, rejection of a duplicate `(domain, stream_id, stream_version)`, rejection of a malformed `event_name`, and rejection of an event name not registered for its domain. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the migration module does not exist.
- [ ] `[AI] [AC-07]` **GREEN** — Action: add `20260901000000_create_event_log.exs` for the shared log and cursors and `20260901000100_create_learning_projections.exs` for the learning projections, with the exact DDL, the two append-only triggers, and a guarded `down/0` on the log migration that raises when any row exists; implement `BnestApp.EventLog` with append-at-expected-version, domain-scoped reads, and a per-domain name registry. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: the log refuses edits and rejects a stale expected version.
- [ ] `[AI] [AC-07]` **REFACTOR** — Action: keep `BnestApp.EventLog` free of any learning concept, and move stream naming and expected-version handling into one module so no caller builds a `stream_id` by hand. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged results.
- [ ] `[AI] [AC-07]` **RED** — Input: the catalogue in [the event model](tech-docs/01-event-model.md). Action: write `apps/bnest-app/test/unit/bnest_app/event_log/catalog_spec_test.exs`, which parses the per-domain tables in `specs/apps/bnest/app/event-catalog.md` and asserts the specified set of `{domain, event, version}` equals `BnestApp.EventLog.Registry.all/0` in both directions, and that no payload example in the file contains a value outside the synthetic fixture set. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected failure: neither the specification file nor the registry exists.
- [ ] `[AI] [AC-07]` **GREEN** — Action: write `specs/apps/bnest/app/event-catalog.md` with the `learning` section, add its entry to `specs/apps/bnest/app/README.md`, and register every catalogued name in `BnestApp.EventLog.Registry` so the append path rejects an unregistered name. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: the file and the registry agree, and an unregistered name is refused at append.
- [ ] `[AI] [AC-07]` **REFACTOR** — Action: keep one definition per event in the registry and let the test derive from it, so adding an event is one registry entry and one specification row and nothing else. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: unchanged results, and deleting a specification row still fails the suite.
- [ ] `[AI] [AC-07] [AC-07b]` **RED** — Action: write `apps/bnest-app/test/unit/bnest_app/learning/projector_test.exs` asserting that projecting a fixed event list yields fixed rows, that timestamps come from `occurred_at`, and that no projector reads the system clock or any value outside the event. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected failure: the projector does not exist.
- [ ] `[AI] [AC-07] [AC-07b]` **GREEN** — Action: implement `Learning.Projector` for every projection with checkpoint advancement inside the same transaction as the append. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: deterministic projection with no clock read.
- [ ] `[AI] [AC-07b]` **REFACTOR** — Action: express each projection as an independent handler keyed by event name so adding a read model does not touch existing ones. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: unchanged results.
- [ ] `[AI] [AC-07b]` **RED** — Action: write `rebuild_test.exs` that runs a representative workload, checksums every projection table, drops the projections, replays the log, and compares. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the rebuild path does not exist.
- [ ] `[AI] [AC-07b]` **GREEN** — Action: implement `Learning.Rebuild` and `mix bnest.learning.rebuild_projections`, truncating projections, resetting checkpoints, and replaying from `event_id` 1 in one transaction. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: byte-identical projections before and after rebuild.
- [ ] `[AI] [AC-07]` **REFACTOR** — Action: add `upcaster_test.exs` and `Learning.Upcaster` with a version-1 identity chain, so the versioning path exists and is exercised before any second version is needed. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: events read through the chain unchanged.
- [ ] `[AI] [AC-03]` **RED** — Action: write `mission_kind_test.exs` and `corpus_test.exs` covering all six kinds, every pass-rule pairing, slug rules, canonical serialization, and whole-corpus rejection. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected failure: the corpus and kind modules do not exist.
- [ ] `[AI] [AC-03]` **GREEN** — Action: implement `Learning.MissionKind` and `Learning.Corpus`. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: kind and corpus suites green.
- [ ] `[AI] [AC-03]` **REFACTOR** — Action: collapse the six kind validators onto one declarative contract table, keeping the same rejections. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: unchanged results with one contract definition.
- [ ] `[AI] [AC-01]` **RED** — Action: write `content_test.exs` asserting that a first sync appends one defining event per item, that an unchanged corpus appends nothing, that an invalid corpus appends nothing and leaves the previous projection serving, and that removal appends a retirement event while progress stays readable. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: `Learning.Content` does not exist.
- [ ] `[AI] [AC-01]` **GREEN** — Action: implement the sync as a command handler that diffs the corpus against projected content state and appends only real differences, plus the `mix bnest.learning.sync` task. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: a second run appends nothing and changes no row.
- [ ] `[AI] [AC-01]` **REFACTOR** — Action: separate corpus reading, diffing, and event building so each is testable alone. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged results.
- [ ] **Blocking checkpoint — Phase 2:** `[AI] [AC-01] [AC-03] [AC-07] [AC-07b]` Confirm both migrations are additive, the shared log carries no learning concept, every appendable event name appears in `specs/apps/bnest/app/event-catalog.md` and nowhere else defines the vocabulary, the log refuses edits and deletes, a stale expected version is rejected, projections rebuild byte-identically, an unchanged corpus appends nothing, an invalid corpus appends nothing, and the previous revision still runs against the migrated database.

## Phase 3 — Commands, coins, verification, and the API

- [ ] `[AI] [AC-02] [AC-03] [AC-04]` **RED** — Action: write `progress_test.exs` for each pass rule, streak reset, the review ladder, a mission mastered once serving two courses, and a concurrent second command on the same learner stream. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the command handler does not exist.
- [ ] `[AI] [AC-02] [AC-03] [AC-04] [AC-07]` **GREEN** — Action: implement `Learning.Command` — load state, decide, append at the expected stream version, project, advance the checkpoint — and the attempt, mastery, and review decisions on top of it. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: progress suite green, and a concurrent append is rejected and retried rather than overwriting.
- [ ] `[AI] [AC-02] [AC-04]` **REFACTOR** — Action: extract the interval ladder into `Learning.Review` with a table-driven unit test, keeping `next_review_at` computed by the handler and carried in the event. Command: `GUARD nx run -p bnest-app -t test:unit`. Expected pass: unchanged scheduling results.
- [ ] `[AI] [AC-06]` **RED** — Action: write `coins_test.exs` asserting one `coins.earned` event at first mastery, none on repeat, none for a zero-reward mission, none under a concurrent double submission, and a ledger that rebuilds to the same rows. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the coin decision does not exist.
- [ ] `[AI] [AC-06]` **GREEN** — Action: implement `Learning.Coins` so the handler appends the earning event only on first mastery, inside the same append as the mastery event, with the ledger row derived as `evt:<event_id>`. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: balances exact under repeat, concurrency, and rebuild.
- [ ] `[AI] [AC-06]` **REFACTOR** — Action: assert in the same suite that no writer can produce a `spent` entry and that no projection row exists without an event behind it. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged results plus the new invariants.
- [ ] `[AI] [AC-05]` **RED** — Action: write `verification_test.exs` and `authorization_test.exs` for submission, approval, rejection, self-verification refusal, the actor recorded on the child's stream, and the full role matrix. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: verification and authorization modules do not exist.
- [ ] `[AI] [AC-05]` **GREEN** — Action: implement submission, parent decision, and the access rules, enforced by the command handler and not only at the edge. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: a child cannot self-verify and cannot read another learner.
- [ ] `[AI] [AC-05]` **REFACTOR** — Action: unify the access rules behind one predicate used by every command and query. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged denials.
- [ ] `[AI] [AC-07]` **RED** — Action: write `dispatcher_test.exs` asserting that a live subscriber may miss messages harmlessly, that a durable subscriber resumes on the event it had not finished, and that handling the same event twice changes no stored value. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the dispatcher does not exist.
- [ ] `[AI] [AC-07]` **GREEN** — Action: implement `BnestApp.EventLog.Dispatcher` as a supervised post-commit publisher with per-listener cursors and PubSub topics scoped per domain, per learner, and for content. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: resume and idempotence proven, no cross-learner topic leakage.
- [ ] `[AI] [AC-07]` **REFACTOR** — Action: move topic naming into one module so no caller builds a PubSub topic string by hand. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged results.
- [ ] `[AI] [AC-08]` **RED** — Action: write `learning_query_test.exs`, `learning_mutation_test.exs`, `authorization_test.exs`, and `limits_test.exs` under `test/integration/bnest_app_web/graph/`, covering every field, the denial matrix, depth and complexity limits, rejected batching, the absence of content and raw-event mutations, and a mutation's effect being visible to a query in the same request cycle. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the schema does not exist.
- [ ] `[AI] [AC-08]` **GREEN** — Action: implement the Absinthe schema, types, resolvers, authorization middleware, and session context plug; serve reads from projections only; mount `/api/graphql` and gate GraphiQL behind `dev_routes`. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: every query and mutation behaves as specified and every denial is value-free.
- [ ] `[AI] [AC-08]` **REFACTOR** — Action: route all resolver access through the context so no resolver queries the repository or the log directly. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged results with no `Repo` call in the resolver module.
- [ ] `[AI] [AC-01] [AC-02] [AC-03]` **RED** — Action: bind the new corpus in `test/behaviour/steps/learning_steps.exs` for every scenario the Elixir adapters can drive, exempting browser-only scenarios explicitly. Command: `GUARD nx run -p bnest-app -t test:coverage:behaviour`. Expected failure: bindings present, behaviour incomplete.
- [ ] `[AI] [AC-01] [AC-02] [AC-03] [AC-06]` **GREEN** — Action: complete the context behaviour so the bound scenarios pass. Command: `GUARD nx run -p bnest-app -t test:coverage:behaviour`. Expected pass: no undefined, ambiguous, or unused binding.
- [ ] `[AI] [AC-07b] [AC-08]` **REFACTOR** — Action: remove any test-only accessor that duplicates a GraphQL field, and re-run the rebuild test after the full command surface exists so replay covers every event name. Command: `GUARD nx run -p bnest-app -t test:quick`. Expected pass: quick gate green and rebuild still byte-identical.
- [ ] **Blocking checkpoint — Phase 3:** `[AI] [AC-02] [AC-04] [AC-05] [AC-06] [AC-07] [AC-07b] [AC-08]` Confirm every write goes through a command handler, mastery is global to the learner and mission, coins are exactly once under repeat and concurrency, a child reaches only their own data through every path, durable subscribers resume, rebuild remains byte-identical across all event names, and the schema exposes no content or raw-event mutation.

## Phase 4 — Generic runner and parity

- [ ] `[AI] [AC-09]` **RED** — Action: write `test/integration/bnest_app_web/mission_live_test.exs` and `verify_live_test.exs` covering all eight runner states, the parent queue, denial for a child, keyboard order, live regions, and locked choices with automatic advance. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the LiveViews do not exist.
- [ ] `[AI] [AC-09]` **GREEN** — Action: implement `LearnLive`, `LearnCourseLive`, `MissionLive`, `ReviewLive`, `VerifyLive`, the shared learning components, and the routes, following the selected trail direction in [the UI design](tech-docs/04-ui-design.md). Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: every state renders with its documented copy and semantics.
- [ ] `[AI] [AC-09]` **REFACTOR** — Action: extract the six kind renderers behind one `MissionCard` contract so adding a seventh kind touches one module. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged rendering.
- [ ] `[AI] [AC-09]` Input: all twelve reviewed assets. Action: implement responsive trail behaviour with a container query, 44-pixel targets, visible focus, polite live regions, alert focus, reduced-motion collapse, and no colour-only status. Outcome: the selected design works on desktop, tablet, and mobile. Proof: focused accessibility assertions plus a manual pass at the exact origin across the route and state matrix at 200% zoom, keyboard-only, dark and light, recorded as route, state, viewport, and pass or fail.
- [ ] `[AI] [AC-10]` **RED** — Action: change only the route in `specs/apps/bnest/app/behaviours/sifat_allah.feature`, rebind `test/behaviour/steps/sifat_allah_steps.exs` and `apps/bnest-app-e2e/tests/steps/sifat_allah.steps.ts` to the generic runner, and run the corpus. Command: `GUARD nx run -p bnest-app-e2e -t test:e2e -- --grep "@sifat"`. Expected failure: named scenarios fail against the generic runner; record exactly which.
- [ ] `[AI] [AC-10]` **GREEN** — Action: implement the missing runner capability the corpus demands — trail markers, automatic advance after a locked answer, browser Back returning to the mission dashboard, immediate queue movement, varied answer positions, and the exam skipping learned questions. Command: `GUARD nx run -p bnest-app-e2e -t test:e2e -- --grep "@sifat"`. Expected pass: every scenario passes with none deleted or weakened.
- [ ] `[AI] [AC-10]` **REFACTOR** — Action: fold any Sifat-Allah-specific branch into a general runner capability driven by content, leaving no course-specific code path. Command: `GUARD nx run -p bnest-app-e2e -t test:e2e -- --grep "@sifat"`. Expected pass: unchanged results with no course identifier in the runner.
- [ ] `[AI] [AC-09]` **RED** — Action: add `apps/bnest-app-e2e/tests/steps/learning.steps.ts`, `learning_api.steps.ts`, and `tests/support/learning.ts`, and register the browserless `api` Playwright project in `playwright.config.mts` and `project.json`. Command: `GUARD nx run -p bnest-app-e2e -t test:coverage:behaviour`. Expected failure: bindings exist, journeys incomplete.
- [ ] `[AI] [AC-08] [AC-09]` **GREEN** — Action: complete the browser journeys, using the API for assertion and setup but never for the action under test. Command: `GUARD nx run -p bnest-app-e2e -t test:e2e` and `GUARD nx run -p bnest-app-e2e -t test:e2e -- --project api`. Expected pass: learner, parent, and endpoint scenarios green at the exact origin with isolated `test-user-` identities.
- [ ] `[AI] [AC-09]` **REFACTOR** — Action: move repeated setup into `tests/support/learning.ts` and confirm every task-created tab and context is closed. Command: `GUARD nx run -p bnest-app-e2e -t test:e2e`. Expected pass: unchanged results, no leaked context.
- [ ] `[AI] [AC-09]` Input: the as-built engine. Action: update `specs/apps/bnest/app/architecture.md` with the learning component, its stores, the GraphQL entry point, the event-bus relationship, and the two new constraints; do not remove the Sifat Allah elements yet, because they still exist. Outcome: the C4 model matches the system as built at this phase. Proof: `GUARD nx run -p badakmini-cli -t test:repo`.
- [ ] **Blocking checkpoint — Phase 4:** `[AI] [AC-09] [AC-10]` Confirm the unchanged Sifat Allah corpus passes against the generic runner, every runner state is proven on three viewports, the architecture model matches the built system, and no course-specific branch remains in the runner.

## Phase 5 — Migration and contraction

- [ ] `[AI] [AC-11] [AC-07b]` **RED** — Action: write `sifat_allah_migration_test.exs` covering version 1 and version 2 sources, set equality per learner, idempotent re-run, a malformed or changed source blocking the cutover without mutation, and a rebuild after migration reproducing byte-identical projections. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected failure: the migration module does not exist.
- [ ] `[AI] [AC-11] [AC-07b]` **GREEN** — Action: implement `BnestApp.Learning.SifatAllahMigration` and `mix bnest.learning.migrate_sifat_allah`, reusing `SifatAllah.restore/1` for version handling and appending `progress.imported` events per learner rather than writing projection rows. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: per-learner set equality, a second run that appends nothing, a blocked source that mutates nothing, and a post-migration rebuild that still reproduces byte-identical projections.
- [ ] `[AI] [AC-11]` **REFACTOR** — Action: separate inventory, conversion, and outcome recording so a retry resumes by identity. Command: `GUARD nx run -p bnest-app -t test:integration`. Expected pass: unchanged results.
- [ ] `[AI] [AC-11]` Input: a verified migrated fixture. Action: start a fresh process in an isolated fixture with the `sifat-allah-progress` source removed and drive the full product journey — open the course, answer, master, earn a coin, review — with a synthetic identity. Outcome: the engine is authoritative and fails closed rather than falling back. Proof: fresh-process journey evidence with categories only; row counts alone are explicitly insufficient.
- [ ] `[AI] [AC-11]` **RED** — Action: assert in `learning_steps.exs` that the legacy route redirects to the engine route and that the legacy modules are absent. Command: `GUARD nx run -p bnest-app -t test:coverage:behaviour`. Expected failure: the legacy modules still exist.
- [ ] `[AI] [AC-11]` **GREEN** — Action: redirect `/apps/sifat-allah`, delete `BnestApp.SifatAllah`, `BnestAppWeb.SifatAllahLive`, `sifat_history.js`, `sifat_celebration.js`, and their two obsolete test files, and keep the `sifat_allah` entry in `BnestApp.Storage.RecordMap` so records stay readable. Command: `GUARD nx run -p bnest-app -t test:quick`. Expected pass: quick gate green with the legacy screen gone and the retained reader intact.
- [ ] `[AI] [AC-11]` **REFACTOR** — Action: remove now-dead helpers surfaced by the deletion and confirm coverage thresholds still hold. Command: `GUARD nx run -p bnest-app -t test:quick`. Expected pass: unchanged results.
- [ ] `[AI] [AC-11]` Input: the as-built system. Action: remove the Sifat Allah component and its LiveView from `specs/apps/bnest/app/architecture.md`, record the retained readable source and its retention window, and update behaviour traceability. Outcome: the model matches the contracted system. Proof: `GUARD nx run -p badakmini-cli -t test:repo`.
- [ ] **Blocking checkpoint — Phase 5:** `[AI] [AC-11]` Confirm every learner's mastered and difficult sets match their pre-migration values, the fresh-process journey passed with the previous source unavailable, source records are byte-identical, and the retained reader still resolves them.

## Phase 6 — Continuity-safe production transition

- [ ] `[AI] [AC-12]` Input: clean authorized `main`, a healthy route, and the verified build. Action: prepare an independent inactive-slot candidate while the current revision keeps serving; prove candidate revision, readiness, schema compatibility, and a connected LiveView journey directly against the candidate. Outcome: the candidate is safe to promote. Proof: direct health headers, revision, and an exact-origin connected journey.
- [ ] `[AI] [AC-01] [AC-12]` Input: the promoted-ready candidate. Action: run `mix bnest.learning.sync` against production so content exists before any learner can reach the route, and confirm the outcome is `applied` followed by `unchanged` on an immediate re-run. Outcome: the read model is populated and stable. Proof: sync outcome and structural counts only.
- [ ] `[AI] [AC-11] [AC-12]` Input: the healthy dual-capable route. Action: take the migration lock, run the progress migration, verify per-learner set equality and the fresh-process journey, and leave sources untouched. Outcome: production progress is on the engine with the previous reader retained. Proof: migration identifiers and generic outcome categories only.
- [ ] `[AI] [AC-12]` Input: the verified candidate and migrated data. Action: promote through Caddy, prove local and routed revision at the exact origin, exercise LiveView and WebSocket reconnect without a refresh while a learner is mid-mission, sample responsiveness continuously, monitor the five-minute drain, retire the previous slot, and remove every temporary resource. Outcome: only the intended revision serves and only the active route remains. Proof: proxy state, routed revision, reconnect evidence with the mission preserved, the sampling summary, and confirmation that no inactive listener, candidate, or temporary worktree survives.
- [ ] `[AI] [AC-09] [AC-12]` Input: the routed engine. Action: run the [exploratory pass](../../../repo-governance/workflows/exploratory-and-usability-testing.md) with Playwright MCP at the exact origin across all five routes and three viewport classes, using an isolated identity, and record findings under `## Exploratory findings` in `learnings.md`. Outcome: spec-aware probing of edges, routes, and passive security signals is complete. Proof: labelled findings with route, state, and category, or an explicit none-found record.
- [ ] `[AI] [AC-09]` Input: the routed engine. Action: delegate the [usability pass](../../../repo-governance/workflows/exploratory-and-usability-testing.md) to a fresh agent context given only the origin, routes, and viewport classes, withholding specs, source, and assets; record findings under `## Usability findings`. Outcome: a structurally spec-blind first-time-user judgement exists. Proof: separately labelled findings, cross-referenced where they share a root cause with an exploratory finding.
- [ ] `[AI] [AC-09]` Input: accepted findings from both passes. Action: reconcile every proposed specification gap through the BDD Iron Rule — update the Gherkin, bind failing steps, confirm red, then implement — as its own tracked work. Outcome: no accepted finding is merged into `specs/` unreconciled. Proof: the red evidence and final green for each reconciled scenario.
- [ ] `[AI] [AC-12]` **Recovery, only if triggered:** Input: a routed failure, a single sample above 2 seconds, or p95 above 500 ms across a rolling minute. Action: run managed Caddy rollback to the last healthy compatible backend, verify route and revision, retire the failed candidate, and record a safe diagnosis before retrying. Outcome: the household service is restored. Proof: rollback status and a healthy routed journey. If not triggered, reconcile as `Not triggered` with dated evidence.
- [ ] `[AI] [AC-11]` **Recovery, only if triggered:** Input: a migration or verification failure. Action: keep the previous revision authoritative, preserve every source and its evidence, release the migration lock, verify the current route, and retry only after diagnosis; never delete or overwrite a source. Outcome: no unsafe cutover and no data loss. Proof: authority state, generic failure category, and a healthy route. If not triggered, reconcile as `Not triggered` with dated evidence.
- [ ] **Blocking checkpoint — Phase 6:** `[AI] [AC-01] [AC-09] [AC-11] [AC-12]` Confirm the routed intended revision, populated content, migrated progress, reconnect without refresh, the responsiveness budget met from preflight through drain, both testing passes recorded, and only the active route remaining.

## Phase 7 — Documentation and reconciliation

- [ ] `[AI] [AC-01] [AC-03]` Input: the as-built engine. Action: write `apps/bnest-app/priv/learning/README.md` as the authoring guide with its directory map, and update `apps/bnest-app/README.md`, `apps/bnest-app-e2e/README.md`, and the root `README.md` with the learning targets, the corpus layout, the API, and the `api` end-to-end project. Outcome: a maintainer can add a subject and run every gate without reading the plan. Proof: `GUARD nx run -p badakmini-cli -t test:repo` and the Markdown link gate.
- [ ] `[AI] [AC-07]` Input: every event the engine appends as built, including any added after Phase 2. Action: reconcile `specs/apps/bnest/app/event-catalog.md` with the final registry, filling the stream, appending component, payload, and consumer columns for each event. Outcome: the catalogue describes the system as built, not as planned. Proof: `GUARD nx run -p bnest-app -t test:unit` passes with no drift, and the reviewed diff shows no real learner value in any example.
- [ ] `[AI] [AC-07] [AC-07b]` Input: the plan's own glossary in [the technical entry point](tech-docs/README.md) and the as-built engine. Action: move the event-sourcing vocabulary that the repository now actually uses — event log, stream, stream version, projection, projector, replay, cursor, live and durable subscriber, at-least-once, upcaster, compensating event — into `docs/reference/glossary.md` under a new section, keep each entry linked to its canonical owner rather than restating it, and leave the plan's glossary as the plan-scoped copy it is. Outcome: the durable vocabulary lives in repository reference documentation once the thing it names exists. Proof: `GUARD nx run -p badakmini-cli -t test:repo` and the Markdown link gate.
- [ ] `[AI] [AC-01] [AC-02] [AC-03] [AC-04] [AC-05] [AC-06] [AC-07] [AC-07b] [AC-08] [AC-09] [AC-10] [AC-11] [AC-12]` Input: all delivery evidence. Action: rerun the plan quality gate, reconcile every acceptance criterion, the File Impact list, specifications, directory maps, learnings, and recovery dispositions, and confirm no unneeded server, watcher, candidate, or temporary proxy is still running. Outcome: the plan matches repository and runtime state exactly. Proof: clean recursive inventory, green gates, and a single active route.
- [ ] `[AI]` Input: accepted completion and the final local date. Action: move — never copy — this folder to `plans/done/YYYY-MM-DD__family-learning-engine/`, update both stage indexes and the status header together, and verify one destination with no references to the old path. Outcome: the delivery record is archived. Proof: destination and source checks, the repository diff, and the final map gate.
- [ ] **Final checkpoint:** `[AI]` Confirm acceptance, data preservation, routed service, cleanup, documentation, both testing passes, learnings, and archive verification before reporting completion.
