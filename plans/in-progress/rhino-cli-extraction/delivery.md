# Delivery

Every command runs through the pinned HIPPO guard. `HIPPO` below abbreviates `./hippo run --class ephemeral --disk-path . --`; write it in full when executing. Upstream work happens in the local RHINO checkout, referred to below as `<rhino-checkout>`. Sibling work happens in the local HIPPO and grind-in-public checkouts. Commit and push authorization does not cross a repository boundary: each repository needs its own. Commit and push in either repository require separate [authorization](../../../repo-governance/conventions/commit-authorization.md).

## Phase 1 — Baseline, Evidence, and Frozen Contracts

- [x] `[AI] [AC-09]` Input: clean `main` and the routed BeaverNest origin. Action: record the revision, routed health, and served revision; confirm no other in-progress plan overlaps. Outcome: a baseline this plan can be judged against. Proof: recorded revision and health in `learnings.md`. _Done 2026-09-07: baseline `debb17116`, clean tree, sole in-progress plan; routed origin 0/12 failures, p95 3 ms, serving `f536f97da`._
- [x] `[AI] [AC-12]` Input: the unmodified BeaverNest and grind-in-public trees and [the benchmark design](tech-docs/05-benchmarks.md). Action: capture the before baseline for both retired validators — warm and cold gate wall-clock, single-validator startup, peak resident memory, toolchain acquisition, and repository footprint — following the stated method: HIPPO-guarded, Nx cache skipped, first two warm runs discarded, median of the repeats with the observed range. Outcome: a baseline that cannot be reconstructed later, because both validators cease to exist at cutover. Outcome check: also record the exact corpus counts the gate reports, since the plan's expected-proof numbers are read from here rather than assumed. Proof: the recorded table in `learnings.md` with date, host state, and the exact commands. _Done 2026-09-07: both tables recorded. BeaverNest — warm gate median 0.392 s, startup 0.277 s, peak RSS 64.4 MiB, cold restore+build 5.035 s, 38 files / 6,793 lines, 674 MB SDK. grind-in-public — warm gate 0.236 s built and 0.373 s as wired through `go run`, startup 0.215 s, peak RSS 12.1 MiB, `go build` 1.461 s to a 4.7 MiB binary, 41 files / 8,878 lines, 258 MB GOROOT plus a 2.3 GB module cache that is not shed. Corpus counts re-read live from both gates; the scripts are kept under the ignored `local-tmp/rhino-bench/` so Phase 11 re-measures by the same harness. A harness caveat is recorded with them: the bracketed timings carry a constant overhead and are comparable to each other, not absolute._
- [x] `[AI] [AC-02]` Input: `apps/badakmini-cli/Governance.fs` and `HarnessContract.fs`. Action: inventory every regular expression with the engine features it uses, flagging lookaround, backreferences, balancing groups, and named-group behaviour that Rust's `regex` crate does not provide. Outcome: a per-pattern disposition of "port as-is" or "rewrite as explicit parsing". Proof: the inventory table in `learnings.md`, with a count of patterns in each disposition. _Done 2026-09-07: 31 distinct patterns — 28 port as-is, 3 rewritten as explicit parsing (one lookbehind, one lookahead, one line split that was never a regex problem)._
- [x] `[AI] [AC-02]` Input: [dependency selection](../../../repo-governance/development/dependency-selection.md) and current primary sources. Action: re-verify the `yaml_serde` evidence recorded on 2026-09-07 — latest release date, repository activity, and the absence of any OSV or RustSec advisory against it or `libyaml-rs` — and settle the remaining open decision, the `regex` disposition, recording need, rejected alternatives, evidence, and ownership impact. Outcome: a defensible dependency set before any code exists. Proof: recorded decisions in `learnings.md`; if `yaml_serde` has since stalled or acquired an advisory, record the switch to `serde_norway` or `serde_yaml_ng` with its evidence. _Done 2026-09-07: `yaml_serde` 0.10.7 re-verified, no advisory, decision unchanged; `regex` 1.13.1 selected over `fancy-regex` and `pcre2`, with the linear-time guarantee as the deciding reason._
- [x] `[AI] [AC-02]` Input: `ose-public`'s `apps/rhino-cli` surface and its `repo-config.yml`. Action: read both in full and record every point where this plan's command spelling, flag names, exit codes, or schema shape agree or diverge, with a reason for each divergence. Outcome: alignment is a recorded decision rather than a coincidence. Proof: the alignment table in `learnings.md`. _Done 2026-09-07: six agreements, eight reasoned divergences; the reading also corrected the plan's framing of the `harness parity` rename from a departure to a convergence._
- [x] `[AI] [AC-01, AC-02]` Input: the constants table in [the configuration contract](tech-docs/02-configuration-contract.md) and the two source files. Action: transcribe the exact capability, constraint, and required-MCP vocabularies from `HarnessContract.fs` into the schema, and confirm no constant in the inventory is missing. Outcome: a complete, frozen schema with no elided values. Proof: an exact search showing every listed constant has a schema home. _Done 2026-09-07: seven capabilities, two constraints, the MCP vector, eleven scan exclusions, and eight palette colours all verified present. Two gaps found and closed — the Mermaid palette was assigned to the binary and is now configuration; `scan.exclude-directories` carried `dist`, which the source never excluded, and omitted five directories it does._
- [x] **Blocking checkpoint — Phase 1:** `[AI] [AC-01, AC-02, AC-09, AC-12]` Confirm both baselines captured with their conditions, routed health unchanged, every regex disposed, both dependency decisions evidenced, alignment recorded, and the schema complete with no placeholder. **Proof:** _Cleared 2026-09-07._ Both baselines are recorded with their host conditions, including the differing HIPPO admission state between the warm and cold BeaverNest series, which is stated rather than averaged away. Routed health re-checked at the end of the phase: twelve samples through the routed HTTPS origin, zero failures, p95 56 ms against a 500 ms budget, still serving `f536f97da` — unchanged in revision and well inside the continuity budget; the phase touched no runtime input. All 31 regexes are disposed with none open. Both dependency decisions carry evidence — `yaml_serde` 0.10.7 re-verified advisory-free, `regex` 1.13.1 chosen for its linear-time guarantee. The `ose-public` alignment table records six agreements and eight reasoned divergences. The schema is complete with no elision, and freezing it found and closed two real gaps. Three plan defects were corrected in this phase rather than carried forward: grind-in-public's fourth `rule-change` leaf, the Mermaid palette wrongly assigned to the binary, and a stale link count.

## Phase 2 — Upstream Repository and Specification Corpus

- [x] `[AI] [AC-01]` Input: the empty `wahidyankf/rhino` repository and [the upstream design](tech-docs/01-rhino-repository.md). Action: scaffold the Rust-idiomatic layout — root package with an `xtask` workspace member, `rust-toolchain.toml`, `rustfmt.toml`, `clippy.toml`, `deny.toml`, `.cargo/config.toml`, `src/lib.rs` with `#![forbid(unsafe_code)]`, `src/main.rs`, the three husky hooks matching HIPPO's and BeaverNest's — `pre-commit` running `lint-staged`, `commit-msg` running `commitlint`, and a HIPPO-guarded `pre-push` running `cargo xtask test-quick` — and a `ci.yml` running format, clippy, `cargo deny`, and tests. Outcome: an empty but fully gated repository. Proof: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo deny check` all pass on the scaffold. _Done 2026-09-07 at `e0a7fe4`, pushed._ All three named proofs pass: `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`). Three departures from the written scaffold, each for minimal sufficiency rather than convenience: the four `[[test]]` targets, the `scheduled.yml` workflow, and the `coverage`, `self-check`, and `dist` xtask subcommands are all deferred to the phases that make them real, because declaring a target whose file does not exist breaks the gate the scaffold is supposed to prove, and shipping a subcommand that returns "not implemented yet" is the placeholder this plan forbids. `xtask test-quick` therefore runs format and clippy today. The HIPPO consumer bootstrap was copied in and its concurrency mapping retargeted from the sibling consumers' `NX_PARALLEL`, `GOMAXPROCS`, and `DOTNET_PROCESSOR_COUNT` to `CARGO_BUILD_JOBS` and `RUST_TEST_THREADS`.
- [x] `[AI] [AC-01]` **Added 2026-09-07 at the maintainer's request, during execution.** Input: the published `wahidyankf/rhino` repository. Action: set its GitHub About — description and topics — so the repository presents itself the way its sibling tools do. Outcome: the repository is discoverable and self-describing before anyone is asked to depend on it. Proof: the description reads "RHINO (Repository Hygiene & INtegration Orchestrator): configuration-driven repository-hygiene validation for documentation, directory maps, internal links, Mermaid accessibility, and coding-harness parity", matching HIPPO's acronym-then-colon form, with topics `accessibility`, `cli`, `developer-tools`, `documentation`, `governance`, `linter`, `linux`, `macos`, `markdown`, and `rust`. _Done 2026-09-07._ Homepage is deliberately left empty, as HIPPO's is, until there is a documentation site to point at.
- [x] `[AI] [AC-01]` Input: this repository's principles for BDD, specification maintenance, TDD, minimal sufficiency, and dependency selection. Action: write upstream `AGENTS.md` restating those principles as self-contained upstream rules in Rust's own vocabulary — never by importing this repository's target names, directory names, or Nx wiring — plus the generic-tool rule, the exit-code and `version --json` compatibility rule, the `cargo xtask dist` release rule, the never-replace-a-tag rule, and the `specs/`-at-root departure. Add `CLAUDE.md` containing only the import. Outcome: the upstream repository is self-governing without reading another repository at all. Proof: `AGENTS.md` is within the 750-word budget and `CLAUDE.md` contains only the import. _Done 2026-09-07 at `3ce4aac`._ `AGENTS.md` is 700 words by the repository's own Unicode-aware counting rule, inside the 750-word budget; `CLAUDE.md` contains the single line `@AGENTS.md` and nothing else. An exact search of `AGENTS.md` finds no BeaverNest target name, directory name, or Nx reference, and all five required rules are present — generic-tool, exit codes with `version --json`, `cargo xtask dist`, never-replace-a-tag, and the `specs/`-at-root departure.
- [x] `[AI] [AC-03]` Input: the seven `.feature` files under `specs/apps/badakmini/cli/behaviours/`. Action: move them to upstream `specs/behaviours/`, reword every scenario that asserts a BeaverNest constant so its policy is established in a `Given` and asserted against, carry the flag renames through every scenario that names them — `--format` becomes `--output`, and the `--version` flag becomes the `version` subcommand with `--json` — drop the TickSpec `{hash}` workaround where the new runner parses `#` correctly, rename `markdown-links.feature` to `internal-link.feature` to match the renamed leaf, and write the tree's directory maps. Outcome: a policy-neutral corpus that specifies a generic tool. Proof: every one of the 81 scenarios is accounted for by name — kept, merged into a named successor, or renamed — with no scenario dropped; each merge and each addition is listed with its reason, and the resulting count is stated rather than assumed to be 81. An exact search finds no literal `750`, `repo-governance`, `.agents`, `.claude`, `.codex`, or `.opencode` asserted as a tool default. _Done 2026-09-07 at `323af99`._ The full disposition ledger is in `learnings.md`: 77 kept or renamed one-to-one, 2 merged into 1, 2 split into 4 — 82 derived from 81 with none dropped — plus 25 additions each listed with its reason, for **107 scenarios across eight files**. The two renamed files are `harness-contract` → `harness-parity` and `markdown-links` → `internal-link`. An exact search of the new corpus finds no literal `750`, `repo-governance`, `.agents`, `.claude`, `.codex`, `.opencode`, or `{hash}`. `specs/README.md` and `specs/behaviours/README.md` carry directory maps naming every sibling.
- [x] `[AI] [AC-03]` Input: the reworded corpus. Action: run the [Gherkin implementation review](../../../repo-governance/workflows/gherkin-implementation-review.md) over every changed scenario. Outcome: the rewrite is semantically reviewed, not just mechanically applied. Proof: the recorded review result per feature file. _Done 2026-09-07 at `7d5f96b` and `c6b4195`._ Per-file results are tabled in `learnings.md`. First pass over 197 expanded rows: **151 PASS, 46 FAIL** — `cli-contract` 30/32, `repo-config` 11/2, `harness-parity` 19/3, `directory-map` 11/2, `internal-link` 2/7, and all three Mermaid and word-budget files clean. Every failure was fixed rather than accepted, the changed scenarios were re-reviewed independently, and the one further failure that pass found was fixed too. Binding registry and step vocabulary were clean on the first pass at every adapter. **The review's largest finding was not in the corpus.** Thirty-one failures came from one defect in the hand-written Gherkin reader, and two more of the same family were found with it: escaped pipes were treated as column separators, `Background:` was discarded, and docstrings were discarded with `#` opening a comment inside them. Twelve of the affected rows were passing while invoking nothing they named. Five parser tests now sit in the quick gate.
- [x] `[AI] [AC-01]` Input: `specs/apps/badakmini/cli/architecture.md`. Action: rewrite it upstream as `specs/architecture.md` with the Rust containers, the library-plus-binary boundary, the configuration input, and the read-only, network-free, process-free constraints, following the [architecture specification standard](../../../repo-governance/development/architecture-specifications.md). Outcome: an as-built C4 model for the new system. Proof: every Mermaid diagram satisfies the label and colour rules. _Done 2026-09-07 at `abdf311`._ Three diagrams — context, containers, components — every node segment at or under 32 graphemes and every edge segment at or under 24, checked by scanning only inside `mermaid` fences so Markdown table rows cannot produce a false positive. Every `classDef` uses a fill from the declared six with `#000000` strokes and `#000000`/`#FFFFFF` text. `specs/README.md`'s directory map was updated in the same change that added the file.
- [x] `[AI] [AC-03]` **RED:** Input: the corpus. Action: create `tests/unit/main.rs`, `tests/integration/main.rs`, `tests/e2e/main.rs`, `tests/coverage/main.rs`, and shared `tests/support/` with the step vocabulary ported from `BehaviourSteps.fs` and `BehaviourSupport.fs`, bound to unimplemented production calls. Outcome: the specification fails for the right reason. Proof: `cargo test` fails with every scenario undefined or unimplemented, and no scenario passes. _Done 2026-09-07 at `d5c2ea6`._ `cargo test --test unit` reports **0 of 197 expanded scenarios pass; 197 do not**, and integration and E2E report the same; every failure names the step sentence that stopped it. The static coverage adapter passes with 5 tests, confirming all 107 scenarios bound exactly once at unit with no unit exemption, and bound or validly exempt at the other two layers. `cargo fmt --all --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. Two departures worth recording. The step vocabulary is 116 normalised sentences covering 758 expanded step lines, and it distinguishes `Unimplemented` (a known sentence with no production call yet) from `Undefined` (a sentence no adapter can run) — the second is the dangerous one, because an unrun scenario reports nothing and reporting nothing looks like passing; a test asserts the corpus contains none, and it already passes. `tests/support/world.rs` and `fixtures.rs` were drafted but deliberately not shipped: the driver trait and its three implementations arrive in Phase 3 with the validators that need them, and shipping unused code now would be scaffolding nobody asked for.
- [x] **Blocking checkpoint — Phase 2:** `[AI] [AC-01, AC-03]` Confirm the scaffold gates pass, upstream rules are self-contained, the corpus is policy-neutral and reviewed, the C4 model is written, and every scenario is red for a stated reason. **Proof:** _Cleared 2026-09-07 at `c6b4195`, pushed._ All four gates pass on the scaffold — `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo deny check`, and `cargo xtask test-quick`. Upstream rules are self-contained: `AGENTS.md` is 723 words inside the 750-word budget, `CLAUDE.md` contains the single line `@AGENTS.md`, and an exact search of `AGENTS.md`, `CLAUDE.md`, `specs/`, `src/`, and `xtask/` finds no occurrence of BeaverNest, Badakmini, `repo-governance`, Nx, or `750`. The corpus is policy-neutral and reviewed: 108 scenarios across eight files, reviewed one by one with outlines expanded, 46 failures found and every one fixed, then the changed scenarios re-reviewed and the one further failure fixed. The C4 model is written with three diagrams inside the label and colour rules. Every scenario is red for a stated reason: **0 of 199 expanded scenarios pass at each of the three adapters**, each failure naming the step sentence that stopped it, while the static coverage adapter passes 10 tests. One thing this checkpoint should be read as saying plainly: the phase's most valuable output was not the corpus but the discovery that the test harness's own parser was wrong in three ways, two of which made scenarios pass while asserting nothing. That was found by a reviewer running the parser rather than reading the corpus, and it is why five parser tests now sit in the quick gate.

## Phase 3 — Validator Port

Each item below ships code and therefore carries its own cycle. Run `cargo test --test unit` for RED and GREEN unless a different target is named.

- [x] `[AI] [AC-01, AC-02]` **RED:** Input: the frozen schema. Action: bind the configuration scenarios — missing file, unknown schema, unknown key, missing required key, wrong type, escaping path — against an unimplemented `config` module and `rhino repo-config validate`. Proof: `cargo test --test unit` fails on the configuration scenarios only. _Done 2026-09-07 at `bbe0ac5`._ The adapter failed to compile for exactly the missing production surface: ``error[E0425]: cannot find function `execute` in crate `rhino``` and ``error[E0432]: unresolved import `rhino::runtime` ``. The binding work itself is `tests/support/world.rs` (the `Driver` trait, `Declaration`, and `CommandResult`), `tests/support/fixtures.rs` (a complete legal configuration rendered as the YAML text a repository would commit), `tests/support/binding.rs` (the step `match`), and `tests/unit/driver.rs`. One design note worth keeping: the fixtures live in the harness, not in `src/`. A scenario says _the repository declares a complete configuration_, and the values behind that sentence are test data — putting them in the product would reintroduce precisely the borrowed default this extraction removes.
- [x] `[AI] [AC-01, AC-02]` **GREEN:** Action: implement `src/config.rs` and `src/runtime.rs` — the filesystem port trait, its in-memory test double, schema parsing, unknown-key rejection, and fail-closed exit `2` mapping. Proof: `cargo test --test unit` passes the configuration scenarios; a repository with no configuration exits `2`. _Done 2026-09-07 at `087f24d` and `bbe0ac5`._ All three executing adapters report **14 of 199 expanded scenarios pass** — `cargo test --test unit`, `--test integration`, and `--test e2e` each — and `grep '^repo-config:'` over each failure list is empty, so every configuration scenario is green at every boundary and nothing else has turned green by accident. Fourteen is the twelve scenarios with the three-example path-escape outline expanded. `cargo xtask test-quick` passes with its 10 static coverage tests; `cargo clippy --all-targets --all-features -- -D warnings` and `cargo fmt --all --check` are clean.
      Three implementation notes. **Unknown sections are ignored, unknown keys are refused** — `deny_unknown_fields` sits on each owned section struct and deliberately not on the document, because a consuming repository may keep another tool's policy in the same file, while a typo _inside_ a section RHINO owns is a policy that silently does nothing. **The parser's own error text is passed through rather than paraphrased**, because `yaml_serde` already reports `section.key: reason at line L column C`, which is the shape a maintainer needs; only faults the parser cannot see — path containment and canon-without-a-roster — are positioned by RHINO, by locating the declaring line in the source text. **Filesystem links are skipped unconditionally** in `DiskTree`, by configuration and not: following one can leave the repository, and a validator that walked out of the root would report findings against files the repository does not own.
- [ ] `[AI] [AC-01, AC-02]` **REFACTOR:** Action: remove any BeaverNest-shaped default that crept in and confirm the finding type is shared rather than duplicated per validator. Proof: an exact search finds no repository-specific literal in `src/`; `cargo clippy --all-targets --all-features -- -D warnings` passes.
- [ ] `[AI] [AC-02, AC-03]` **RED:** Action: bind the internal-link scenarios against an unimplemented `src/markdown/internal_link.rs`. Proof: `cargo test --test unit` fails on the six internal-link scenarios.
- [ ] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement Markdown scanning, inline and reference link extraction, fence handling, declared source exclusions, and local-target resolution; external URLs are recognized and skipped, never fetched. Proof: those scenarios pass.
- [ ] `[AI] [AC-02, AC-03]` **REFACTOR:** Action: extract the shared Markdown scan so internal links, Mermaid, and word budget read the tree once. Proof: unit scenarios stay green; clippy passes.
- [ ] `[AI] [AC-02, AC-03]` **RED:** Action: bind the `mermaid-cli` and `mermaid-governance` scenarios against an unimplemented `src/markdown/mermaid.rs`. Proof: `cargo test --test unit` fails on the twenty-one Mermaid scenarios.
- [ ] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement block extraction for the eight supported diagram kinds, `classDef` colour and contrast checks, entity decoding and markup removal, `<br>`-and-escaped-newline segment splitting, grapheme-cluster counting against the declared limits, the state-transition semicolon rejection, and the excluded constructs. Proof: those scenarios pass.
- [ ] `[AI] [AC-02, AC-03]` **REFACTOR:** Action: reconcile the regex dispositions from Phase 1, replacing any pattern that needed an unsupported construct with explicit parsing. Proof: scenarios stay green; the inventory has no open disposition.
- [ ] `[AI] [AC-02, AC-03, AC-06]` **RED:** Action: bind the `word-budget` scenarios and `md word-count inspect` against an unimplemented `src/governance/word_budget.rs`. Proof: `cargo test --test unit` fails on the seven word-budget scenarios.
- [ ] `[AI] [AC-02, AC-03, AC-06]` **GREEN:** Action: implement Unicode-aware word counting, ordered glob surface selection with last-match-wins, and `fail`/`warn`/`target` evaluation. Proof: those scenarios pass.
- [ ] `[AI] [AC-02, AC-03]` **RED:** Action: bind the `directory-map` scenarios against an unimplemented `src/governance/directory_map.rs`. Proof: `cargo test --test unit` fails on the eleven directory-map scenarios.
- [ ] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement README presence, direct-sibling completeness, sibling-link validity, and the `--directory` override over declared trees. Proof: those scenarios pass.
- [ ] `[AI] [AC-02, AC-03]` **RED:** Action: bind the `harness parity` scenarios against an unimplemented `src/harness.rs`. Proof: `cargo test --test unit` fails on the sixteen harness scenarios, which cover all three harnesses in the roster rather than the Claude path alone.
- [ ] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement canonical instruction and adapter checks, skill-bundle and agent-prompt record construction, SHA-256 digests, the declared harness roster with per-harness adapter extensions, optional command directories, and each harness's own capability file and format, prohibited-source globs, the closed capability and constraint vocabularies, and semantic MCP capability comparison. Proof: those scenarios pass.
- [ ] `[AI] [AC-02, AC-03]` **REFACTOR:** Action: fold the roster loop over declared harnesses so adding a fourth harness needs no code change; confirm nothing in `src/harness.rs` names a specific harness. Proof: an exact search for `codex`, `claude`, and `opencode` in `src/` returns nothing; scenarios stay green.
- [ ] `[AI] [AC-01, AC-03]` **RED then GREEN:** Action: implement `src/cli.rs` and `main.rs` — the command tree, `--root`, `--output text|json`, `--quiet`, `--verbose`, `--no-color`, repeated `--file` with `-` reading stdin, and the `0`/`1`/`2` exit mapping — driven by the `cli-contract` scenarios. Proof: RED shows the twenty CLI-contract scenarios failing; GREEN shows them passing.
- [ ] `[AI] [AC-01, AC-03]` Input: the implemented tool. Action: add the boundary policy tests — no network including loopback, no child process, no write to the inspected tree, no path escaping the root, no `unsafe` — and comment the loopback test to record that it is a product invariant stricter than the integration layer, which permits an owned loopback socket. Outcome: the Unix-tool claims are enforced rather than documented, and cannot be relaxed later by someone reading only the layer rule. Proof: `cargo test --test integration` includes the passing policy tests.
- [ ] `[AI] [AC-03]` Input: all four adapters. Action: complete the integration and E2E bindings and the static coverage check, classifying each test by the strongest real boundary its setup, subject, or assertions touch, and giving any exemption a boundary reason with named alternative proof — never difficulty, runtime, flakiness, or cost. Outcome: one corpus, three levels, statically verified mapping. Proof: `cargo test` passes at all levels; `cargo test --test coverage` reports no missing, unused, ambiguous, placeholder, or invalidly exempted binding.
      _Discovered during the configuration cycle (2026-09-07, `bbe0ac5`), and partly done early because the gate forced it:_ the quick gate runs `cargo clippy --all-targets`, so the integration and E2E adapters have to compile from the moment the shared support gains a driver trait. Their drivers therefore ship now — `tests/integration/driver.rs` against a real temporary directory and `tests/e2e/driver.rs` against the built executable, both materialising the declared repository through `tests/support/sandbox.rs`, which removes itself on drop including when a scenario fails. What remains for this item is the classification and exemption work, not the plumbing.
- [ ] `[AI] [AC-03]` Input: the composed gates. Action: confirm `cargo xtask test-quick` runs the unit adapter and the static behaviour check only, and add `scheduled.yml` running every integration suite followed by the complete unfiltered E2E target. Outcome: the slow adapters are gated where BeaverNest's standard puts them, not in a hook. Outcome check: no Git hook and no quick gate executes an integration or E2E scenario. Proof: an exact search of the gate definition and hooks finds no integration or E2E invocation; the scheduled workflow runs both in order.
- [ ] `[AI] [AC-03]` Input: the implementation. Action: bring line coverage over the validator modules to at least 99%, naming `main.rs` and the concrete filesystem adapter as the only exclusions in the repository `README.md` as the standard requires. Outcome: the single numeric gate, with its denominator exclusions declared where a reader will find them. Proof: `cargo llvm-cov` reports at or above 99%; `README.md` names both exclusions and why a unit test cannot reach them.
- [ ] **Blocking checkpoint — Phase 3:** `[AI] [AC-01, AC-02, AC-03]` Confirm every scenario green at every applicable level, static coverage clean, line coverage at or above 99%, clippy and `cargo deny` clean, no repository-specific literal in `src/`, and boundary policies enforced. **Proof:** —

## Phase 4 — First Consumer: RHINO Itself

RHINO adopts RHINO before BeaverNest does. The purpose is generalization pressure at the earliest point it is cheap: a repository with no `repo-governance/` tree, no `plans/` tree, and no multi-harness roster will break any assumption the port inherited from BeaverNest, and it will break it while the schema is still soft.

- [ ] `[AI] [AC-10]` Input: the implemented binary and HIPPO's documentation tree as the reference model. Action: write `README.md` in HIPPO's section order, `docs/README.md` with the category table, the four Diátaxis category READMEs with their page lists and next-steps blocks, and every page named in the documentation table — executing every command shown and pasting real output. Outcome: a consumer can learn, do, look up, and understand without reading source. Outcome check: no page contradicts `specs/`. Proof: every `console` transcript reproduced against the built binary, and each page assigned to exactly one category.
- [ ] `[AI] [AC-08, AC-10]` Input: the release history. Action: write `CHANGELOG.md` with the Keep-a-Changelog headings, the Semantic Versioning statement, the release-immutability statement, and link definitions. Outcome: a consumer can see what changed between pins. Proof: the `v0.1.0` entry describes consumer-visible behaviour, not commits.
- [ ] `[AI] [AC-01, AC-02, AC-10]` Input: the frozen schema and RHINO's own tree. Action: write upstream `repo-config.yml` describing RHINO — word-budget surfaces for `AGENTS.md` and `README.md`, mapped trees `docs/` and `specs/`, Mermaid limits, scan exclusions, and an explicit `harness-parity` declaring `AGENTS.md`, `CLAUDE.md`, and an empty harness roster. Outcome: a second repository is describable in the schema, and the degenerate roster is declared rather than omitted. Proof: `rhino repo-config validate` passes; no section is left implicit.
- [ ] `[AI] [AC-01, AC-02]` Input: the freshly built binary and that configuration. Action: run every validator against the RHINO repository and record each finding. Outcome: the first evidence of what the tool assumes. Proof: the recorded finding list, separated into genuine RHINO defects and BeaverNest-shaped assumptions leaking through the tool.
- [ ] `[AI] [AC-01, AC-02]` **RED then GREEN:** Input: each leaked assumption from the previous task. Action: add a scenario to the corpus that fails because of it, then fix the tool so it passes — never by adding a RHINO-shaped default in place of a BeaverNest-shaped one. Outcome: generalization is proved by a second real repository rather than asserted. Proof: RED shows each new scenario failing; GREEN shows the full corpus passing and the RHINO tree clean.
- [ ] `[AI] [AC-10]` Input: the passing self-validation. Action: wire it into `ci.yml` and the `xtask test-quick` gate so the binary runs against its own repository on every push. Outcome: a permanent second corpus, not a one-time exercise. Proof: a deliberate broken link and an over-budget `AGENTS.md`, each introduced in a scratch commit, fail CI; both are reverted.
- [ ] **Blocking checkpoint — Phase 4:** `[AI] [AC-01, AC-02, AC-10]` Confirm RHINO's own tree validates clean, every leaked assumption has a scenario and a fix, no RHINO-specific default replaced a BeaverNest-specific one, self-validation runs in CI, and the schema survived a repository that is not BeaverNest. **Proof:** —

## Phase 5 — Generalization Against Every Target Tree

Nothing is tagged until the schema has met all four trees. These are read-only dry runs with a locally built binary: no bootstrap, no lock, no CI wiring, and no commits in the sibling repositories.

- [ ] `[AI] [AC-01, AC-02]` Input: grind-in-public's `apps/badakmini-cli` and this repository's. Action: compare the two implementations command by command and rule by rule, recording where the independent Go implementation and the F# one already disagree. Outcome: the required configurability, established empirically rather than predicted. Proof: a recorded difference table covering command grammar, word-budget scope, mapped trees, parity vocabulary, and diagram coverage.
- [ ] `[AI] [AC-01, AC-02]` Input: the HIPPO and grind-in-public trees and their written rules. Action: draft a candidate `repo-config.yml` for each without committing it, taking every value from that repository's current validator or rules and never inventing one. Outcome: two more repositories expressed in the schema. Proof: `rhino repo-config validate` passes for each draft; every value traces to a source in its own repository.
- [ ] `[AI] [AC-01, AC-02]` **RED then GREEN:** Input: HIPPO's tree, which has no `CLAUDE.md` and no harness adapters. Action: add the scenario for an omitted `instruction-adapter`, prove it red, then make that field optional while keeping the competing-instruction-source prohibition in full force. Proof: RED shows the scenario failing; GREEN shows the corpus passing and HIPPO's tree validating.
- [ ] `[AI] [AC-01, AC-02]` **RED then GREEN:** Input: grind-in-public and HIPPO, which use ASCII art rather than Mermaid and therefore contain zero diagrams. Action: add the zero-diagram scenario, prove it red, then make a zero-match scan report a clean, explicit zero — never a warning, never an unexamined success, and never an inference that the walk failed. Outcome: a repository whose visualization convention is not Mermaid is a first-class citizen, not a degraded one. Proof: RED shows the scenario failing; GREEN shows both trees reporting zero diagrams checked and exiting clean.
- [ ] `[AI] [AC-01, AC-02]` Input: each draft configuration. Action: run every validator against all four trees, then triage findings into genuine repository defects, legitimate policy differences, and BeaverNest-shaped assumptions leaking through the tool. Outcome: the assumption set that four trees expose but one cannot. Proof: the recorded triage, with every leak given a scenario and a fix that adds no new repository-shaped default.
- [ ] `[AI] [AC-02]` Input: the hardened schema. Action: re-run the RHINO and BeaverNest trees against the changed binary to confirm no earlier repository regressed. Outcome: hardening is additive. Proof: both trees still validate as they did before the change.
- [ ] **Blocking checkpoint — Phase 5:** `[AI] [AC-01, AC-02]` Confirm all four trees are describable, every leaked assumption has a scenario and a fix, no repository-shaped default was introduced, the absent-adapter and zero-diagram cases are covered, earlier trees have not regressed, and no sibling repository has been modified. **Proof:** —

## Phase 6 — Release Pipeline and First Tag

- [ ] `[AI] [AC-08]` **RED:** Input: the packaging requirements. Action: write the artifact-verification tests — four platform archives present, `checksums.txt` matching, each executable's `version --json` matching the tag and commit exactly, and each stripped size within the declared ceiling — against an unimplemented `xtask dist`. Proof: `cargo test --package xtask` fails on every artifact assertion.
- [ ] `[AI] [AC-08]` **GREEN:** Action: implement `cargo xtask dist`, including the size-optimized release profile (`opt-level = "z"`, fat LTO, one codegen unit, abort on panic, stripped symbols). Outcome: reproducible, verifiable assets from one command. Proof: a local `cargo xtask dist` for the host platform passes the verification tests; record the measured stripped size and set the ceiling from it.
- [ ] `[AI] [AC-08]` **REFACTOR:** Action: if the measured size exceeds expectations, apply the ordered options from the binary-size section — narrow `regex` features, then explicit parsing, then `clap`'s builder API — and re-measure. Outcome: a stated, held size budget. Proof: recorded before-and-after sizes, or a recorded decision that no reduction was needed.
- [ ] `[AI] [AC-08]` Input: HIPPO's `release.yml` as reference. Action: write `release.yml` — tag trigger, peeled-tag-on-main ancestry verification, a native build matrix across macOS and Ubuntu on `amd64` and `arm64`, asset assembly, artifact verification, and `gh release create --verify-tag`. Outcome: a release cannot describe a commit that is not on `main`. Proof: a dry run on a throwaway pre-release tag publishes and verifies four archives plus checksums, after which that tag and release are deleted.
- [ ] `[HUMAN] [AC-08]` Input: a green upstream `main`. Action: authorize and push the `v0.1.0` tag. Outcome: an immutable first release exists to pin. Proof: the published release with four archives, `checksums.txt`, and matching `version --json` output per platform. _This is `[HUMAN]` because publishing an immutable public artifact is an external, irreversible action._
- [ ] `[AI] [AC-08, AC-12]` Input: the published `v0.1.0` assets. Action: record the distribution end result for all four platforms — stripped executable size, downloaded archive size, and the release pipeline's own wall-clock. Outcome: the artifact half of the distribution claim has numbers. Proof: the four-platform table in `learnings.md`.
- [ ] **Blocking checkpoint — Phase 6:** `[AI] [AC-08, AC-12]` Confirm four platform archives, matching checksums, correct embedded identity per platform, sizes within budget and recorded, the throwaway tag removed, and `v0.1.0` published and not replaced. **Proof:** —

## Phase 7 — BeaverNest Adoption

- [ ] `[AI] [AC-01, AC-02]` Input: the constants table, the schema as hardened by Phases 4 and 5, and the Phase 5 draft configurations as worked examples. Action: write `repo-config.yml` at this repository's root, transcribing every current Badakmini constant. Outcome: this repository's policy is declared and readable. Proof: `repo-config.yml` covers every row of the constants table; no credential, hostname, or runtime value appears in it, per [public repository data safety](../../../repo-governance/conventions/public-repository-data-safety.md).
- [ ] `[AI] [AC-04]` **RED:** Input: [the HIPPO consumer specification](../../../specs/tools/hippo-consumer/README.md). Action: write `specs/tools/rhino-consumer/` with its maps and `behaviours/rhino-bootstrap.feature`, then `.github/scripts/test-rhino-bootstrap.sh` implementing it, before `./rhino` exists. Proof: the suite fails because the bootstrap is absent.
- [ ] `[AI] [AC-04]` **GREEN:** Action: write `./rhino` as the documented fork of `./hippo` — renamed identifiers, RHINO cache root, RHINO release URL, `rhino` executable — preserving lock validation, the platform matrix, warm-cache digest and identity verification, install serialization, live-owner identity checks, release claims, and bounded retention. Write `rhino.lock` pinning `v0.1.0`, its commit, and all four checksums. Proof: `.github/scripts/test-rhino-bootstrap.sh` passes; `./rhino version --json` matches the lock after a cold cache install.
- [ ] `[AI] [AC-04]` **REFACTOR:** Action: verify the two bootstraps cannot interfere — distinct cache roots, distinct install guards, and RHINO retention never seeing a HIPPO release. Proof: an exact search shows no shared path; both tools run successfully in sequence from a cold cache.
- [ ] `[AI] [AC-02, AC-05]` Input: the pinned binary and the declared configuration. Action: create `apps/rhino-consumer/` with `project.json` composing `test:repo` and `test:bootstrap`, and its [project README](../../../repo-governance/conventions/project-readmes.md). Outcome: every existing reference to a `test:repo` target keeps working. Proof: `HIPPO npm exec -- nx run -p rhino-consumer -t test:repo` runs all eight validator invocations against this repository.
- [ ] `[AI] [AC-05]` Input: the first full run. Action: reconcile every finding against Badakmini's current clean result, fixing the tool or the configuration — never the governed content — until both agree. Outcome: the new gate is clean for the same reasons the old one was. Proof: both gates run clean over the same corpus. The Phase 1 baseline run on 2026-09-07 reported 57 word-budgeted files, 47 directory maps across four trees (6, 5, 11, 25), 158 link-checked Markdown files, three-harness parity covering seven skills, two agents, and one capability declaration, and 33 Mermaid diagrams — but these are re-read from the run immediately preceding the comparison rather than quoted from here, because the link count alone drifted by three while this plan was being written.
- [ ] `[AI] [AC-04, AC-12]` Input: the working bootstrap. Action: measure the consumer half of distribution — cold pin-to-executable wall-clock from an empty cache with the network stated, warm per-invocation resolution overhead against the cached executable invoked directly, and cache-root size after install and after bounded retention runs. Outcome: the path from a lock line to a running check is measured, not just the artifact at the end of it. Outcome check: if warm resolution overhead is a material fraction of a validator run, fix the caching before any cutover — it would be paid on every invocation forever. Proof: the recorded figures with their network and cache conditions, and the comparison against the baseline's SDK-install-plus-build cost.
- [ ] `[AI] [AC-06]` Input: both binaries. Action: run `md word-count inspect` under each over every governed Markdown file and compare counts. Outcome: the numeric gate is proven not to have drifted. Proof: zero differing files; any difference stops the line until resolved.
- [ ] **Blocking checkpoint — Phase 7:** `[AI] [AC-02, AC-04, AC-05, AC-06, AC-12]` Confirm declared policy is complete, the bootstrap suite passes, the pinned binary verifies from a cold cache, distribution cost is measured and its warm overhead acceptable, both gates agree, and word counts match exactly. **Proof:** —

## Phase 8 — BeaverNest Cutover and Retirement

- [ ] `[AI] [AC-05]` Input: the verified consumer. Action: repoint `.husky/pre-push` — validation paths lose the Badakmini projects and gain `repo-config.yml` and `apps/rhino-consumer/**`, a RHINO consumer predicate is added, that predicate also runs `shellcheck` over the owned `rhino` fork and its suite, and the gate command becomes the new target — in one edit. Outcome: no commit exists at which neither validator gates a push. Proof: a simulated push over a governed-content range runs the new gate; a range touching only the consumer boundary runs only the bootstrap suite.
- [ ] `[AI] [AC-05]` Input: the working consumer. Action: delete `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, `specs/apps/badakmini/`, and `beaver-nest.sln`; remove the `badakmini-test-symphony` job and its .NET setup from `.github/workflows/scheduled-quality-gates.yml`; and update `.gitignore`, `apps/bnest-app/tools/release.mjs`, and `RTK.md`. Outcome: .NET leaves this repository entirely. Proof: an exact search for `badakmini`, `Badakmini`, `dotnet`, and `.fsproj` returns hits only in `plans/done/` and `.nx/` cache.
- [ ] `[AI] [AC-05]` Input: the deleted projects and Git history as the recovery source. Action: rehearse the rollback before relying on it — restore `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, `beaver-nest.sln`, and the previous `.husky/pre-push` into a scratch worktree under `local-tmp/`, run the retired gate there, and confirm it reports the same clean result it gave before deletion. Outcome: the recovery path is proven while it is still cheap to prove, rather than discovered to be broken during an incident. Outcome check: if the restore cannot reproduce the prior clean result, the deletion is not final until it can. Proof: the retired gate's clean output from the scratch worktree, and the worktree removed afterwards.
- [ ] `[AI] [AC-05]` Input: the retired tree. Action: perform the authority cutover verification — clear the RHINO cache, start a fresh process, run the full gate from the persisted configuration and pin with no fallback present, then inject one violation of each kind into a scratch copy under `local-tmp/` and confirm each is detected. Outcome: "clean" is proven to mean "checked". Proof: the cold-cache clean run plus five detected injected violations; the scratch copy is removed afterwards.
- [ ] `[AI] [AC-07]` Input: the changed enforcement mechanism, with the new target already working. Action: run one [rules-propagation](../../../repo-governance/workflows/rules-propagation.md) transaction across `AGENTS.md`, `repo-governance/README.md`, and the fifteen affected rule files — including the eleven that embed the literal `nx run -p badakmini-cli -t test:repo` command, one of which is `rules-propagation.md` itself — and give `coding-harness-contract.md` and `directory-maps.md` the treatment described in [the cutover design](tech-docs/03-beaver-nest-cutover.md) so neither becomes a second copy of a declared value. Also carry the `governance harness-contract validate` to `harness parity validate` rename through every rule that spells it. Outcome: this repository's rules name RHINO as what enforces them, and the repaired workflow can verify itself. Outcome check: the transaction's own step-4 verification must run the repaired command, not the deleted one. Proof: the recorded terminal result — `PASS_CHANGED` or a specific named blocker, never an unrecorded pass.
- [ ] `[AI] [AC-07]` Input: the remaining references. Action: update `README.md`, `docs/reference/glossary.md`, `docs/how-to-guides/coding-harnesses.md`, `libs/ex-bdd/test/integration/ex_bdd/boundary_policy_test.exs`, `plans/backlogs/family-learning-engine/delivery.md`, `specs/apps/README.md`, and `specs/tools/README.md`; discover by exact search whether anything under `.agents/skills/**` also names the retired target and update it if so. Outcome: no document contradicts the repository. Proof: an exact search finds no live reference to the retired projects or targets outside `plans/done/`.
- [ ] `[AI] [AC-07]` Input: the completed cutover. Action: update this repository's affected specifications under the [plan specification-change convention](../../../repo-governance/conventions/plan-specification-changes.md) — the removed Badakmini tree, the new consumer specification, and both affected directory maps. Outcome: `specs/` matches the as-built repository. Proof: the specification map gate passes.
- [ ] `[AI] [AC-12]` Input: the Phase 1 BeaverNest baseline and the completed cutover. Action: re-measure every metric by the same method on the same host, and compare. Outcome: the change is quantified for this repository. Outcome check: if the warm gate is slower or peak memory higher than baseline, stop the line — investigate and fix, or obtain and record an explicit justified acceptance; never pass it silently. Proof: the before-and-after rows in `learnings.md`, including any metric that got worse.
- [ ] **Blocking checkpoint — Phase 8:** `[AI] [AC-05, AC-07, AC-12]` Confirm the hook routes correctly, both projects and the solution are gone, the rollback was rehearsed and reproduced the prior clean result, the cold-cutover run is clean and provably checking, injected violations are detected, propagation returned a recorded terminal result after repairing its own verification command, and no live reference remains. **Proof:** —

## Phase 9 — HIPPO: Additive Adoption

HIPPO has no validator to retire, so nothing here can regress. It is sequenced between the two cutovers deliberately, as a cheap confirmation that a repository with no prior validator adopts cleanly.

- [ ] `[AI] [AC-01, AC-04, AC-11]` Input: the Phase 5 draft and the published release. Action: add `repo-config.yml`, `./rhino`, and `rhino.lock` to HIPPO, reusing the bootstrap pattern established for this repository. Outcome: HIPPO consumes the pinned binary. Proof: `./rhino version --json` matches the lock after a cold-cache install; `rhino repo-config validate` passes.
- [ ] `[AI] [AC-02, AC-11]` Input: the adopted tool. Action: run every validator over HIPPO and reconcile findings against its written rules — fixing HIPPO's content only where it genuinely violates its own documented standard, and never loosening a rule to obtain a pass. Outcome: HIPPO's documentation contract is mechanically enforced for the first time. Proof: a clean run, with any content fix recorded and justified against the rule it violated.
- [ ] `[AI] [AC-11]` Input: HIPPO's existing gates. Action: wire RHINO into its quick gate and pre-push, and confirm every gate HIPPO already had still passes unchanged. Outcome: adoption is additive. Proof: HIPPO's full existing suite passes; an exact search shows nothing was removed.
- [ ] `[HUMAN] [AC-11]` Input: the verified HIPPO change. Action: authorize the commit and push in that repository. Outcome: the change lands under its own history. Proof: the pushed commit. _This is `[HUMAN]` because it is a separate repository and authorization does not carry across repository boundaries._
- [ ] **Blocking checkpoint — Phase 9:** `[AI] [AC-04, AC-11]` Confirm HIPPO validates clean, its existing gates are untouched and passing, nothing was removed, the mutual pin between HIPPO and RHINO is documented on both sides, and the change is pushed. **Proof:** —

## Phase 10 — grind-in-public: Adoption, Cutover, and Retirement

The second cutover. Its validator was written independently in Go, so its findings are the ones most likely to disagree in interesting ways — which is why it runs last, with three repositories of evidence already behind the tool.

- [ ] `[AI] [AC-01, AC-04]` Input: the Phase 5 draft and the published release. Action: add `repo-config.yml`, `./rhino`, and `rhino.lock` to grind-in-public. Outcome: the pinned binary is available alongside the existing validator. Proof: cold-cache install verified against the lock.
- [ ] `[AI] [AC-02, AC-05]` Input: both validators. Action: run each over the repository and reconcile every difference, fixing the tool or the configuration but never the governed content, and investigating each disagreement rather than assuming a RHINO defect. Outcome: two independent implementations agree on one tree. Proof: the recorded reconciliation; any tool change re-verified against the other three trees.
- [ ] `[AI] [AC-06]` Input: both validators. Action: compare word counts file by file across every governed file in that repository. Outcome: the numeric gate is proven not to have drifted there either. Proof: zero differing files; any difference stops the line.
- [ ] `[AI] [AC-05]` Input: the reconciled result. Action: repoint grind-in-public's Nx targets and hooks from `badak-mini`'s three hygiene commands to the RHINO equivalents, leaving its project-contract, governance-structure, workflow-contract, spelling, HIPPO bootstrap, and `harness rule-change` checks untouched. Outcome: only general hygiene changes owner. Outcome check: `rule-change` reads staged paths and stdin payloads and triggers that repository's own workflows, so it is outside RHINO's read-only, process-free posture and is not a candidate for replacement. Proof: an exact search shows every non-hygiene check still invoked — including `badak-mini harness rule-change validate` and its `hook` form — and the repository gate passes.
- [ ] `[AI] [AC-05]` Input: the working consumer. Action: delete only the three retired hygiene packages — `internal/governance/`, `internal/markdownlinks/`, and `internal/parity/` with their tests, their CLI wiring, their Nx targets, and their bindings in both modules — then perform the authority cutover verification: cold cache, fresh process, the retired checks absent, then one injected violation of each kind detected in a scratch copy. Outcome: RHINO is authoritative for hygiene there and provably checking. Outcome check: `internal/rulechange/` shares the module, so neither Go module, neither `go.mod`, nor the Go toolchain setup is removed; a task that deletes them has deleted a check this plan never intended to replace. Proof: the clean cold run plus each injected violation detected; `badak-mini harness rule-change validate` still passing afterwards; the scratch copy removed.
- [ ] `[AI] [AC-05]` Input: grind-in-public's deleted hygiene packages and its Git history. Action: rehearse that repository's rollback on the same terms — restore the three deleted packages, their bindings in both modules, and the previous hooks into a scratch worktree, run its retired Go gate there, and confirm the prior clean result. Outcome: the second retirement's recovery path is proven too, independently of the first. Proof: the retired gate's clean output from the scratch worktree, and the worktree removed afterwards.
- [ ] `[AI] [AC-07]` Input: the changed enforcement mechanism there, with its new target already working. Action: run one transaction of grind-in-public's own `repo-governance/workflows/rules/rules-propagation.md` — its inventory, canonical-home, conflict-resolution, and idempotency-gate sequence — across the thirteen surfaces naming its Go validator, including its own workflow's embedded gate command. Outcome: that repository's rules name RHINO, under its own governance rather than this plan's. Outcome check: closed against its hierarchy, not this repository's; the two transactions are not required to reach the same wording. Proof: the recorded terminal result and an exact search finding no live reference.
- [ ] `[HUMAN] [AC-05]` Input: the verified grind-in-public change. Action: authorize the commit and push in that repository. Outcome: the cutover lands under its own history. Proof: the pushed commits. _This is `[HUMAN]` for the same repository-boundary reason as Phase 9._
- [ ] `[AI] [AC-12]` Input: the Phase 1 grind-in-public baseline and the completed cutover. Action: re-measure by the same method and compare, holding the same two cutover thresholds — warm gate no slower, peak memory no higher. Outcome: the Go-to-Rust comparison is recorded honestly. Outcome check: a comparable-performance result is the expected outcome there and is recorded as such — the win in that repository is one shared implementation instead of its own, not raw speed. Proof: the before-and-after rows in `learnings.md`.
- [ ] `[AI] [AC-05, AC-06]` **Recovery, only if triggered:** trigger is grind-in-public's gate failing after cutover for a reason not attributable to genuinely non-compliant content, or a word count that changed there. Action: restore the three deleted hygiene packages, their bindings in both modules, and the previous hooks from that repository's Git history, then re-run its prior gate. This rollback touches only grind-in-public and never a published RHINO tag. **Disposition:** —
- [ ] **Blocking checkpoint — Phase 10:** `[AI] [AC-02, AC-05, AC-06, AC-07, AC-12]` Confirm both validators agreed before retirement, word counts matched, only hygiene checks changed owner, the three hygiene packages are gone while `rule-change` and its module remain, that repository's rollback was rehearsed, injected violations are detected, that repository's own propagation returned a recorded terminal result, and the change is pushed. **Proof:** —

## Phase 11 — Verification, Cleanup, and Reconciliation

- [ ] `[AI] [AC-05, AC-07, AC-09]` Input: the complete change. Action: run the full uncached repository gate, the affected `test:quick` graph, and the bootstrap suite here; upstream's full suite including its scheduled integration and E2E targets; and each sibling repository's own full gate. Then confirm the routed origin still serves the baseline revision and is healthy. Outcome: all four repositories are green and the household service is untouched. Proof: each gate's result per repository, and the routed health and revision.
- [ ] `[AI] [AC-05, AC-09]` Input: the working trees and machine. Action: remove every scratch copy and rehearsal worktree under `local-tmp/` in each repository, any throwaway upstream tag or release, temporary caches created for cold-start testing, and any watcher or server started during delivery; confirm no non-production process remains. Outcome: nothing temporary survives the plan. Proof: exact searches showing each artifact absent.
- [ ] `[AI] [AC-12]` Input: every measurement taken during delivery. Action: assemble one before-and-after table in [the benchmark design](tech-docs/05-benchmarks.md) covering both cutover repositories, every metric, and the four-platform distribution figures. Outcome: the plan closes with its evidence in one place instead of scattered across dated entries. Proof: the completed table, with each figure traceable to a dated `learnings.md` entry and its conditions.
- [ ] `[AI] [AC-01]` Input: delivery evidence. Action: capture learnings, then search `plans/ideas/` for overlap with `ose-public` and `ose-private` adoption — including the unresolved question of merging with `ose-public`'s own `rhino-cli` — and either merge into an existing brief or create one distinct mapped brief, linked from `learnings.md`. Outcome: the follow-on work is recorded where the next reader will find it. Proof: the merged or created brief and its map entry.
- [ ] `[AI] [AC-01 … AC-12]` Input: final evidence. Action: reconcile every acceptance criterion, checklist item, specification, document, rule, and map with what was actually done; then archive by collision-safe dated move with the stage-index and map updates in the same change. Outcome: delivery history and as-built truth agree. Proof: the destination collision check and the final repository gate accompanying the archive move.
- [ ] `[AI] [AC-04, AC-05, AC-06]` **Recovery, only if triggered:** trigger is the repository gate failing after cutover for a reason not attributable to genuinely non-compliant content, a bootstrap that cannot verify the pinned release, or a governed word count that changed across the rewrite. Action: restore `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, `beaver-nest.sln`, and the previous `.husky/pre-push` from Git; re-run the Badakmini gate; diagnose before retrying. Never edit or delete a published upstream tag — a defect becomes a new patch version and a new pin. **Disposition:** —
- [ ] **Final checkpoint:** `[AI] [AC-01 … AC-12]` Confirm every acceptance criterion has evidence, all four repositories are green, both retirements are complete with their rollbacks dormant and dispositioned, the routed service is unchanged on its baseline revision, nothing temporary remains, and the archive move is verified. **Proof:** —
