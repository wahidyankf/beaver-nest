# Delivery

Every command runs through the pinned HIPPO guard. `HIPPO` below abbreviates `./hippo run --class ephemeral --disk-path . --`; write it in full when executing. Upstream work happens in the local RHINO checkout, referred to below as `<rhino-checkout>`. Sibling work happens in the local HIPPO and grind-in-public checkouts. Commit and push authorization does not cross a repository boundary: each repository needs its own. Commit and push in either repository require separate [authorization](../../../repo-governance/conventions/commit-authorization.md).

## Phase 1 — Baseline, Evidence, and Frozen Contracts

- [x] `[AI] [AC-09]` Input: clean `main` and the routed BeaverNest origin. Action: record the revision, routed health, and served revision; confirm no other in-progress plan overlaps. Outcome: a baseline this plan can be judged against. Proof: recorded revision and health in `learnings.md`. _Done 2026-09-07: baseline `debb17116`, clean tree, sole in-progress plan; routed origin 0/12 failures, p95 3 ms, serving `f536f97da`._
- [x] `[AI] [AC-12]` Input: the unmodified BeaverNest and grind-in-public trees and [the benchmark design](tech-docs/05-benchmarks.md). Action: capture the before baseline for both retired validators — warm and cold gate wall-clock, single-validator startup, peak resident memory, toolchain acquisition, and repository footprint — following the stated method: HIPPO-guarded, Nx cache skipped, first two warm runs discarded, median of the repeats with the observed range. Outcome: a baseline that cannot be reconstructed later, because both validators cease to exist at cutover. Outcome check: also record the exact corpus counts the gate reports, since the plan's expected-proof numbers are read from here rather than assumed. Proof: the recorded table in `learnings.md` with date, host state, and the exact commands. _Done 2026-09-07: both tables recorded. BeaverNest — warm gate median 0.392 s, startup 0.277 s, peak RSS 64.4 MiB, cold restore+build 5.035 s, 38 files / 6,793 lines, 674 MB SDK. grind-in-public — warm gate 0.236 s built and 0.373 s as wired through `go run`, startup 0.215 s, peak RSS 12.1 MiB, `go build` 1.461 s to a 4.7 MiB binary, 41 files / 8,878 lines, 258 MB GOROOT plus a 2.3 GB module cache that is not shed. Corpus counts re-read live from both gates; the scripts are kept under the ignored `local-tmp/rhino-bench/` so Phase 11 re-measures by the same harness. A harness caveat is recorded with them: the bracketed timings carry a constant overhead and are comparable to each other, not absolute._
- [x] `[AI] [AC-02]` Input: `apps/badakmini-cli/Governance.fs` and `HarnessContract.fs`. Action: inventory every regular expression with the engine features it uses, flagging lookaround, backreferences, balancing groups, and named-group behaviour that Rust's `regex` crate does not provide. Outcome: a per-pattern disposition of "port as-is" or "rewrite as explicit parsing". Proof: the inventory table in `learnings.md`, with a count of patterns in each disposition. _Done 2026-09-07: 31 distinct patterns — 28 port as-is, 3 rewritten as explicit parsing (one lookbehind, one lookahead, one line split that was never a regex problem)._
- [x] `[AI] [AC-02]` Input: [dependency selection](../../../repo-governance/development/dependency-selection.md) and current primary sources. Action: re-verify the `yaml_serde` evidence recorded on 2026-09-07 — latest release date, repository activity, and the absence of any OSV or RustSec advisory against it or `libyaml-rs` — and settle the remaining open decision, the `regex` disposition, recording need, rejected alternatives, evidence, and ownership impact. Outcome: a defensible dependency set before any code exists. Proof: recorded decisions in `learnings.md`; if `yaml_serde` has since stalled or acquired an advisory, record the switch to `serde_norway` or `serde_yaml_ng` with its evidence. _Done 2026-09-07: `yaml_serde` 0.10.7 re-verified, no advisory, decision unchanged; `regex` 1.13.1 selected over `fancy-regex` and `pcre2`, with the linear-time guarantee as the deciding reason._
- [x] `[AI] [AC-02]` Input: `ose-public`'s `apps/rhino-cli` surface and its `repo-config.yml`. Action: read both in full and record every point where this plan's command spelling, flag names, exit codes, or schema shape agree or diverge, with a reason for each divergence. Outcome: alignment is a recorded decision rather than a coincidence. Proof: the alignment table in `learnings.md`. _Done 2026-09-07: six agreements, eight reasoned divergences; the reading also corrected the plan's framing of the `harness parity` rename from a departure to a convergence._
- [x] `[AI] [AC-01, AC-02]` Input: the constants table in [the configuration contract](tech-docs/02-configuration-contract.md) and the two source files. Action: transcribe the exact capability, constraint, and required-MCP vocabularies from `HarnessContract.fs` into the schema, and confirm no constant in the inventory is missing. Outcome: a complete, frozen schema with no elided values. Proof: an exact search showing every listed constant has a schema home. _Done 2026-09-07: seven capabilities, two constraints, the MCP vector, eleven scan exclusions, and eight palette colours all verified present. Two gaps found and closed — the Mermaid palette was assigned to the binary and is now configuration; `scan.exclude-directories` carried `dist`, which the source never excluded, and omitted five directories it does._
- [x] **Blocking checkpoint — Phase 1:** `[AI] [AC-01, AC-02, AC-09, AC-12]` Confirm both baselines captured with their conditions, routed health unchanged, every regex disposed, both dependency decisions evidenced, alignment recorded, and the schema complete with no placeholder. **Proof:** _Cleared 2026-09-07._ Both baselines are recorded with their host conditions, including the differing HIPPO admission state between the warm and cold BeaverNest series, which is stated rather than averaged away. Routed health re-checked at the end of the phase: twelve samples through the routed HTTPS origin, zero failures, p95 56 ms against a 500 ms budget, still serving `f536f97da` — unchanged in revision and well inside the continuity budget; the phase touched no runtime input. All 31 regexes are disposed with none open. Both dependency decisions carry evidence — `yaml_serde` 0.10.7 re-verified advisory-free, `regex` 1.13.1 chosen for its linear-time guarantee. The `ose-public` alignment table records six agreements and eight reasoned divergences. The schema is complete with no elision, and freezing it found and closed two real gaps. Three plan defects were corrected in this phase rather than carried forward: grind-in-public's fourth `rule-change` leaf, the Mermaid palette wrongly assigned to the binary, and a stale link count.

## Phase 2 — Upstream Repository and Specification Corpus

- [x] `[AI] [AC-01]` Input: the empty `wahidyankf/rhino` repository and [the upstream design](tech-docs/01-rhino-repository.md). Action: scaffold the Rust-idiomatic layout — root package with an `xtask` workspace member, `rust-toolchain.toml`, `rustfmt.toml`, `clippy.toml`, `deny.toml`, `.cargo/config.toml`, `src/lib.rs` with `#![forbid(unsafe_code)]`, `src/main.rs`, the three husky hooks matching HIPPO's and BeaverNest's — `pre-commit` running `lint-staged`, `commit-msg` running `commitlint`, and a HIPPO-guarded `pre-push` running `cargo xtask test-quick` — and a `ci.yml` running format, clippy, `cargo deny`, and tests. Outcome: an empty but fully gated repository. Proof: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo deny check` all pass on the scaffold. _Done 2026-09-07 at `e0a7fe4`, pushed._ All three named proofs pass: `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`). Three departures from the written scaffold, each for minimal sufficiency rather than convenience: the four `[[test]]` targets, the `scheduled.yml` workflow, and the `coverage`, `self-check`, and `dist` xtask subcommands are all deferred to the phases that make them real, because declaring a target whose file does not exist breaks the gate the scaffold is supposed to prove, and shipping a subcommand that returns "not implemented yet" is the placeholder this plan forbids. `xtask test-quick` therefore runs format and clippy today. The HIPPO consumer bootstrap was copied in and its concurrency mapping retargeted from the sibling consumers' `NX_PARALLEL`, `GOMAXPROCS`, and `DOTNET_PROCESSOR_COUNT` to `CARGO_BUILD_JOBS` and `RUST_TEST_THREADS`.
- [x] `[AI] [AC-01]` **Added 2026-09-07 at the maintainer's request, during execution.** Input: the published `wahidyankf/rhino` repository. Action: set its GitHub About — description and topics — so the repository presents itself the way its sibling tools do. Outcome: the repository is discoverable and self-describing before anyone is asked to depend on it. Proof: the description reads "RHINO (Repository Hygiene & INtegration Orchestrator): configuration-driven repository-hygiene validation for documentation, directory maps, internal links, Mermaid accessibility, and coding-harness parity", matching HIPPO's acronym-then-colon form, with topics `accessibility`, `cli`, `developer-tools`, `documentation`, `governance`, `linter`, `linux`, `macos`, `markdown`, and `rust`. _Done 2026-09-07._ Homepage is deliberately left empty, as HIPPO's is, until there is a documentation site to point at.
- [x] `[AI] [AC-01]` Input: this repository's principles for BDD, specification maintenance, TDD, minimal sufficiency, and dependency selection. Action: write upstream `AGENTS.md` restating those principles as self-contained upstream rules in Rust's own vocabulary — never by importing this repository's target names, directory names, or Nx wiring — plus the generic-tool rule, the exit-code and `version --json` compatibility rule, the `cargo xtask dist` release rule, the never-replace-a-tag rule, and the `specs/`-at-root departure. Add `CLAUDE.md` containing only the import. Outcome: the upstream repository is self-governing without reading another repository at all. Proof: `AGENTS.md` is within the 750-word budget and `CLAUDE.md` contains only the import. _Done 2026-09-07 at `3ce4aac`._ `AGENTS.md` is 700 words by the repository's own Unicode-aware counting rule, inside the 750-word budget; `CLAUDE.md` contains the single line `@AGENTS.md` and nothing else. An exact search of `AGENTS.md` finds no BeaverNest target name, directory name, or Nx reference, and all five required rules are present — generic-tool, exit codes with `version --json`, `cargo xtask dist`, never-replace-a-tag, and the `specs/`-at-root departure.
- [x] `[AI] [AC-03]` Input: the seven `.feature` files under `specs/apps/badakmini/cli/behaviours/`. Action: move them to upstream `specs/behaviours/`, reword every scenario that asserts a BeaverNest constant so its policy is established in a `Given` and asserted against, carry the flag renames through every scenario that names them — `--format` becomes `--output`, and the `--version` flag becomes the `version` subcommand with `--json` — drop the TickSpec `{hash}` workaround where the new runner parses `#` correctly, rename `markdown-links.feature` to `internal-link.feature` to match the renamed leaf, and write the tree's directory maps. Outcome: a policy-neutral corpus that specifies a generic tool. Proof: every one of the 81 scenarios is accounted for by name — kept, merged into a named successor, or renamed — with no scenario dropped; each merge and each addition is listed with its reason, and the resulting count is stated rather than assumed to be 81. An exact search finds no literal `750`, `repo-governance`, `.agents`, `.claude`, `.codex`, or `.opencode` asserted as a tool default. _Done 2026-09-07 at `323af99`._ The full disposition ledger is in `learnings.md`: 77 kept or renamed one-to-one, 2 merged into 1, 2 split into 4 — 82 derived from 81 with none dropped — plus 25 additions each listed with its reason, for **107 scenarios across eight files**. The two renamed files are `harness-contract` → `harness-parity` and `markdown-links` → `internal-link`. An exact search of the new corpus finds no literal `750`, `repo-governance`, `.agents`, `.claude`, `.codex`, `.opencode`, or `{hash}`. `specs/README.md` and `specs/behaviours/README.md` carry directory maps naming every sibling.
- [x] `[AI] [AC-03]` Input: the reworded corpus. Action: run the [Gherkin implementation review](../../../repo-governance/workflows/gherkin-implementation-review.md) over every changed scenario. Outcome: the rewrite is semantically reviewed, not just mechanically applied. Proof: the recorded review result per feature file. _Done 2026-09-07 at `7d5f96b` and `c6b4195`._ Per-file results are tabled in `learnings.md`. First pass over 197 expanded rows: **151 PASS, 46 FAIL** — `cli-contract` 30/32, `repo-config` 11/2, `harness-parity` 19/3, `directory-map` 11/2, `internal-link` 2/7, and all three Mermaid and word-budget files clean. Every failure was fixed rather than accepted, the changed scenarios were re-reviewed independently, and the one further failure that pass found was fixed too. Binding registry and step vocabulary were clean on the first pass at every adapter. **The review's largest finding was not in the corpus.** Thirty-one failures came from one defect in the hand-written Gherkin reader, and two more of the same family were found with it: escaped pipes were treated as column separators, `Background:` was discarded, and docstrings were discarded with `#` opening a comment inside them. Twelve of the affected rows were passing while invoking nothing they named. Five parser tests now sit in the quick gate.
- [x] `[AI] [AC-01]` Input: `specs/apps/badakmini/cli/architecture.md`. Action: rewrite it upstream as `specs/architecture.md` with the Rust containers, the library-plus-binary boundary, the configuration input, and the read-only, network-free, process-free constraints, following the [architecture specification standard](../../../repo-governance/development/architecture-specifications.md). Outcome: an as-built C4 model for the new system. Proof: every Mermaid diagram satisfies the label and colour rules. _Done 2026-09-07 at `abdf311`._ Three diagrams — context, containers, components — every node segment at or under 32 graphemes and every edge segment at or under 24, checked by scanning only inside `mermaid` fences so Markdown table rows cannot produce a false positive. Every `classDef` uses a fill from the declared six with `#000000` strokes and `#000000`/`#FFFFFF` text. `specs/README.md`'s directory map was updated in the same change that added the file.
- [x] `[AI] [AC-03]` **RED:** Input: the corpus. Action: create `tests/unit/main.rs`, `tests/integration/main.rs`, `tests/e2e/main.rs`, `tests/coverage/main.rs`, and shared `tests/support/` with the step vocabulary ported from `BehaviourSteps.fs` and `BehaviourSupport.fs`, bound to unimplemented production calls. Outcome: the specification fails for the right reason. Proof: `cargo test` fails with every scenario undefined or unimplemented, and no scenario passes. _Done 2026-09-07 at `d5c2ea6`._ `cargo test --test unit` reports **0 of 197 expanded scenarios pass; 197 do not**, and integration and E2E report the same; every failure names the step sentence that stopped it. The static coverage adapter passes with 5 tests, confirming all 107 scenarios bound exactly once at unit with no unit exemption, and bound or validly exempt at the other two layers. `cargo fmt --all --check` and `cargo clippy --all-targets --all-features -- -D warnings` are clean. Two departures worth recording. The step vocabulary is 116 normalised sentences covering 758 expanded step lines, and it distinguishes `Unimplemented` (a known sentence with no production call yet) from `Undefined` (a sentence no adapter can run) — the second is the dangerous one, because an unrun scenario reports nothing and reporting nothing looks like passing; a test asserts the corpus contains none, and it already passes. `tests/support/world.rs` and `fixtures.rs` were drafted but deliberately not shipped: the driver trait and its three implementations arrive in Phase 3 with the validators that need them, and shipping unused code now would be scaffolding nobody asked for.
- [x] **Blocking checkpoint — Phase 2:** `[AI] [AC-01, AC-03]` Confirm the scaffold gates pass, upstream rules are self-contained, the corpus is policy-neutral and reviewed, the C4 model is written, and every scenario is red for a stated reason. **Proof:** _Cleared 2026-09-07 at `c6b4195`, pushed._ All four gates pass on the scaffold — `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo deny check`, and `cargo xtask test-quick`. Upstream rules are self-contained: `AGENTS.md` is 723 words inside the 750-word budget, `CLAUDE.md` contains the single line `@AGENTS.md`, and an exact search of `AGENTS.md`, `CLAUDE.md`, `specs/`, `src/`, and `xtask/` finds no occurrence of BeaverNest, Badakmini, `repo-governance`, Nx, or `750`. The corpus is policy-neutral and reviewed: 108 scenarios across eight files, reviewed one by one with outlines expanded, 46 failures found and every one fixed, then the changed scenarios re-reviewed and the one further failure fixed. The C4 model is written with three diagrams inside the label and colour rules. Every scenario is red for a stated reason: **0 of 199 expanded scenarios pass at each of the three adapters**, each failure naming the step sentence that stopped it, while the static coverage adapter passes 10 tests. One thing this checkpoint should be read as saying plainly: the phase's most valuable output was not the corpus but the discovery that the test harness's own parser was wrong in three ways, two of which made scenarios pass while asserting nothing. That was found by a reviewer running the parser rather than reading the corpus, and it is why five parser tests now sit in the quick gate.

## Phase 3 — Validator Port

Each item below ships code and therefore carries its own cycle. Run `cargo test --test unit` for RED and GREEN unless a different target is named.

- [x] `[AI] [AC-01, AC-02]` **RED:** Input: the frozen schema. Action: bind the configuration scenarios — missing file, unknown schema, unknown key, missing required key, wrong type, escaping path — against an unimplemented `config` module and `rhino repo-config validate`. Proof: `cargo test --test unit` fails on the configuration scenarios only. _Done 2026-09-07 at `bbe0ac5`._ The adapter failed to compile for exactly the missing production surface: ``error[E0425]: cannot find function `execute` in crate `rhino``` and ``error[E0432]: unresolved import `rhino::runtime` ``. The binding work itself is `tests/support/world.rs` (the `Driver` trait, `Declaration`, and `CommandResult`), `tests/support/fixtures.rs` (a complete legal configuration rendered as the YAML text a repository would commit), `tests/support/binding.rs` (the step `match`), and `tests/unit/driver.rs`. One design note worth keeping: the fixtures live in the harness, not in `src/`. A scenario says _the repository declares a complete configuration_, and the values behind that sentence are test data — putting them in the product would reintroduce precisely the borrowed default this extraction removes.
- [x] `[AI] [AC-01, AC-02]` **GREEN:** Action: implement `src/config.rs` and `src/runtime.rs` — the filesystem port trait, its in-memory test double, schema parsing, unknown-key rejection, and fail-closed exit `2` mapping. Proof: `cargo test --test unit` passes the configuration scenarios; a repository with no configuration exits `2`. _Done 2026-09-07 at `087f24d` and `bbe0ac5`._ All three executing adapters report **14 of 199 expanded scenarios pass** — `cargo test --test unit`, `--test integration`, and `--test e2e` each — and `grep '^repo-config:'` over each failure list is empty, so every configuration scenario is green at every boundary and nothing else has turned green by accident. Fourteen is the twelve scenarios with the three-example path-escape outline expanded. `cargo xtask test-quick` passes with its 10 static coverage tests; `cargo clippy --all-targets --all-features -- -D warnings` and `cargo fmt --all --check` are clean.
      Three implementation notes. **Unknown sections are ignored, unknown keys are refused** — `deny_unknown_fields` sits on each owned section struct and deliberately not on the document, because a consuming repository may keep another tool's policy in the same file, while a typo _inside_ a section RHINO owns is a policy that silently does nothing. **The parser's own error text is passed through rather than paraphrased**, because `yaml_serde` already reports `section.key: reason at line L column C`, which is the shape a maintainer needs; only faults the parser cannot see — path containment and canon-without-a-roster — are positioned by RHINO, by locating the declaring line in the source text. **Filesystem links are skipped unconditionally** in `DiskTree`, by configuration and not: following one can leave the repository, and a validator that walked out of the root would report findings against files the repository does not own.
- [x] `[AI] [AC-01, AC-02]` **REFACTOR:** Action: remove any BeaverNest-shaped default that crept in and confirm the finding type is shared rather than duplicated per validator. Proof: an exact search finds no repository-specific literal in `src/`; `cargo clippy --all-targets --all-features -- -D warnings` passes. _Done 2026-09-07 at `887fe22`._ The search — `AGENTS.md`, `CLAUDE.md`, `repo-governance`, `node_modules`, `.claude`, `.codex`, `opencode`, `beaver`, `badak`, `hippo`, `grind`, `plans/done`, any `#RRGGBB` literal, `.agents`, `npx`, and the bare numbers `750`, `32`, `24` — returns three hits, all the schema key name `agents-root`, which is a key RHINO owns rather than a value any repository holds. One default did crawl in and is gone: a doc comment illustrating the word budget with `750`, which is BeaverNest's number sitting in `src/`. `cargo clippy --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`, `cargo deny check` (advisories, bans, licenses, sources all ok), and `cargo xtask test-quick` all pass, and all three executing adapters still report **14 of 199** with no configuration failure.
      Rather than confirm a shared finding type existed, this step created it, because with zero validators ported there was nothing yet to de-duplicate and waiting would have meant de-duplicating five copies later. `src/report.rs` holds `Finding` and `Report`: one rendering, one exit-code mapping, and an **atomic** category prefix — `[word-budget]` is a constant per command rather than assembled from the command path, so a consumer grepping it keeps working if a command is renamed or renested. The summary line always states what was inspected including when that is zero, which is the explicit clean zero two of the four target repositories permanently need. `Finding` itself is not exercised at this commit: nothing `repo-config validate` can report is a finding, since a configuration fault is exit `2` by contract. The next item is the first validator and uses it.
- [x] `[AI] [AC-02, AC-03]` **RED:** Action: bind the internal-link scenarios against an unimplemented `src/markdown/internal_link.rs`. Proof: `cargo test --test unit` fails on the six internal-link scenarios. _Done 2026-09-07 at `c1c119b`._ **Correction to this item's own text:** the feature holds **seven** scenarios, not six — one is a three-example outline, so nine expansions. Six was a miscount at planning time; the corpus is the authority and the disposition ledger in `learnings.md` already carries seven for this feature. All nine expansions failed at their assertion rather than at a missing sentence — `expected exit 0, got 2`, `expected exit 1, got 2`, and `expected 2 violations, counted 1` — which is the proof that every arrangement step before them ran. Two harness decisions worth keeping: violations are counted from prefixed stderr lines, because the E2E adapter has only the process contract and a count taken from anywhere else would not be the same claim at all three boundaries; and `{nul}` is substituted in the binding rather than written into the corpus, because a Gherkin file cannot carry a NUL byte.
- [x] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement Markdown scanning, inline and reference link extraction, fence handling, declared source exclusions, and local-target resolution; external URLs are recognized and skipped, never fetched. Proof: those scenarios pass. _Done 2026-09-07 at `8db239a`._ All three executing adapters report **23 of 199 expanded scenarios pass** — up from 14 — and `grep '^internal-link:'` over each failure list is empty at unit, integration, and E2E. `cargo clippy --all-targets --all-features -- -D warnings` and `cargo deny check` (advisories, bans, licenses, sources) are clean. Two dependencies were added and both are used by this commit: `regex` for link extraction, `globset` for the declared source exclusions.
      **One defect the corpus caught, worth recording because the shape recurs.** Resolving `..` past the repository root was written `segments.pop()?`, and in a function returning `Option<Fault>` that `?` means _return no fault_. A link pointing out of the repository therefore passed silently — the exact failure mode this validator exists to prevent, produced by a one-character idiom. Only the `../../outside.md` example failed, so the scenario outline's other two examples would have covered the arm without exposing it; the third example is what made it visible.
      Three pieces were shared from the start rather than left for the next refactor, because the second and third consumers are the next two items: `src/scan.rs` selects Markdown by the repository's declared exclusion list, matched by directory _name_ at any depth; `src/markdown.rs` answers "is this inside a fenced example?" once; and `DiskTree` learns its exclusions before it walks, so a real repository's dependency directories are never descended into rather than descended into and then filtered out.
- [x] `[AI] [AC-02, AC-03]` **REFACTOR:** Action: extract the shared Markdown scan so internal links, Mermaid, and word budget read the tree once. Proof: unit scenarios stay green; clippy passes. _Done 2026-09-07 at `90fb966`._ All three adapters still report **23 of 199**; `cargo clippy --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`, and `cargo xtask test-quick` (10 static coverage tests) pass. `scan::Corpus` now holds the walk and the read, and `internal_link` consumes it instead of reading files itself.
      Centralising the read turned out to matter for a reason beyond cost, and it is the reason worth keeping: an unreadable file has to refuse the whole run with exit `2`, and one implementation of that refusal is one place for it to be right. A validator that quietly skipped a file it could not open would report a repository it never read as clean — which is the same failure mode as a validator running on a malformed configuration, already refused at the same exit code. Source exclusion stays per-validator and deliberately does not remove a file from the corpus: a document excluded as a link _source_ is still a valid link _target_, and the corpus carries two scenarios asserting exactly that distinction.
- [x] `[AI] [AC-02, AC-03]` **RED:** Action: bind the `mermaid-cli` and `mermaid-governance` scenarios against an unimplemented `src/markdown/mermaid.rs`. Proof: `cargo test --test unit` fails on the twenty-one Mermaid scenarios. _Done 2026-09-07 at `597b4d5`._ **Two corrections to this item's own text.** The features are `mermaid-cli` and `mermaid-legibility`, not `mermaid-governance` — the second was renamed when the corpus moved upstream and de-policied, and the ledger in `learnings.md` already carries the new name. And there are **24** scenarios across the two, not twenty-one: 16 plus 8, expanding to **71**. Twenty-one was a planning-time miscount; the corpus is the authority.
      All 71 expansions failed on the absent validator — `expected exit 1, got 2` and `expected exit 0, got 2` — rather than on a sentence nothing could run. The substance of the RED is `tests/support/mermaid.rs`, a catalogue of 42 named samples plus three generators, because scenarios name a sample rather than carrying its text and a copy per scenario would drift. Each sample is written to be a faithful instance of its own name, which is what makes a failure attributable: `missing stroke` is a class declaration with no stroke and nothing else wrong with it.
      The contrast expectations follow from the declared palette rather than from a rule written down a second time. Against it, `#0173B2` carries white text and not black (4.10:1), while `#DE8F05` and `#029E73` carry black and not white — which is what makes `insufficient text contrast` a sample where both colours are declared and only the pairing is wrong. The grapheme samples exist because counting code points instead of clusters is the drift worth catching: the combining sample is 32 clusters in 64 code points, and the encoded one is 32 clusters in 160 characters.
- [x] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement block extraction for the eight supported diagram kinds, `classDef` colour and contrast checks, entity decoding and markup removal, `<br>`-and-escaped-newline segment splitting, grapheme-cluster counting against the declared limits, the state-transition semicolon rejection, and the excluded constructs. Proof: those scenarios pass. _Done 2026-09-07 at `1b08584`._ All three executing adapters report **83 of 199 expanded scenarios pass**, up from 23. Sixty of the 71 Mermaid expansions are green. `cargo clippy --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`, `cargo deny check`, and `cargo xtask test-quick` all pass, and the repository-specific literal search over `src/` now returns nothing at all.
      **Eleven Mermaid expansions remain red, all for the same stated reason:** they invoke `--file` (1) or `--output json` (10), and flag parsing is the CLI-contract item further down this phase. They are not being carried as unexplained failures — each fails with `rhino: unrecognized command`, and the CLI-contract item turns all eleven green. This is the plan's ordering working as written rather than a deviation from it.
      **One rule was decided here rather than in the plan, and it changed a scenario's outcome.** An unsafe diagram was producing two findings — an undeclared fill _and_ a contrast failure measured against that same undeclared fill — where the corpus asserts exactly one. Contrast is now measured only between two colours the repository actually permits. Measuring against a fill it already rejected is a second finding about a value that has to change anyway, and it buries the one a reader can act on. `insufficient text contrast`, where both colours are declared and only the pairing fails, is unaffected and still reports.
      Two further behaviours worth recording because they are policy decisions rather than mechanics. A diagram whose syntax this build cannot read is skipped **whole**, never partially inspected, because a half-parsed diagram produces findings about text the tool misread. And a colour outside a `classDef` is refused wherever it appears rather than compared against the palette — the declared sets cannot reach it there — while a plain `%%` comment is prose _about_ the palette and is never inspected, which is why a repeated or inaccurate palette comment is correctly not a finding.
- [x] `[AI] [AC-02, AC-03]` **REFACTOR:** Action: reconcile the regex dispositions from Phase 1, replacing any pattern that needed an unsupported construct with explicit parsing. Proof: scenarios stay green; the inventory has no open disposition. _Done 2026-09-07 at `334d1dd` and `9776aac`._ All three adapters report **84 of 200** with zero internal-link failures and zero Mermaid failures other than the eleven flag-dependent ones; `cargo clippy --all-targets --all-features -- -D warnings`, `cargo fmt --all --check`, and `cargo xtask test-quick` pass. The reconciliation table is in `learnings.md`.
      **This item’s proof is met with one stated exception, and the exception is scheduled rather than waived.** Dispositions 2 (negative lookbehind) and 21 (line splitting) are closed. Disposition 29 — the lookahead terminating a TOML table body — is still open because it belongs to harness parity, a later item in this same phase; it closes there, and the phase checkpoint below is where a claim of no open disposition becomes genuinely provable.
      Closing disposition 2 needed a scenario rather than only a rewrite, because excluding image syntax is behaviour and the corpus covered none of it. `Image syntax is not an internal link` was added, taking the corpus to **109 scenarios, 200 expanded**. The mandated Gherkin implementation review over the changed scope rejected the first draft: it asserted a violation _count_, and a document holding both a broken image and a broken document link yields one violation whether the guard is right or inverted — a single-character mistake would have survived inside the scenario written to stop it. The assertion now pins count and identity together, and inverting the guard was confirmed to fail the scenario by name before the guard was restored. The same review produced three harness fixes: a fail-closed sandbox root, bound-exactly-once assertions at integration and E2E to match unit, and the removal of a `Declaration` flag that four arms set and nothing read.
- [x] `[AI] [AC-02, AC-03, AC-06]` **RED:** Action: bind the `word-budget` scenarios and `md word-count inspect` against an unimplemented `src/governance/word_budget.rs`. Proof: `cargo test --test unit` fails on the seven word-budget scenarios. _Done 2026-09-07 at `2fbf972`._ All seven failed on the absent validator. Surfaces are recorded as an **ordered** list, because the order is the declaration — the last matching surface wins, and a set would lose exactly the behaviour one of these scenarios asserts.
      **One harness defect this RED exposed, and it would have misled every future feature without a Background.** The fixture flag was `present`, which every declaring arm had to remember to set. The word-budget feature has no Background, so its scenarios arrived with no configuration at all and failed with the validator refusing an absent file — a reason none of them mentions, and one that would have looked like a validator bug rather than a harness one. The flag is now `absent`, so the default is the complete legal base the corpus README says a declaring Given is additive over, and no arm can forget it.
- [x] `[AI] [AC-02, AC-03, AC-06]` **GREEN:** Action: implement Unicode-aware word counting, ordered glob surface selection with last-match-wins, and `fail`/`warn`/`target` evaluation. Proof: those scenarios pass. _Done 2026-09-07 at `4c868c2`._ All three adapters report **90 of 200**, up from 85. Six of the seven word-budget scenarios are green; the seventh inspects a single file through `--file`, which the CLI-contract item delivers. Every gate passes and the repository-specific literal search over `src/` returns nothing.
      Two behaviours here are policy rather than mechanics, and both are worth a reader knowing. **Last match wins** because that is how a repository says “this tree, except that one file”, which makes the surface list ordered data rather than a set. And **a clean run still lists every path it read** — a surface that matched nothing and a surface that matched everything both pass, and only the listing tells them apart; this is the same reason the Mermaid validator reports an explicit zero.
      Two departures from a first reading of the schema. `warn` and `target` are parsed and carried but not yet evaluated: no scenario asserts an advisory tier, and emitting one without a scenario would be behaviour nothing checks. And Markdown extensions and declared globs are now matched **case-insensitively**, which the `rules/nested/RULES.MD` fixture forced — whether one file shouts its extension is not a policy decision any repository made.
- [x] `[AI] [AC-02, AC-03]` **RED:** Action: bind the `directory-map` scenarios against an unimplemented `src/governance/directory_map.rs`. Proof: `cargo test --test unit` fails on the eleven directory-map scenarios. _Done 2026-09-07 at `efefffc`._ Eight of the eleven failed on the absent validator, each naming the assertion that stopped it.
      **The other three passed, and passed vacuously — recorded here rather than counted as coverage.** The invalid-location outline asserts only that an argument error is raised, and an unrecognised command already exits `2`, so all three expansions pass without `--directory` existing at all. They begin genuinely exercising the flag at the CLI-contract item below; until then they prove nothing. This is the second time in this plan that an exit-code-only assertion has passed for the wrong reason — the first was the truncated command vectors the Phase 2 Gherkin review found — which is why `the only violation is …` is now one shared helper asserting count and identity together across three validators.
- [x] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement README presence, direct-sibling completeness, sibling-link validity, and the `--directory` override over declared trees. Proof: those scenarios pass. _Done 2026-09-07 at `9bea315`._ All three adapters report **103 of 200**, up from 93, with no directory-map failure at any boundary. Every gate passes and the repository-specific literal search over `src/` returns nothing. **The `--directory` override is not implemented here**: it is a flag, and flags arrive with the CLI-contract item — which is also where the three vacuous expansions above stop being vacuous.
      A map entry is deliberately **narrower** than an ordinary link: it may not be absolute, carry a scheme, leave its own directory, or point at something that is not there. A map is a claim about _this_ directory, and an entry addressing anywhere else is a claim about something the reader did not ask about — which is why the same `https://` target the internal-link validator correctly skips is a finding here. Two cases resolve in the entries’ favour: a section with no entries is a **complete** map rather than a missing one, and an entry may address a subdirectory either directly or through that directory’s own README.
      One ordering decision worth keeping: entries are resolved before siblings are checked, so an unusable entry is reported as itself rather than as a sibling that appears to have gone missing. Reporting the same defect twice under two names is how a finding list stops being readable.
      One constant lives in `src/` and is deliberate: `README.md`. It is stated as tool behaviour in the configuration contract rather than as a repository value, so it is not a leaked default — but it is the one place a consuming repository could disagree with RHINO without a configuration key to say so, and Phases 4 and 5 will surface it if any of the four trees does.
- [x] `[AI] [AC-02, AC-03]` **RED:** Action: bind the `harness parity` scenarios against an unimplemented `src/harness.rs`. Proof: `cargo test --test unit` fails on the sixteen harness scenarios, which cover all three harnesses in the roster rather than the Claude path alone. _Done 2026-09-07 at `92e70d3`._ **Correction to this item's own text:** the feature holds **twenty-two** scenarios, not sixteen; sixteen was a planning-time count and the corpus is the authority. Twenty-one of the twenty-two failed on the absent validator, each naming its own assertion rather than a missing sentence, and all three adapters reported **104 of 200**. The twenty-second — an unreadable capability declaration expecting exit 2 — passed **vacuously**, because an unrecognised command already exits 2; it proves nothing until the validator exists, and it is recorded here rather than counted.
      The binding work is `tests/support/harness.rs`: a whole repository as a fixture — one canonical instruction body, one skill bundle, one agent prompt, and per-harness adapters and capability declarations for every name in the declared roster — from which each scenario breaks exactly one thing. Capability declarations are written in each harness's own syntax, JSON for two and TOML for one, so a passing run has to have compared meaning rather than text.
- [x] `[AI] [AC-02, AC-03]` **GREEN:** Action: implement canonical instruction and adapter checks, skill-bundle and agent-prompt record construction, SHA-256 digests, the declared harness roster with per-harness adapter extensions, optional command directories, and each harness's own capability file and format, prohibited-source globs, the closed capability and constraint vocabularies, and semantic MCP capability comparison. Proof: those scenarios pass. _Done 2026-09-07 at `9f6cfd6`, then substantially repaired at `adc8af3` after the mandated review; see the next paragraph, which is the more important half of this item._ All three adapters now report **135 of 211**, with every harness-parity scenario green except the one narrowed by `--harness`, which is a flag and therefore belongs to the CLI-contract item below.
      **This item closes the last open Phase 1 regex disposition.** Disposition 29 — the lookahead terminating a TOML table body — is retired rather than translated: JSON is parsed with `serde_json` and TOML with the `toml` crate, both into one `serde_json::Value`, and the required server is then found by **name** rather than by the key its vendor nests it under. No vendor key appears in `src/`, so a harness that spells its section differently is not penalised for its own syntax, and the comparison is about the executable vector rather than about text. Two regexes remain in `src/`, both in Markdown parsing, both recorded in `learnings.md`.
      **The mandated [Gherkin implementation review](../../../repo-governance/workflows/gherkin-implementation-review.md) rejected the first implementation, and it was right to.** Over 66 rows — 22 scenarios × 3 adapters — it ran twenty mutants of the new validator against the corpus and **seventeen survived**. The worst was not in this feature at all: `the repository snapshot is unchanged` compared a copy the test had made with the map it had made it from, so it could never fail, and a validator that wrote a file into the repository it was inspecting passed at all three boundaries. `AGENTS.md` says that property is held by tests; it was held by nothing.
      Everything else was one defect repeated: fixtures that broke two or three rules at once and then asserted a kind several rules share, so deleting any one of them left the scenario green. The repair is recorded in full in `learnings.md`. In outline: `Driver::invoke` now returns what the adapter observed on **both sides** of the run, walked with `std::fs` rather than through the port; every multi-fault fixture is split so one scenario breaks one rule; every production arm has its own message; every assertion names the path as well as the rule; and eight branches the corpus proved nowhere gained scenarios of their own.
      **One production rule was deleted rather than tested, because it was unreachable.** A canonical skill's declared name is required to equal its own directory, so two skills cannot share a name — the duplicate-detection branch could never run. Requiring the two to match makes duplication unrepresentable rather than merely detectable, which is the stronger property; the branch is gone and the corpus now says what is actually enforced.
      **Proof, stated as the mutation result rather than as a green run:** all twenty mutants are killed. Nineteen die at all three boundaries. The twentieth — a validator writing a file into the tree — dies at **E2E only**, and this is a real and stated asymmetry rather than an oversight: at unit and integration `rhino::execute` runs inside the test process, so a write to a hard-coded relative path lands in the crate root rather than in the sandbox, and whichever scenario ran first would have created it. Only at E2E is the inspected repository also the process's working directory, so only there is every write it could make inside the observation. The unit and integration rows still read genuine before-and-after evidence; they simply prove less, and the code says so where a future reader will find it.
      Corpus: **120 scenarios, 211 expanded**, up from 109 and 200. The eleven additions are all consequences of this review.
- [x] `[AI] [AC-02, AC-03]` **REFACTOR:** Action: fold the roster loop over declared harnesses so adding a fourth harness needs no code change; confirm nothing in `src/harness.rs` names a specific harness. Proof: an exact search for `codex`, `claude`, and `opencode` in `src/` returns nothing; scenarios stay green. _Done 2026-09-07 at `6e16d6e`._ The exact search returns nothing for `codex`, `claude`, `opencode`, `cursor`, `copilot`, `mcpServers`, `mcp_servers`, `nx-mcp`, `.agents`, `AGENTS.md`, `CLAUDE.md`, `beaver`, `badak`, `repo-governance`, or `750` anywhere in `src/`. All three adapters hold at **135 of 211** and every gate passes.
      The canon — skills root, skills, agents, and the required capability — is now one borrowed structure rather than four threaded parameters, because it is a property of the repository and not of the harness being checked against it. The three per-harness reconciliations consequently share one shape, which is the form a fifth check could join without another argument.
      **The proof is a run rather than a reading.** A scratch repository declaring **four** harnesses — two different agent extensions, both capability formats, and a command directory on one of them — reconciles clean with no change to `src/`: `checked 4 harnesses, no findings`, `canon 4 harnesses, 1 skills, 1 agents, 4 reconciled capability declarations`. Breaking the fourth harness three ways then produces exactly its three findings — `missing-agent-adapter`, `divergent-capability`, `missing-skill-adapter` — sorted, with the reconciled count falling to three and the digest unchanged, since the canon did not change. A clean pass over an unchecked harness and a clean pass over a checked one are the same output, so the second half of that run is what makes the first half mean anything. The scratch tree was removed afterwards.
      One defect the four-harness run exposed and fixed: the summary read `checked 4 harnesss`. The pluraliser appended an `s` to a subject already ending in one — invisible while every count in the corpus was one or three and the subject was `file`, `link`, or `diagram`.
      One constant remains in `src/harness.rs` and is deliberate, on the same footing as `README.md` in directory-map: `SKILL.md`. It is stated as tool behaviour rather than as a repository value, and Phases 4 and 5 will surface it if any of the four trees disagrees.
- [x] `[AI] [AC-01, AC-03]` **RED then GREEN:** Action: implement `src/cli.rs` and `main.rs` — the command tree, `--root`, `--output text|json`, `--quiet`, `--verbose`, `--no-color`, repeated `--file` with `-` reading stdin, and the `0`/`1`/`2` exit mapping — driven by the `cli-contract` scenarios. Proof: RED shows the twenty CLI-contract scenarios failing; GREEN shows them passing. _Done 2026-09-07 at `9d44b2b`, then substantially repaired at `efc6b6a` and `053edb1` after the mandated review._ All four adapters are green over **122 scenarios, 218 expanded**.
      The command tree is a table rather than a parser: eight leaves, each declaring the flags it accepts, so a flag a leaf has no use for is refused rather than dropped. `build.rs` embeds `git rev-parse HEAD` at compile time. Every validator now returns one `Report`, and one place decides how it renders — which is what keeps a refusal from quietly acquiring a shape of its own under `--output json`.
      **The mandated [Gherkin implementation review](../../../repo-governance/workflows/gherkin-implementation-review.md) rejected the first implementation, over 195 rows, and found one serious production defect and three lesser ones.** The serious one is the kind this whole extraction exists to prevent: **`--root` inherited the caller's scan exclusions**, so a repository selected with `--root` was walked with the _working directory's_ exclusion list. Reproduced end to end — cwd excluding `guides`, `--root` at a repository that excludes nothing and holds a failing diagram — the tool reported `checked 0 diagrams, no findings, exit 0` where the control reported `exit 1`. A false clean, produced by a flag whose entire purpose is to change which repository is read.
      The other three: `--file /rules/README.md` was silently reinterpreted as repository-relative, so a file the caller did not name got inspected; `--json` silently overrode an explicit `--output text`, making the same two flags mean different things depending on assembly order; and `--help` short-circuits before flag validation, which is left as-is and now has a row of its own saying so.
      **The corpus was the larger failure.** Sixty-three rows asserted an exit code and nothing else. Forty-eight help rows would have passed a build that printed an empty help body; nine presentation-flag rows would have passed a build that turned `--quiet` into a help request; three `--root` rows always pointed the flag at the tree the run would have used anyway, so the prefix-stripping body of `MemoryTree::rooted_at` was executed by no scenario; and three rows walked zero things in the nothing-to-find fixture, so a validator that did nothing passed. Three harness-level scanners — `json_number`, `json_string`, `first_violation` — were substring readers that could read the wrong record, truncate on an escaped quote, or split a message containing `}`.
      **Proof, stated as the mutation result rather than as a green run:** eleven mutants written against the eleven findings, all killed. Nine die at all three boundaries. The remaining two are a matched pair and boundary-specific _by construction_ rather than by omission: `MemoryTree::rooted_at` is only reachable from unit and `DiskTree::rooted_at` only from integration and E2E, so a mutant of either can only die where that tree is the one in use. Together they cover `--root` at every boundary.
      Corpus: **122 scenarios, 218 expanded**, up from 120 and 211.
- [x] `[AI] [AC-01, AC-03]` Input: the implemented tool. Action: add the boundary policy tests — no network including loopback, no child process, no write to the inspected tree, no path escaping the root, no `unsafe` — and comment the loopback test to record that it is a product invariant stricter than the integration layer, which permits an owned loopback socket. Outcome: the Unix-tool claims are enforced rather than documented, and cannot be relaxed later by someone reading only the layer rule. Proof: `cargo test --test integration` includes the passing policy tests. _Done 2026-09-07 at `b546b5c`._ `cargo test --test integration` reports **8 passed** — the corpus plus six policy tests — and `--test e2e` reports **2 passed**.
      The loopback comment says what the item asked it to say, and says why: the layer rule's permission is about test _plumbing_, whereas this is a claim about the _product_, and a local port is exactly where an unwanted collaborator would listen.
      **Two of the five claims are absences, and an absence cannot be observed by running the tool** — no run proves the _next_ run opens no socket. Those two are checked against `src/` and against `Cargo.lock` instead, which is where the capability would have to appear before it could ever be exercised. The lock-file check carries a **positive control**, because a search that finds nothing and a search that cannot find anything return the same answer; without it the test would have been vacuous, and the mutant that proved so is recorded in `learnings.md`.
      **The no-write claim is proved at two boundaries on purpose, and this is the same asymmetry the harness-parity item recorded.** Integration proves nothing was written _through the root_. Only E2E, where the inspected repository is also the spawned process's working directory, can see a write to a relative path — and a mutant that wrote such a file survived at integration precisely because an earlier run had already created it in the crate root. `tests/e2e/policy.rs` gives every leaf its own sandbox and kills it deterministically.
      **Proof, stated as the mutation result:** seven mutants, one violating exactly one policy each, all killed.
- [x] `[AI] [AC-03]` Input: all four adapters. Action: complete the integration and E2E bindings and the static coverage check, classifying each test by the strongest real boundary its setup, subject, or assertions touch, and giving any exemption a boundary reason with named alternative proof — never difficulty, runtime, flakiness, or cost. Outcome: one corpus, three levels, statically verified mapping. Proof: `cargo test` passes at all levels; `cargo test --test coverage` reports no missing, unused, ambiguous, placeholder, or invalidly exempted binding.
      _Discovered during the configuration cycle (2026-09-07, `bbe0ac5`), and partly done early because the gate forced it:_ the quick gate runs `cargo clippy --all-targets`, so the integration and E2E adapters have to compile from the moment the shared support gains a driver trait. Their drivers therefore ship now — `tests/integration/driver.rs` against a real temporary directory and `tests/e2e/driver.rs` against the built executable, both materialising the declared repository through `tests/support/sandbox.rs`, which removes itself on drop including when a scenario fails. What remains for this item is the classification and exemption work, not the plumbing.
      _Done 2026-09-07 at `1dd3e6a`._ `cargo test` passes at every level — **15** static checks, **2** unit, **8** integration, **2** E2E — and the static gate reports no missing, unused, ambiguous, placeholder, or invalidly exempted binding.
      **The classification is recorded in RHINO's `README.md`** as a table, one row per target, each classified by the strongest real boundary its setup, subject, or assertions touch rather than by the resources it is permitted to use. Two tests are split across boundaries on purpose and the table says which and why. Every scenario is bound at all three executing boundaries and **no layer declares an exemption** — stated there as a property of this corpus rather than as a default, because a sentence here is a claim that holds whether the tree is a map, a directory, or a process's working directory.
      **The gate was half a gate, and the missing half was the quiet one.** It proved every scenario is bound and no binding names a scenario that does not exist. It said nothing about the step vocabulary, which is the other direction of the same mapping. Five checks close it: every committed feature file contributes a scenario; every step resolves to **exactly one** vocabulary entry, so neither zero (a scenario written in a sentence no adapter knows, which passes by never running) nor two (where an unrelated edit to the array's order changes what the scenario means) can pass; every entry is used by some scenario and has exactly one dispatch arm; and no scenario or binding name is a placeholder.
      The first run of the new check **found a real gap immediately** — a vocabulary entry whose dispatch arm the scanner could not see because rustfmt had put the or-pattern's alternatives on separate lines. The scanner was wrong, not the code, but the failure is the point: the check discriminates.
      The unit adapter's `every_corpus_step_is_in_the_vocabulary` is **removed** rather than kept alongside: the new check subsumes it and adds ambiguity, and one claim belongs in one place.
      **Proof, stated as the mutation result:** six mutants — a scenario-less feature file, an unknown sentence, two entries matching one sentence, an unused entry, an entry with no arm, and a `TODO` scenario name — all killed.

- [x] `[AI] [AC-03]` Input: the composed gates. Action: confirm `cargo xtask test-quick` runs the unit adapter and the static behaviour check only, and add `scheduled.yml` running every integration suite followed by the complete unfiltered E2E target. Outcome: the slow adapters are gated where BeaverNest's standard puts them, not in a hook. Outcome check: no Git hook and no quick gate executes an integration or E2E scenario. Proof: an exact search of the gate definition and hooks finds no integration or E2E invocation; the scheduled workflow runs both in order. _Done 2026-09-07 at `5674c0e`._
      **The confirmation the item asked for found a gap rather than confirming.** `test-quick` ran `fmt`, `clippy`, and the static behaviour check — and **never ran the unit adapter at all**. The gate contract names four steps and this had three, with the missing one the only executing test in it. `cargo xtask test-quick` now runs the unit adapter before the static check.
      `scheduled.yml` runs integration and then E2E, daily and unfiltered. The order is deliberate rather than alphabetical: a defect that exists on disk is cheaper to read at the integration boundary than through a process's exit code, so the boundary that can explain it reports first.
      **The outcome check is a test rather than a search, because the failure it guards against is one nobody reruns a search for.** A hook that got slower is a hook a maintainer starts passing `--no-verify` to, and by then the gate is decorative. `the_quick_gate_and_the_hooks_run_no_slow_adapter` reads `xtask/src/main.rs` and all three hooks and `ci.yml`, discards their comments — every one of those files discusses the policy in prose — and fails if what remains names a slow adapter. It carries a **positive control** for the reason recorded in `learnings.md`: a search that finds nothing and a search that read the wrong file return the same answer. A second check requires the scheduled run to hold both adapters, in that order, with no `--skip`, `--exact`, or `--test-threads` narrowing them.
      **Proof, stated as the mutation result:** six mutants — the quick gate running integration, the quick gate dropping the unit adapter, the pre-push hook running E2E, the scheduled run narrowing itself, the scheduled run reversing the order, and the scheduled run dropping E2E — all killed.
- [x] `[AI] [AC-03]` Input: the implementation. Action: bring line coverage over the validator modules to at least 99%, naming `main.rs` and the concrete filesystem adapter as the only exclusions in the repository `README.md` as the standard requires. Outcome: the single numeric gate, with its denominator exclusions declared where a reader will find them. Proof: `cargo llvm-cov` reports at or above 99%; `README.md` names both exclusions and why a unit test cannot reach them. _Done 2026-09-07 at `3a9688e`._ **99.48% over 1,937 lines, up from 88.63%.**
      **The gate did not exist, and the denominator was wrong.** Coverage was measured nowhere, and two modules a unit test may not touch were counted. Structure came first: `DiskTree` moved to `src/runtime/disk.rs` so the only module that talks to `std::fs` can be _named_, and the process entry — argument collection, standard input, the working directory, the two output streams — moved into `src/main.rs`, where it belongs and where it is already excluded. That move also removed the **second place exclusions were decided**: `run` no longer pre-applies the working directory's exclusion list, which is the shape that produced the `--root` defect two items above.
      **Then thirty-eight scenarios, and not one written to move a number.** Each states behaviour the corpus had not: a flag given without its value, an unknown option, an empty command line, a configuration with no schema comment and one that cannot be read, an unusable glob in each of the three places one can be declared, a map section ending at the next heading, a map entry reaching past a sibling, an excluded directory inside a mapped tree, a Mermaid label measured as it is _seen_ rather than as it is _written_, a fenced block in another language, a capability declaration nested inside a list, and eleven harness-contract shapes that were reachable and unasserted. The corpus is now **160 scenarios, 275 expanded**.
      **The corpus gained its first exemption, and it is a real one.** `MemoryTree` grew `mark_vanished`, because "not found" and "cannot be opened" are different answers: the first is a file that is gone, which is nobody's policy violation, and the second is a file that is there and must be failed closed on. A real directory cannot be made to drop a file between the walk and the read without a second process racing the one under inspection, so integration and E2E name that boundary and name the alternative proof rather than pretending to cover it. It also means the exemption machinery is exercised by something rather than only asserted about.
      `Report::category` was **deleted rather than covered**: nothing asked it.
      **Two lines remain uncovered by choice and are worth naming:** the `\n` and `\r` arms of the JSON string writer. Every message the tool produces is single-line, so no finding can carry either. They stay because a JSON writer missing an escape is a defect waiting for the first multi-line message, and the honest record is that they are correct and unreachable rather than covered.
      **Proof that the threshold bites:** `cargo xtask test-quick` runs `cargo llvm-cov --test unit --fail-under-lines 99` in the _same_ execution as the tests — one run, because two can disagree and the number that gates has to be the number the passing run produced — and setting the threshold to `100` fails the gate. `README.md` carries the exclusion table with the reason for each.
- [x] **Blocking checkpoint — Phase 3:** `[AI] [AC-01, AC-02, AC-03]` Confirm every scenario green at every applicable level, static coverage clean, line coverage at or above 99%, clippy and `cargo deny` clean, no repository-specific literal in `src/`, and boundary policies enforced. **Proof:** rhino `6835bb1`. Corpus **162 scenarios, 280 expanded**, green at every applicable boundary: unit 1 passed (280/280 expanded), integration 8 passed, e2e 2 passed. Static coverage clean — `cargo test --test coverage`, 17 checks, including both directions of the corpus-to-binding mapping and the gate-composition checks. Line coverage **99.48%** (1,937 lines, 10 missed), gated at 99 by `cargo xtask test-quick`, with `src/main.rs` and `src/runtime/disk.rs` the two declared exclusions and the `\n`/`\r` arms of the JSON string writer the two deliberate misses. `cargo fmt --all --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo deny check` reports `advisories ok, bans ok, licenses ok, sources ok`. A fixed-string scan of `src/` for repository-specific literals leaves exactly two constants, both tool behaviour rather than a consumer's layout: `SKILL.md` in `src/harness.rs` and `README.md` in `src/governance/directory_map.rs`. Boundary policies are enforced as tests rather than prose — seven at integration (no network including loopback, no dependency that could reach one, no child process, no `unsafe`, no write to the inspected repository, a path leaving the root refused, a symbolic link out of the root not followed) and one at E2E (no write to the inspected repository, where the repository is also the process's working directory). One exemption is declared, at integration and E2E, for the vanishing-file scenario; the unit adapter proves it and `Sandbox::build` now refuses the precondition outright. **Mandated Gherkin implementation review:** run by four agents over the scope changed since `6e16d6e` — 127 expanded scenarios, 381 rows, 338 PASS / 2 EXEMPT / 41 FAIL. Both exemption rows upheld on independent review. All 41 remediated in rhino `d3064a8` and `6835bb1`; 17 mutants, two full passes, all killed.

## Phase 4 — First Consumer: RHINO Itself

RHINO adopts RHINO before BeaverNest does. The purpose is generalization pressure at the earliest point it is cheap: a repository with no `repo-governance/` tree, no `plans/` tree, and no multi-harness roster will break any assumption the port inherited from BeaverNest, and it will break it while the schema is still soft.

- [x] `[AI] [AC-10]` Input: the implemented binary and HIPPO's documentation tree as the reference model. Action: write `README.md` in HIPPO's section order, `docs/README.md` with the category table, the four Diátaxis category READMEs with their page lists and next-steps blocks, and every page named in the documentation table — executing every command shown and pasting real output. Outcome: a consumer can learn, do, look up, and understand without reading source. Outcome check: no page contradicts `specs/`. Proof: every `console` transcript reproduced against the built binary, and each page assigned to exactly one category. **Proof:** `README.md` rewritten in HIPPO's section order (title, highlights, why, install, quick start, how it works, documentation, project status, OSE, development, license) plus `docs/README.md`, the four category READMEs, two tutorials, five how-to guides, five reference pages, and four explanation pages — twenty-one files, each in exactly one category. Every `console` transcript was reproduced against the built binary. Six were wrong and were corrected: `cli.md`'s `--help` block was truncated before its Options and Exit-codes sections; `cli.md` carried `checked 2 directories`, `checked 11 links`, and `README.md: 682 words` from before the documentation tree existed (now 7, 109, and 1593); `wire-rhino-into-your-gates.md` invented the message `does-not-exist.md does not resolve inside the repository` (the real one is `` `does-not-exist.md` does not exist ``, at `README.md:5` over 5 links) and now also shows the aggregate script exit; `configuration.md` claimed `line 43` where reproduction gives `line 31`, the `harnesses:` line rather than a section header. The `scripts/hygiene.sh` loop printed in that guide was executed end to end in a sandbox — exit 0 clean, exit 1 with the deliberate break. Nothing in the tree contradicts `specs/`.
- [x] `[AI] [AC-08, AC-10]` Input: the release history. Action: write `CHANGELOG.md` with the Keep-a-Changelog headings, the Semantic Versioning statement, the release-immutability statement, and link definitions. Outcome: a consumer can see what changed between pins. Proof: the `v0.1.0` entry describes consumer-visible behaviour, not commits. **Proof:** `CHANGELOG.md` carries the Keep-a-Changelog heading structure, the SemVer statement, the release-immutability statement, and a link definition. The single entry is `## [Unreleased]` rather than `## [v0.1.0]` because no tag is published — Phase 6 promotes it. Its twelve bullets describe commands, the configuration contract, each validator's rule, the exit contract, the always-reported inspected count, narrowing, JSON output, and the read-only guarantee: consumer-visible behaviour, no commit list. Writing it surfaced one false claim I had to correct against the binary — `--json` is accepted by `version` alone, not by every command.
- [x] `[AI] [AC-01, AC-02, AC-10]` Input: the frozen schema and RHINO's own tree. Action: write upstream `repo-config.yml` describing RHINO — word-budget surfaces for `AGENTS.md` and `README.md`, mapped trees `docs/` and `specs/`, Mermaid limits, scan exclusions, and an explicit `harness-parity` declaring `AGENTS.md`, `CLAUDE.md`, and an empty harness roster. Outcome: a second repository is describable in the schema, and the degenerate roster is declared rather than omitted. Proof: `rhino repo-config validate` passes; no section is left implicit. **Proof:** `repo-config.yml` at the RHINO root declares word-budget surfaces for `AGENTS.md` (fail 1200, warn 1000) and `README.md` (fail 1600, warn 1400), mapped trees `docs` and `specs`, Mermaid limits 32/24 with the Okabe-Ito palette, scan exclusions `.git`/`node_modules`/`target`, and a `harness-parity` block naming `AGENTS.md`, `CLAUDE.md`, and `harnesses: []` with a comment recording that the empty roster is declared rather than omitted. `rhino repo-config validate` -> `checked 1 configuration file, no findings`. Every section of the schema appears; none is implicit.
- [x] `[AI] [AC-01, AC-02]` Input: the freshly built binary and that configuration. Action: run every validator against the RHINO repository and record each finding. Outcome: the first evidence of what the tool assumes. Proof: the recorded finding list, separated into genuine RHINO defects and BeaverNest-shaped assumptions leaking through the tool. **Proof:** Findings recorded in `local-tmp` and reproduced here. Leaked assumption L1: `harness-parity.required-mcp` was mandatory even with an empty roster, so RHINO could not describe itself (`missing field 'required-mcp' at line 37 column 3`). Leaked assumption L2: `unexpected-instruction-source` fired on `docs/tutorials/reconcile-your-first-harness-contract.md`, because the page shows `@AGENTS.md` inside a fenced example — the tool could not document its own adapter rule without accusing the documentation. Genuine RHINO defects: D1 `specs/architecture.md:32` filled with `#808080`, a grey outside the accessible family, carrying white text; D2 `specs/architecture.md:56` filled with `#CA9161`, which belongs to the same Okabe-Ito family and was simply absent from the declared palette. D1 was fixed in the diagram, D2 in the configuration. Clean on the first run: word-budget 2 files, directory-map 2 directories, internal-link 11 links, harness-parity 0 harnesses.
- [x] `[AI] [AC-01, AC-02]` **RED then GREEN:** Input: each leaked assumption from the previous task. Action: add a scenario to the corpus that fails because of it, then fix the tool so it passes — never by adding a RHINO-shaped default in place of a BeaverNest-shaped one. Outcome: generalization is proved by a second real repository rather than asserted. Proof: RED shows each new scenario failing; GREEN shows the full corpus passing and the RHINO tree clean. **Proof:** L1: scenario `An empty harness roster alongside a required MCP server is refused` plus the `harness-parity.required-mcp` row of `A declared harness roster requires the keys that reconcile it`; fixed by making `required_mcp` an `Option` and folding it into the same symmetric rule that already governed `skills-root` and `agents-root` — no RHINO-shaped default was introduced, the key simply follows the roster in both directions. L2: scenario `A documentation page showing the import in a fenced example is not a source`, RED reported `docs/adapters.md: unexpected-instruction-source` at the unit boundary; GREEN routes the Markdown case through the existing `markdown::prose_lines`, leaving non-Markdown files on the whole-text check. Bound at all three executing boundaries and registered in all three registries. Two mutants, each killed by a different scenario: dropping the prose filter kills the new scenario, and making Markdown never count as an import kills `With no adapter declared, no file may import the canonical instructions` — so the new scenario did not make the rule vacuous. Corpus 163 scenarios, 283 expanded; unit 1, integration 8, e2e 2, coverage 17 all pass; line coverage 99.49% over 1,943 lines; `cargo xtask test-quick` exits 0. All six validators clean against RHINO: repo-config 1, word-budget 2 files, directory-map 7 directories, internal-link 109 links, mermaid 3 diagrams, harness-parity 0 harnesses.
- [x] `[AI] [AC-10]` Input: the passing self-validation. Action: wire it into `ci.yml` and the `xtask test-quick` gate so the binary runs against its own repository on every push. Outcome: a permanent second corpus, not a one-time exercise. Proof: a deliberate broken link and an over-budget `AGENTS.md`, each introduced in a scratch commit, fail CI; both are reverted. **Proof:** `cargo xtask self-validate` runs all six validators against RHINO with the binary RHINO builds, and `test-quick` calls it last -- after the corpus, so a repository that is out of date cannot mask a product that is broken. `ci.yml` names it as its own step so a failing repository is legible in the job list. Proved on a scratch branch that was never pushed and has been deleted: a broken link in `README.md` gave `[internal-link] checked 110 links, 1 finding` / `README.md:269: \`docs/nowhere.md\` does not exist`and`self-validate`exit 1; twelve hundred filler words appended to`AGENTS.md`gave`AGENTS.md: is longer than its declared word budget (words=1923, limit=1200)`and exit 1.`cargo xtask test-quick`exited 1 for each and returned to 0 after both were reverted. Deviation from the literal wording, recorded: the breakages were committed to a scratch branch and run through the exact command CI runs, rather than pushed, because pushing a deliberately broken commit to a public`main` is not a thing to do to prove a gate works.
- [x] **Blocking checkpoint — Phase 4:** `[AI] [AC-01, AC-02, AC-10]` Confirm RHINO's own tree validates clean, every leaked assumption has a scenario and a fix, no RHINO-specific default replaced a BeaverNest-specific one, self-validation runs in CI, and the schema survived a repository that is not BeaverNest. **Proof:**

**RHINO's own tree validates clean.** All six validators, against the repository, with the binary it builds: `[repo-config] checked 1 configuration file, no findings`; `[word-budget] checked 2 files, no findings` scanning `AGENTS.md` and `README.md`; `[directory-map] checked 7 directories, no findings`; `[internal-link] checked 109 links, no findings`; `[mermaid] checked 3 diagrams, no findings`; `[harness-parity] checked 0 harnesses, no findings` with `canon 0 harnesses, 0 skills, 0 agents, 0 reconciled capability declarations`. Exit 0 for each.

**Every leaked assumption has a scenario and a fix.** Two were found, and the second only because the documentation was written. L1 -- `harness-parity.required-mcp` was mandatory alongside an empty roster, so RHINO could not describe itself; scenario `An empty harness roster alongside a required MCP server is refused` plus a new row in `A declared harness roster requires the keys that reconcile it`. L2 -- a page showing `@AGENTS.md` in a fenced example was reported as a competing instruction source; scenario `A documentation page showing the import in a fenced example is not a source`. The mandated Gherkin implementation review over L2's changed scope then produced three further scenarios: `A documentation page showing the import in an inline code span is not a source` (the fix had closed only half the false positive), `An unclosed fence does not hide a competing instruction source` (the fix had opened a false negative -- one stray opener hid the rest of a document from a check a harness reading the file as text would still trip), and `A code span containing a backtick is read to its matching close`.

**No RHINO-specific default replaced a BeaverNest-specific one.** L1's fix made `required_mcp` an `Option` and folded it into the symmetric rule already governing `skills-root` and `agents-root` -- the key follows the roster in both directions, and no value was introduced. L2's fix routes the Markdown case through the `markdown::prose_lines` reader that already existed, and grep confirms `src/` still names no harness, no path, and no limit: every value enforced arrives from `repo-config.yml`.

**Self-validation runs in CI.** `cargo xtask self-validate` is a named `ci.yml` step and the last step of `cargo xtask test-quick`, which the pre-push hook runs. Proved failing and recovering, on a scratch branch, for both a broken link and an over-budget instruction file.

**The schema survived a repository that is not BeaverNest.** RHINO has no `repo-governance/` tree, no `plans/` tree, no skills, no agents, and an empty harness roster, and it is fully describable in the frozen schema with no section left implicit. The degenerate roster is declared rather than omitted, and the difference between "none" and "not configured" stays visible.

**Gherkin implementation review.** Changed scope reviewed one-by-one by an independent agent against `repo-governance/workflows/gherkin-implementation-review.md`: 1 scenario x 3 required adapters = 3 rows, all PASS, no exemption declared. One PARTIAL was raised and resolved: the sibling scenario's fixture was a one-line file, which pinned neither which prose line the Markdown branch reads nor how it matches within one, so `.take(1)` and an exact-line comparison both survived. The fixture now puts the route mid-sentence on a later line and both mutants die. The review also produced the false negative and the remaining false positive recorded above, each now a scenario. Corpus totals after remediation: 167 vocabulary entries, 168 scenario headers (25 of them outlines) expanding to 287 executable scenarios.

**Gates.** `cargo xtask test-quick` exit 0 -- fmt, clippy `-D warnings`, unit adapter with coverage, static coverage check, self-validation. Unit 1 pass (287 of 287 expanded scenarios), integration 8 pass, e2e 2 pass, coverage 17 pass. Line coverage 99.49% over 1,974 lines against a 99% floor, with `src/main.rs` and `src/runtime/disk.rs` the only declared exclusions. Six mutants over the new Markdown reading, two full passes, all killed.

**Commits.** `06d7abf` the first fenced-example fix, `19a58d3` the missing MIT `LICENSE` the README already linked, `fe92642` the twenty-one-page documentation tree and README rewrite, `0933046` the changelog, `80a233b` the CI wiring, `d9ef52e` the review remediation. All pushed to `wahidyankf/rhino` `main`.

**Scope.** Only `rhino` and this plan directory were touched. HIPPO and grind-in-public were read for reference and not modified.

## Phase 5 — Generalization Against Every Target Tree

Nothing is tagged until the schema has met all four trees. These are read-only dry runs with a locally built binary: no bootstrap, no lock, no CI wiring, and no commits in the sibling repositories.

- [x] `[AI] [AC-01, AC-02]` Input: grind-in-public's `apps/badakmini-cli` and this repository's. Action: compare the two implementations command by command and rule by rule, recording where the independent Go implementation and the F# one already disagree. Outcome: the required configurability, established empirically rather than predicted. Proof: a recorded difference table covering command grammar, word-budget scope, mapped trees, parity vocabulary, and diagram coverage. **Proof:** Read command by command and rule by rule -- Go 7,264 lines across `internal/cli`, `internal/governance`, `internal/markdownlinks`, `internal/parity`, `internal/rulechange`; F# 5,164 lines across `Cli.fs`, `Governance.fs`, `HarnessContract.fs`. Twenty-eight compared rows, of which **two agree**: the word limit `750` and the canonical instruction file `AGENTS.md`. Everything else differs.

**Command grammar.** The two trees agree on no single command path. Word limits are `harness instruction-size validate` against `governance word-budget validate`; links are `harness markdown-links validate` against `md links validate`; parity is `harness capability-parity validate` against `governance harness-contract validate`. Directory maps, Mermaid, and word-count exist only in F#; `rule-change validate` and `rule-change hook` exist only in Go. Neither has a command that checks its own configuration, because neither has one.

**Word-budget scope.** Same limit, nothing else shared. Go measures `AGENTS.md`, `CLAUDE.md`, `RTK.md`, `repo-governance/**/*.md`, and the `README.md` index inside `.agents`, `.claude`, `.codex`, and `.opencode`, skipping `node_modules` and dotted subtrees; F# measures `AGENTS.md` and `repo-governance/**/*.md` and nothing else. Go treats a missing `AGENTS.md`, `CLAUDE.md`, `RTK.md`, or `repo-governance/` as a hard error; F# lets it contribute nothing. The two disagree about whether `CLAUDE.md` -- an adapter whose whole content is `@AGENTS.md`, four words -- belongs in a word budget at all. This is the strongest single argument for the ordered `glob`/`fail`/`warn` surface list: neither implementation can express the other's scope, and a one-number limit carries no `warn` tier.

**Mapped trees.** Go does not implement directory maps. F# checks exactly one tree, `repo-governance`, hard-coded. RHINO's own configuration maps `docs` and `specs`, in a repository with no `repo-governance` at all. Three repositories, three answers, no overlap.

**Parity vocabulary.** Both hard-code every harness by name, so a fourth harness is a code change in both -- the assumption the data roster removes. Skill wrappers live at `.claude/skills/<n>/SKILL.md` in Go and `.claude/commands/<n>.md` in F#: the same concept in two places. The capability vocabulary is five interpreted names in Go (`repository-read`, `repository-write`, `shell`, `nested-agent`, `approval-or-read-only-shell`) against a closed seven in F# (`repository-read`, `repository-write`, `web-search`, `web-fetch`, `shell`, `nested-agent`, `nx-mcp`); constraints are unvalidated in Go and a closed two in F#. The required MCP server exists in F# only -- `nx-mcp`, vector `npx nx mcp`, reconciled across `.codex/config.toml`, `.mcp.json`, and `opencode.json` -- and Go has none. Go additionally prohibits skill copies under `.codex/skills` and `.opencode/skills`, checks `opencode.json`'s `instructions` is empty, and treats `.claude/rules/**` as an instruction source; F# does none of those. Even the skipped-tree lists differ: `.git .nx node_modules local-tmp generated-reports` against `.git .nx node_modules bin obj _build deps playwright-report test-results worktrees`.

**Diagram coverage.** Mermaid is F#-only: node 32, edge 24, fills `#0173B2 #DE8F05 #029E73 #CC78BC #CA9161 #808080`, edges those plus `#000000`, text `#000000 #FFFFFF`, over eight compatible headers. grind-in-public and HIPPO use ASCII art, so the correct answer for two of the four trees is a clean zero -- which is the evidence that the zero-diagram scenario is a real repository shape rather than a hypothetical. Note that the F# palette carries `#808080`, the plain grey RHINO's own Phase 4 run rejected as an accessibility defect; a repository may still declare it, RHINO simply ships none.

**Link checking.** Go selects files from Git's tracked tree via `git ls-files -z`; F# walks the filesystem minus a hard-coded exclusion list. Go validates `#fragment` targets against the target document's headings, resolves directory links to `README.md`, handles full, collapsed, and shortcut reference links, ignores links inside inline backticks, and re-checks containment after resolving symlinks. F# does none of that.

**Three differences are recorded as decisions rather than configuration.** (1) `rule-change` is out of scope: it is not a hygiene check -- its input is Git's staging area or a harness JSON payload rather than the tree, it prints a workflow notice rather than a finding, and it has no policy to violate. (2) Git-tracked file selection is impossible: RHINO spawns no process, and a boundary-policy test holds that. `scan.exclude-directories` is the declared equivalent. (3) Link fragment validation is a genuine Go-only capability with no RHINO equivalent; it is a new rule rather than a configurable value, so it is a follow-on rather than something smuggled into this extraction.

- [x] `[AI] [AC-01, AC-02]` Input: the HIPPO and grind-in-public trees and their written rules. Action: draft a candidate `repo-config.yml` for each without committing it, taking every value from that repository's current validator or rules and never inventing one. Outcome: two more repositories expressed in the schema. Proof: `rhino repo-config validate` passes for each draft; every value traces to a source in its own repository. **Proof:** Three drafts written, and a fourth for BeaverNest so the triage below has all four trees. All exit `0` under `rhino repo-config validate`. Held in the session scratch directory, uncommitted; the sibling repositories were read through `git archive` snapshots and never written to.

  **HIPPO** is an empty roster: no `CLAUDE.md`, no `.agents/`, no `.claude/`, no adapter directory of any kind. That is the whole `harness-parity` section apart from the instruction and the prohibitions, and it is the shape that makes the optional `instruction-adapter` a real repository rather than a hypothetical one.

  **grind-in-public** transcribes `internal/parity/check.go`. Its `codex` adapter translates no capability at all -- the sandbox is fixed, the field set is closed, and that is the entire contract; its `claude` adapter owes `Read` to `repository-read` rather than granting it always; its skill wrappers are directories at `.claude/skills/{name}/SKILL.md` where BeaverNest's are files at `.claude/commands/{name}.md`. Its declared vocabulary is the union of what its canon actually uses -- six capabilities and two constraints -- rather than the five names its Go checker interprets, because the Go checker validates no vocabulary and RHINO does: declaring only the interpreted five would make RHINO report `unknown-capability` on `solution-output`, a fault that repository's own gate never raised.

  **BeaverNest** transcribes `HarnessContract.fs` and `Governance.fs`, including the `#808080` grey RHINO's own Phase 4 run rejected. A repository may declare it; RHINO ships none.

  Producing them exposed leak **L10**, recorded in the triage below.

- [x] `[AI] [AC-01, AC-02]` **RED then GREEN:** Input: HIPPO's tree, which has no `CLAUDE.md` and no harness adapters. Action: add the scenario for an omitted `instruction-adapter`, prove it red, then make that field optional while keeping the competing-instruction-source prohibition in full force. Proof: RED shows the scenario failing; GREEN shows the corpus passing and HIPPO's tree validating. **Proof:** No RED was manufactured, because there was nothing red. The scenario `A declared instruction adapter is optional` was written in Phase 3 and the field has been `Option<String>` since. Faking a failure here would have meant breaking working code to watch it break. What was verified instead is the claim the task exists to establish: HIPPO's tree -- no `CLAUDE.md`, no `.agents/`, no `.claude/`, no adapter directory of any kind -- runs `harness parity validate` to exit `0`, and the prohibition still holds, since `**/CLAUDE.md` is among the sources its draft forbids.
- [x] `[AI] [AC-01, AC-02]` **RED then GREEN:** Input: grind-in-public and HIPPO, which use ASCII art rather than Mermaid and therefore contain zero diagrams. Action: add the zero-diagram scenario, prove it red, then make a zero-match scan report a clean, explicit zero — never a warning, never an unexamined success, and never an inference that the walk failed. Outcome: a repository whose visualization convention is not Mermaid is a first-class citizen, not a degraded one. Proof: RED shows the scenario failing; GREEN shows both trees reporting zero diagrams checked and exiting clean. **Proof:** Again no RED, and again recorded rather than manufactured: `A repository with no diagrams reports a clean explicit zero` has been in the corpus since Phase 3, and its `Then` asserts the explicit zero rather than the absence of a finding. Both trees report `[mermaid] checked 0 diagrams, no findings` and exit `0`. A third zero turned up unlooked for and is worth the same treatment: HIPPO governs no word surface, so `[word-budget] checked 0 files, no findings`, and grind-in-public maps no directory tree, so `[directory-map] checked 0 directories, no findings`. Three commands, three repositories, three explicit zeroes -- each a repository shape rather than a degraded run.
- [x] `[AI] [AC-01, AC-02]` Input: each draft configuration. Action: run every validator against all four trees, then triage findings into genuine repository defects, legitimate policy differences, and BeaverNest-shaped assumptions leaking through the tool. Outcome: the assumption set that four trees expose but one cannot. Proof: the recorded triage, with every leak given a scenario and a fix that adds no new repository-shaped default. **Proof:** Six commands against four trees, twenty-four runs, all exit `0` on the final pass. Sibling trees were read through `git archive` snapshots; nothing was written to any of them.

  | Tree            | repo-config | word-budget | directory-map | internal-link | mermaid        | harness-parity                                |
  | --------------- | ----------- | ----------- | ------------- | ------------- | -------------- | --------------------------------------------- |
  | rhino           | 1 file      | 2 files     | 7 dirs        | 109 links     | 3 diagrams     | 0 harnesses                                   |
  | beaver-nest     | 1 file      | 57 files    | 6 dirs        | 789 links     | 33 diagrams    | 3 harnesses, 7 skills, 2 agents, 3 reconciled |
  | HIPPO           | 1 file      | **0 files** | 2 dirs        | 152 links     | **0 diagrams** | 0 harnesses                                   |
  | grind-in-public | 1 file      | 124 files   | **0 dirs**    | 895 links     | **0 diagrams** | 3 harnesses, 1 skill, 2 agents, 0 reconciled  |

  **Thirteen leaks, not the seven this task anticipated.** Every one is a rule RHINO held that no repository had asked it to hold, and every one was found by a tree rather than by reading the code. Each has a scenario, a fix, and mutants compiled and run twice.

  | #        | The rule RHINO held                                     | What it did                                                                                                                | Fix                                                         |
  | -------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
  | L3       | `required-mcp` is mandatory                             | A repository with no capability server could not be described                                                              | Pairs with a per-harness `capability`; neither is mandatory |
  | L4/L5/L6 | An adapter repeats the canonical declaration and body   | Matched **neither** production repository                                                                                  | An adapter is a route plus a permission translation         |
  | L7       | A file that is not text is a refusal                    | Any repository holding a logo could not be inspected                                                                       | Not text is skipped; unreadable still fails closed          |
  | L8       | A `README.md` in a canonical root is a declaration      | Every directory index was reported invalid                                                                                 | An index is not a declaration                               |
  | L9       | Any file may be an instruction source by content        | RHINO's own source accused itself                                                                                          | Only Markdown can be one by content                         |
  | L10      | The canon writes `capabilities:`/`denied:`              | **False clean.** Both repositories write `requires:`/`denies:`, unknown fields are ignored, so every translation was inert | The three field names are declared                          |
  | L11      | A word is a run of letters and digits                   | 749 words by one repository's rule, 905 by RHINO's, against a limit of 750                                                 | `count:` is declared                                        |
  | L12      | A closed adapter permits only identity and fixed fields | Reported the field its own translation requires                                                                            | A closed set permits what the contract names                |
  | L13      | A route is compared byte for byte                       | Every formatter-wrapped wrapper read as drift                                                                              | Compared as a sentence                                      |
  | L14      | Link syntax in backticks is a link                      | Prose explaining a convention reported for links it never made                                                             | A quotation is not a link                                   |
  | L15      | A capability writes `command` beside `args`             | Rejected a server naming exactly the required command line                                                                 | The flattened vector is the invariant                       |

  **Genuine repository defects: none.** Every finding was either a leak above or a defect in a draft I wrote.

  **Draft defects, mine, corrected:** grind-in-public's draft mapped four directory trees on the grounds that its tree carried them — 54 of its 75 directories do not, and its validator implements no such check, so the honest declaration is `trees: []`. Both sibling drafts wrote their skill route with `{name}` where the schema substitutes `{path}`.

  **Legitimate policy differences, expressed rather than resolved:** the word-counting rule (L11); grind-in-public's declared capability vocabulary, which is the union its canon uses rather than the five names its Go interprets, because that checker validates no vocabulary and RHINO does; and BeaverNest's `#808080`, the plain grey RHINO's own Phase 4 run rejected — a repository may declare it, RHINO ships none.

  **One position taken rather than made configurable.** L13 folds whitespace in a route on both sides, and this is behaviour, not a knob. The alternative was a finding whose only remedy is turning your Markdown formatter off. It is additive: BeaverNest's wrappers sit on one line and were passing already, so nothing it checked stopped being checked.

  **Two coverage gaps recorded, not closed.** RHINO does not check that a canonical _skill_ carries a fixed field, and it fixes the canonical agent file extension at `.md` and the skill file at `SKILL.md`. All four trees use exactly those, so no gate diverges today; they are noted here so Phase 8 and Phase 10 do not discover them as surprises.

- [x] `[AI] [AC-02]` Input: the hardened schema. Action: re-run the RHINO and BeaverNest trees against the changed binary to confirm no earlier repository regressed. Outcome: hardening is additive. Proof: both trees still validate as they did before the change. **Proof:** Both clean on the final sweep, and the quick gate runs six of those validators against RHINO itself on every push. One figure moved and moved the right way: BeaverNest reconciles **3** capability declarations where it reconciled 2, because L15 means the third is now actually compared rather than reported as divergent. Nothing that was checked before is unchecked now — every fix either widened what a repository may declare or removed an accusation RHINO had no grounds for.
- [x] **Blocking checkpoint — Phase 5:** `[AI] [AC-01, AC-02]` Confirm all four trees are describable, every leaked assumption has a scenario and a fix, no repository-shaped default was introduced, the absent-adapter and zero-diagram cases are covered, earlier trees have not regressed, and no sibling repository has been modified. **Proof:**

  **All four trees describable.** Twenty-four validator runs, all exit `0`. Table above.

  **Every leak has a scenario and a fix.** Thirteen, each committed separately with mutants compiled and run twice: `5d6be98` L3, `7ac30e6` L9, `1551d99` L8, `33bbaea` L4/L5/L6, `9d31457` L10, `db6ef60` L11/L12/L13, `7701945` L14, `f4447f6` L15, `42a2f2b` the review remediation. Thirty-nine mutants in this session's batteries (18 + 7 + 6 + 2 + 4 + 2), on top of those run for L3, L7, L8 and L9 earlier; every one killed by unit, integration, and E2E on two separate runs.

  **No repository-shaped default introduced.** Every fix either moved a value into configuration (`declaration`, `count`) or removed an accusation RHINO had no grounds for (`closed`, route folding, quoted links, executable vectors). Three keys became required — `canonical.declaration`, `word-budget.count`, and each root's route — and none carries a default; a repository that omits one is refused by name and line, never given RHINO's answer. The one position taken rather than made configurable is recorded in the triage with its reason.

  **Absent-adapter and zero-diagram covered.** Both, plus a third and fourth explicit zero neither task anticipated — recorded honestly as already-satisfied rather than as manufactured REDs.

  **No regression.** RHINO and BeaverNest both clean; one count improved (2 → 3 reconciled) because L15 means a declaration that was reported divergent is now actually compared.

  **No sibling repository modified.** HIPPO, grind-in-public, and BeaverNest were read through `git archive` snapshots under the session scratch directory. `git status` in beaver-nest shows only this plan document; nothing was written to any sibling worktree.

  **Gherkin implementation review.** Forty expanded scenarios added or changed this phase, across three required adapters — 120 rows, every one `PASS`, no exemptions. The six scenarios whose `Then` is a clean run were the ones worth doubting, since an inert fixture produces the same result; each was traced to the one mutant that kills it and no other, which proves the `Given` reaches the production path rather than merely differing from the base. That trace found the two `FAIL`s this phase: the limits field of the declaration shape was pinned by nothing, and the renaming Outline's clean arm was killed by no mutant. Both were remediated in `42a2f2b` and re-verified. **Deviation recorded:** the workflow says to delegate the review to an agent. This session forbids spawning agents, so the one-by-one inspection was performed directly — counts, greps, and a green test run were not substituted for it, which is what the clause protects.

  **Gates.** `cargo xtask test-quick` clean at 99.36% line coverage; the integration and E2E corpora pass unfiltered. All local compute through `./hippo run --class ephemeral`.

## Phase 6 — Release Pipeline and First Tag

- [x] `[AI] [AC-08]` **RED:** Input: the packaging requirements. Action: write the artifact-verification tests — four platform archives present, `checksums.txt` matching, each executable's `version --json` matching the tag and commit exactly, and each stripped size within the declared ceiling — against an unimplemented `xtask dist`. Proof: `cargo test --package xtask` fails on every artifact assertion. **Proof:** `xtask/tests/artifacts.rs` holds four assertions and reads `dist/` rather than building anything — a test that rebuilt the artifact would be checking a different binary from the one that ships. Against an empty `dist/` and an unimplemented `dist` task, all four fail: `dist/ holds no archive for any declared platform; run \`cargo xtask dist\``, the checksum read panicking on a missing `checksums.txt`, `dist/rhino is absent`, and the size read panicking on the same path. The four platform names live in the test rather than in the workflow, so a matrix that silently lost a leg fails a test instead of publishing three archives.
- [x] `[AI] [AC-08]` **GREEN:** Action: implement `cargo xtask dist`, including the size-optimized release profile (`opt-level = "z"`, fat LTO, one codegen unit, abort on panic, stripped symbols). Outcome: reproducible, verifiable assets from one command. Proof: a local `cargo xtask dist` for the host platform passes the verification tests; record the measured stripped size and set the ceiling from it. **Proof:** `cargo xtask dist` from a clean `dist/` prints `dist/rhino-aarch64-apple-darwin.tar.gz` and `stripped executable: 1499424 bytes`; `cargo test --package xtask` then reports `4 passed; 0 failed`. One platform per invocation, deliberately: the matrix builds each archive on a runner of that architecture rather than cross-compiling, so every published executable has actually started on the operating system it claims. The size-optimized profile was already in `Cargo.toml` from Phase 2 and was not re-added. Each assertion was then confirmed to bite by mutating the _artifact_ rather than the code — appending a byte to the archive produced `rhino-aarch64-apple-darwin.tar.gz does not match its recorded checksum`, and removing the executable produced ``dist/rhino is absent; run `cargo xtask dist` ``. One defect found and fixed while doing it: `env!("CARGO_PKG_VERSION")` inside the test crate resolves to xtask's own `0.0.0`, not the product's, so the test was comparing the executable against a number nothing ships; it now reads the root manifest. Committed at `d16fc0e`.
- [x] `[AI] [AC-08]` **REFACTOR:** Action: if the measured size exceeds expectations, apply the ordered options from the binary-size section — narrow `regex` features, then explicit parsing, then `clap`'s builder API — and re-measure. Outcome: a stated, held size budget. Proof: recorded before-and-after sizes, or a recorded decision that no reduction was needed. **Proof:** a recorded decision that no reduction was needed. 1,499,424 bytes stripped, 706.1 KiB gzipped, for a binary carrying `regex`, `serde`, `serde_json`, `toml`, `yaml_serde`, `globset`, `sha2`, and `unicode-segmentation`. None of the ordered options — narrowing `regex` features, explicit parsing, `clap`'s builder API — is warranted at that figure, and the last is moot since Phase 2 chose hand-rolled argument parsing over `clap` entirely. No documented size claim needed updating: an exact search of `README.md` and `docs/reference/` for a size figure found none. The budget is stated and held rather than merely measured. It was first written as one ceiling in `xtask/tests/artifacts.rs`, set from the only measurement then available; **the rehearsal tag proved that wrong on its first run** — both Linux legs failed it. The same source produces executables 43% apart, so the budget is now one ceiling per platform, each its own measurement plus about a fifth: `aarch64-apple-darwin` 1,499,424 under 1.75 MiB, `x86_64-apple-darwin` 1,759,232 under 2 MiB, `aarch64-unknown-linux-gnu` 1,905,472 under 2.25 MiB, `x86_64-unknown-linux-gnu` 2,140,760 under 2.5 MiB. Raising the single ceiling to clear the largest would have been the quick fix and the wrong one: it would leave the smallest platform free to grow by three quarters before anything noticed. A ceiling loose enough never to fire is a budget nobody holds. The assertion also stopped weighing only the host executable — identity has to be host-only, since that is the one that starts on the runner, but an archive opens anywhere — so the whole set is weighed wherever the suite runs, each archive in its own directory because every one of them holds a file called `rhino`. Rehearsed against four stand-in archives at the four CI sizes: all four report separately and correctly, and an archive one byte over its ceiling fails naming that platform. Committed at `d16fc0e` and `671af41`.
- [x] `[AI] [AC-08]` **Added during Phase 6 — the executable did not report the tag it ships under.** Writing the artifact verification surfaced it: `version --json` emitted `"version":"0.1.0"` while the tag is `v0.1.0`. AC-08 says each executable reports the tagged version, and [tech-docs/01](tech-docs/01-rhino-repository.md) froze the envelope as `{"schemaVersion":1,"version":"vX.Y.Z","commit":"<40 hex>"}`, so the binary was reporting something a consumer had to convert before comparing. Cargo's manifest cannot carry the `v`, so it is added at the one place that prints it. **This had to land before the tag:** after publication the envelope is a contract and changing it is a breaking release. **Proof:** RED — `And the reported version is spelled as its release tag` failed with ```0.1.0`is not spelled as a tag`` — then GREEN across all four adapters. Four mutants run twice with the build invalidated each time (uppercase`V`, a two-part version, a bare `v`, a non-numeric patch), all killed. The install how-to also described assets `cargo xtask dist`does not produce — a version inside the archive name and a`.sha256`beside each archive — corrected to the real names and the single`checksums.txt`, with `--ignore-missing`proved necessary by executing both forms against a four-line digest file. Committed at`b508a32`.

- [x] `[AI] [AC-03, AC-08]` **Added during Phase 6 — the mandated [Gherkin implementation review](../../../repo-governance/workflows/gherkin-implementation-review.md) over the changed scenario, and its remediation.** Three rows (one scenario × unit, integration, E2E), all `PASS` on binding validity: the `Given` is boundary-valid at each boundary, the E2E driver really spawns `CARGO_BIN_EXE_rhino`, the assertion parses observed stdout, and nothing flows from the production constant into the expected value. **But the review found a survivor the four spelling mutants could not reach.** `let version = "v9.9.9";` satisfies every rule the corpus stated and is still the wrong release: the scenario proved the version was _spelled_ like a tag and never that it was _the_ release. The only thing comparing it to anything was an xtask test that needs `dist/` to exist and is not in the quick gate. The review also noted that the "frozen" envelope was never pinned _as_ an envelope — every existing assertion was an order-independent substring search, so a reordered key or an added field passed unnoticed. **Proof:** two assertions close both. The first reads the version out of the product manifest's text — deliberately not `env!("CARGO_PKG_VERSION")`, which inside the test crate is the same constant `version()` prints, so comparing it would prove a build self-consistent and nothing else. The second parses the envelope by hand rather than through a JSON reader, since a reader is exactly what makes ordering invisible. Five mutants run twice with the build invalidated each time — wrong release, reordered keys, an extra field, a space after a colon, a dropped commit field — all killed, three of them by the new steps. One latent limit recorded in the code that decides it: a prerelease tag such as `v0.2.0-rc.1` fails the spelling predicate on purpose. Committed at `894b36c`.

- [x] `[AI] [AC-08]` Input: HIPPO's `release.yml` as reference. Action: write `release.yml` — tag trigger, peeled-tag-on-main ancestry verification, a native build matrix across macOS and Ubuntu on `amd64` and `arm64`, asset assembly, artifact verification, and `gh release create --verify-tag`. Outcome: a release cannot describe a commit that is not on `main`. Proof: a dry run on a throwaway pre-release tag publishes and verifies four archives plus checksums, after which that tag and release are deleted. **Proof:** two dry runs on throwaway prerelease tags, both now deleted along with their releases; `gh release list` and `git ls-remote --tags` return nothing, and no local tag remains. **The first rehearsal failed, which is the point of having run it.** Both Linux legs exceeded the single size ceiling set from the only measurement available when it was written — the fix is recorded in the REFACTOR row above. **The second rehearsal published and verified four archives plus `checksums.txt`**, marked prerelease, in 2 minutes 20 seconds wall clock: provenance 5s, four native build legs in parallel (the two Linux 37s and 39s, `macos-15` 56s, `macos-15-intel` 104s), publish 20s. The publishing job's own `cargo test --package xtask` reported `4 passed` with the matrix assertion made total by `RHINO_RELEASE_MATRIX=complete`. **Then verified as a consumer**, running exactly what [the install how-to](https://github.com/wahidyankf/rhino/blob/main/docs/how-to/install-a-pinned-release.md) tells a reader to run: `curl` the archive and `checksums.txt`, `shasum -a 256 --ignore-missing -c checksums.txt` → `rhino-aarch64-apple-darwin.tar.gz: OK`, extract, and `./rhino version --json` → `{"schemaVersion":1,"version":"v0.1.0","commit":"671af41ea4136a995180bce20c0d540205e780eb"}`, which is the tag's peeled commit exactly. The reported version is `v0.1.0` rather than `v0.1.0-rc.2` by design: a prerelease is a prerelease _of_ a version, the manifest declares `0.1.0`, and the workflow compares the tag's release core so a rehearsal needs no manifest edit. Four measurements the rehearsal produced, one per platform — stripped executable and gzipped archive: `aarch64-apple-darwin` 1,499,424 / 722,586; `x86_64-apple-darwin` 1,759,232 / 768,999; `aarch64-unknown-linux-gnu` 1,905,472 / 828,057; `x86_64-unknown-linux-gnu` 2,140,760 / 856,472. One design decision worth naming: `checksums.txt` is written by `cargo xtask checksums` with the `sha2` crate rather than by shelling out, because the matrix assembles it from four runners and the collecting job needs the same writer, not a second one spelled in shell — while the verification side still shells out to `sha256sum` or `shasum`, so the digest is checked against an implementation that is not the one that wrote it. Committed at `eafeac4` and `671af41`.
- [x] `[HUMAN] [AC-08]` Input: a green upstream `main`. Action: authorize and push the `v0.1.0` tag. Outcome: an immutable first release exists to pin. Proof: the published release with four archives, `checksums.txt`, and matching `version --json` output per platform. _This is `[HUMAN]` because publishing an immutable public artifact is an external, irreversible action._ **Proof:** authorized by the user in-session and pushed at their instruction. `v0.1.0` is published at <https://github.com/wahidyankf/rhino/releases/tag/v0.1.0>, an annotated tag peeling to `671af41ea4136a995180bce20c0d540205e780eb` on `main`, not marked prerelease, carrying four archives plus `checksums.txt`. The pipeline was green end to end in 2 minutes 29 seconds. **Verified as a consumer rather than by reading the release page:** `curl` the archive and `checksums.txt`, `shasum -a 256 --ignore-missing -c checksums.txt` → `rhino-aarch64-apple-darwin.tar.gz: OK`, extract, and `./rhino version --json` → `{"schemaVersion":1,"version":"v0.1.0","commit":"671af41ea4136a995180bce20c0d540205e780eb"}` — the tag's peeled commit exactly. The downloaded binary then ran `repo-config validate --root` against a real repository read-only and exited `0`. The tag has not been replaced and will not be.
- [x] `[AI] [AC-08, AC-12]` Input: the published `v0.1.0` assets. Action: record the distribution end result for all four platforms — stripped executable size, downloaded archive size, and the release pipeline's own wall-clock. Outcome: the artifact half of the distribution claim has numbers. Proof: the four-platform table in `learnings.md`. **Proof:** the four-platform table in `learnings.md`, every figure read off the published release or its build logs rather than computed from one platform and scaled. Stripped executable and gzipped archive: `aarch64-apple-darwin` 1,499,424 / 722,586; `x86_64-apple-darwin` 1,759,232 / 769,000; `aarch64-unknown-linux-gnu` 1,905,472 / 828,057; `x86_64-unknown-linux-gnu` 2,140,760 / 856,473. `checksums.txt` is 408 bytes and covers all four, so a consumer fetches 723–837 KiB. Pipeline wall clock 2 minutes 29 seconds — provenance 6s, four native legs in parallel (38s, 36s, 42s, and 109s for `macos-15-intel`, which is the critical path by two and a half times, in allocation rather than compilation), publish 21s. Two findings recorded alongside the table: the executables vary 43% across platforms while the archives vary only 16%, because gzip lands near 45% on all four — so the download cost is much flatter than the build output suggests; and a budget derived from one instance of a four-instance population is a guess with a number in it, which is what the rehearsal tag existed to catch.
- [x] **Blocking checkpoint — Phase 6:** `[AI] [AC-08, AC-12]` Confirm four platform archives, matching checksums, correct embedded identity per platform, sizes within budget and recorded, the throwaway tag removed, and `v0.1.0` published and not replaced. **Proof:** every claim re-verified against the published release rather than carried forward from the run that produced it. All five assets downloaded fresh with `gh release download`.

  | check                     | result                                                                                                                                                                                              |
  | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Four platform archives    | `rhino-{aarch64,x86_64}-apple-darwin.tar.gz`, `rhino-{aarch64,x86_64}-unknown-linux-gnu.tar.gz`, plus `checksums.txt` (408 bytes)                                                                   |
  | Matching checksums        | `shasum -a 256 -c checksums.txt` → four `OK`, exit `0`. Not `--ignore-missing`: all four were present, so every recorded digest was checked                                                         |
  | Correct embedded identity | `671af41…` and `v0.1.0` found as literals in all four executables — read out of the binaries this host cannot run, so the claim rests on the artifacts rather than on the workflow's word           |
  | Correct architecture      | `file` reports Mach-O arm64, Mach-O x86_64, ELF aarch64, ELF x86-64 — each archive holds a binary for the platform it is named for                                                                  |
  | Sizes within budget       | 1,499,424 / 1,759,232 / 1,905,472 / 2,140,760, each under its own ceiling of 1.75 / 2 / 2.25 / 2.5 MiB                                                                                              |
  | Sizes recorded            | the four-platform table in `learnings.md`, with archive sizes and the pipeline's wall clock                                                                                                         |
  | Throwaway tags removed    | `git ls-remote --tags` and `git tag -l` both return `v0.1.0` alone; `gh release list` returns one release. `v0.1.0-rc.1` and `v0.1.0-rc.2` are gone locally and remotely, along with their releases |
  | Published, not replaced   | not draft, not prerelease, target `main`, tag object `ece27ea` peeling to `671af41`. Every asset's `createdAt` equals its `updatedAt` at `22:23:11Z` — nothing was re-uploaded                      |

  **Executed identity** was confirmed for `aarch64-apple-darwin` on this host: extract, run, `{"schemaVersion":1,"version":"v0.1.0","commit":"671af41ea4136a995180bce20c0d540205e780eb"}`, then `repo-config validate --root` against a real repository read-only, exit `0`. The other three could not be executed here — Rosetta refused the `x86_64` Darwin binary for an unrelated toolchain mismatch on this machine — so their executed proof is each build leg's own `cargo test --package xtask`, run on a runner of that architecture against that leg's own archive with the tag's commit and version in the environment: `4 passed` on all four legs, and `4 passed` again on the publishing job over the assembled set with `RHINO_RELEASE_MATRIX=complete` making the four-platform assertion total.

  **Not a clean phase, and the record should say so.** Two defects were found and fixed before the tag, both of which would have been breaking changes afterwards: the executable reported `0.1.0` where the tag says `v0.1.0`, and the mandated Gherkin review found a mutant (`v9.9.9`) that satisfied every assertion in the corpus and was still the wrong release. One more was found by the first rehearsal tag: a single size ceiling that fitted one platform and failed two. Each is recorded in its own row above with the reasoning, not folded into a summary.

## Phase 7 — BeaverNest Adoption

- [x] `[AI] [AC-01, AC-02]` Input: the constants table, the schema as hardened by Phases 4 and 5, and the Phase 5 draft configurations as worked examples. Action: write `repo-config.yml` at this repository's root, transcribing every current Badakmini constant. Outcome: this repository's policy is declared and readable. Proof: `repo-config.yml` covers every row of the constants table; no credential, hostname, or runtime value appears in it, per [public repository data safety](../../../repo-governance/conventions/public-repository-data-safety.md). **Done 2026-09-08.** `repo-config.yml` is at the root, transcribed from `apps/badakmini-cli` rather than re-derived: word budget 750 over `AGENTS.md` and `repo-governance/**/*.md`, four mapped trees, `plans/done/**` excluded as a link source, Mermaid 32/24 with the six-fill palette, three-harness parity over seven capabilities and two constraints, four prohibited instruction sources, and eleven scan exclusions. Three omissions were caught only by re-reading the F# source and are recorded in [learnings](learnings.md): the `coverage` scan exclusion, the `plans/done/**` link-source exclusion at `Governance.fs:754`, and one mapped tree where the repository maps four. Validated against the live tree with the published `v0.1.0` binary; every count matches the Phase 1 baseline — 57 word-budgeted files, 47 directory maps across four trees at 6, 5, 11, and 25, 557 links, 33 diagrams, and three-harness parity over 7 skills and 2 agents. No credential, hostname, or runtime value appears in it.
- [x] `[AI] [AC-04]` **RED:** Input: [the HIPPO consumer specification](../../../specs/tools/hippo-consumer/README.md). Action: write `specs/tools/rhino-consumer/` with its maps and `behaviours/rhino-bootstrap.feature`, then `.github/scripts/test-rhino-bootstrap.sh` implementing it, before `./rhino` exists. Proof: the suite fails because the bootstrap is absent. **Done 2026-09-08.** `specs/tools/rhino-consumer/` holds its README, its behaviours map, and a sixteen-scenario `rhino-bootstrap.feature`; `specs/tools/README.md` gained the sibling. Fourteen scenarios are HIPPO's corpus carried over in shape, because the hazard — one machine-wide cache several checkouts install into concurrently — is the same one. One was dropped: _Concurrent owners survive a retryable coordination deferral_ is about HIPPO's own reservation protocol, which RHINO has no equivalent of, and a scenario kept for symmetry would have been bound by a step that asserts nothing. Two are RHINO's own: _Unsupported platform is refused before any transport_, because RHINO publishes four target triples and a fifth host has no asset to fetch, and _The two consumer bootstraps never reach each other's state_. `.github/scripts/test-rhino-bootstrap.sh` (809 lines) is the documented fork of the HIPPO adapter, binding all sixteen by exact title with `*) exit 1 ;;`. **Proof:** the suite exits `1` with `cp: <repo-root>/rhino: No such file or directory` — it fails at the absence of the bootstrap, before any fixture runs.
- [x] `[AI] [AC-04]` **GREEN:** Action: write `./rhino` as the documented fork of `./hippo` — renamed identifiers, RHINO cache root, RHINO release URL, `rhino` executable — preserving lock validation, the platform matrix, warm-cache digest and identity verification, install serialization, live-owner identity checks, release claims, and bounded retention. Write `rhino.lock` pinning `v0.1.0`, its commit, and all four checksums. Proof: `.github/scripts/test-rhino-bootstrap.sh` passes; `./rhino version --json` matches the lock after a cold cache install. **Done 2026-09-08.** `./rhino` is the fork, with every safety property carried over intact and three differences stated in its header: platforms are Rust target triples, there is no repository-local policy file to point the executable at, and the argument vector passes through untouched where HIPPO's maps worker settings ahead of it. `rhino.lock` pins `v0.1.0`, commit `671af41ea4136a995180bce20c0d540205e780eb`, and four checksums — each compared against the release's own `checksums.txt` fetched from GitHub rather than transcribed from the build that produced them, and the commit against `git rev-parse 'v0.1.0^{}'`. **Proof:** the sixteen-scenario suite passes in 18s (`rhino bootstrap tests passed`); from an empty cache `./rhino version --json` returned `{"schemaVersion":1,"version":"v0.1.0","commit":"671af41ea4136a995180bce20c0d540205e780eb"}` — byte-identical to the identity the wrapper assembles from the lock — in 1.23s, and 0.055s warm; `shellcheck -s sh` and `shfmt -d` are clean on both the wrapper and its suite.
- [x] `[AI] [AC-04]` **REFACTOR:** Action: verify the two bootstraps cannot interfere — distinct cache roots, distinct install guards, and RHINO retention never seeing a HIPPO release. Proof: an exact search shows no shared path; both tools run successfully in sequence from a cold cache. **Done 2026-09-08.** Held three ways, because path-disjointness alone would be a claim about how the wrappers are written rather than about what they do. **Statically:** the scenario _The two consumer bootstraps never reach each other's state_ is bound and passing — neither wrapper names the other outside whole-line comments (the fork has to say what it was forked from), each declares its own lock, cache-root override, cache directory, transport override, and asset name, and neither suite drives the other's test seams. **By observation:** both tools ran cold and then warm under one shared `XDG_CACHE_HOME`, in both orders, all four runs reporting their own pinned identity; each wrote exactly seven paths, and an exact search found no path in common and no path of either that is a prefix of a path of the other — so the install guards, at `<root>/.install-guard.lock` under each tool's own root, are separate files. **Against the dangerous case:** five idle releases were planted under _both_ roots and a cold RHINO install forced its retention pass. It reclaimed three of its own, keeping the pinned `v0.1.0` and the two most recent fallbacks, and left all six directories under HIPPO's root — including the three it would have reclaimed had it seen them.
- [x] `[AI] [AC-02, AC-05]` Input: the pinned binary and the declared configuration. Action: create `apps/rhino-consumer/` with `project.json` composing `test:repo` and `test:bootstrap`, and its [project README](../../../repo-governance/conventions/project-readmes.md). Outcome: every existing reference to a `test:repo` target keeps working. Proof: `HIPPO npm exec -- nx run -p rhino-consumer -t test:repo` runs all eight validator invocations against this repository. **Done 2026-09-08.** `apps/rhino-consumer/` holds `project.json` and `README.md` and no source, composing `./rhino` invocations the way Badakmini composed raw `dotnet` ones. `test:repo` runs in parallel with `cwd` at the workspace root; its `inputs` mirror Badakmini's plus `rhino`, `rhino.lock`, and `repo-config.yml`, so a pin move or a policy edit invalidates it. `test:bootstrap` runs the consumer suite so it is reachable outside the hook. **Deviation, stated rather than absorbed: six invocations, not eight.** Badakmini's four `--directory` calls collapse into one, because the four mapped trees are now declared in `repo-config.yml` and the call site no longer restates them — the same 49 directories either way; `repo-config validate` is the one addition, and it has no Badakmini counterpart because the policy only became a checkable artifact when it stopped being compiled into the validator. Coverage is unchanged; only the call count fell. **Proof:** `./hippo run --class ephemeral --disk-path . -- npm exec -- nx run rhino-consumer:test:repo` succeeded in 1.4s — `[repo-config] checked 1 configuration file`, `[word-budget] checked 57 files`, `[directory-map] checked 49 directories`, `[harness-parity] checked 3 harnesses` over 7 skills, 2 agents, and 3 reconciled capability declarations, `[internal-link] checked 572 links`, `[mermaid] checked 33 diagrams`, every one with no findings and exit `0`. `test:bootstrap` succeeded in 19.1s. Every existing reference still works untouched: all fourteen name `badakmini-cli:test:repo` explicitly, and that project is unchanged.
- [x] `[AI] [AC-05]` Input: the first full run. Action: reconcile every finding against Badakmini's current clean result, fixing the tool or the configuration — never the governed content — until both agree. Outcome: the new gate is clean for the same reasons the old one was. Proof: both gates run clean over the same corpus. The Phase 1 baseline run on 2026-09-07 reported 57 word-budgeted files, 47 directory maps across four trees (6, 5, 11, 25), 158 link-checked Markdown files, three-harness parity covering seven skills, two agents, and one capability declaration, and 33 Mermaid diagrams — but these are re-read from the run immediately preceding the comparison rather than quoted from here, because the link count alone drifted by three while this plan was being written. **Done 2026-09-08.** Both gates ran on the same tree within a minute of each other and both are clean, so there were no findings to reconcile — the work was reconciling the _reports_, because three of the six numbers are spelled differently by the two tools and only one of those differences turned out to be about the report rather than the coverage.

  | Check          | Badakmini                                                 | RHINO                                                                 | Reconciled                                                                         |
  | -------------- | --------------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
  | word budget    | 57 governed files, 750-word limit                         | 57 files                                                              | identical                                                                          |
  | directory maps | 6 + 5 + 13 + 25 = 49                                      | 49, and 6 / 5 / 13 / 25 asked tree by tree                            | identical tree by tree, not only in total                                          |
  | Mermaid        | 33 compatible diagrams                                    | 33 diagrams                                                           | identical                                                                          |
  | internal links | 161 Markdown files                                        | 572 links                                                             | same corpus, different unit                                                        |
  | harness parity | 3 harnesses, 7 skills, 2 agents, 1 capability declaration | 3 harnesses, 7 skills, 2 agents, 3 reconciled capability declarations | same reconciliation, different thing counted                                       |
  | configuration  | —                                                         | 1 configuration file                                                  | no counterpart; the policy only became checkable when it stopped being compiled in |

  **Links.** Badakmini reports how many documents it read; RHINO reports how many link targets it resolved inside them. To establish the corpus is the same rather than merely plausible, the source set was derived independently from `repo-config.yml`: every `*.md` outside the eleven excluded directories is 209 files, less the 48 under `plans/done/**` excluded as sources, is **161** — Badakmini's number, reached without asking either tool.

  **Capability declarations.** The repository declares one canonical `required-mcp` and three per-harness capability files: `.codex/config.toml`, `.mcp.json`, and `opencode.json`. Badakmini counts the canon once; RHINO counts the three reconciliations performed against it. Neither misses anything, and RHINO's is the number that answers "how many places did you look".

  **The two digests are not comparable, and should not later be read as drift.** `e889551a…` and `5dc29388…` are two tools hashing their own canonical sets under their own framings; comparing them would compare hash functions rather than repositories.

  **Proof:** `badakmini-cli:test:repo --skipNxCache` succeeded in 3.0s across its eight invocations, `rhino-consumer:test:repo` in 1.4s across its six, every leaf exiting `0` with no findings. Nothing was fixed to reach this — not the governed content, and not the tool or the configuration either.

- [x] `[AI] [AC-04, AC-12]` Input: the working bootstrap. Action: measure the consumer half of distribution — cold pin-to-executable wall-clock from an empty cache with the network stated, warm per-invocation resolution overhead against the cached executable invoked directly, and cache-root size after install and after bounded retention runs. Outcome: the path from a lock line to a running check is measured, not just the artifact at the end of it. Outcome check: if warm resolution overhead is a material fraction of a validator run, fix the caching before any cutover — it would be paid on every invocation forever. Proof: the recorded figures with their network and cache conditions, and the comparison against the baseline's SDK-install-plus-build cost.**Done 2026-09-08.** Full figures, harnesses, and the reasoning are in [learnings](learnings.md); the short form is that the distribution claim holds and the outcome check fired, but not on the thing it was aimed at.

  **Cold, with the network stated:** empty cache to a successful `rhino version --json` is a **median 0.864 s** over n=5 (min 0.825, max 1.499), downloading the 722,586-byte `aarch64-apple-darwin` archive from GitHub Releases over a residential connection. Against the baseline's 674 MB .NET SDK plus 5.035 s of restore and build, this is the part of the claim that holds without qualification.

  **Cache footprint:** 1,476 KiB after install. Retention was exercised rather than waited for — five idle releases planted with ~9 MiB of filler took it to 10,476 KiB, and a forced install pruned it back to **5,076 KiB across three release directories**: the pinned release and two recent fallbacks.

  **Warm overhead, and the outcome check:** ~**54 ms** per invocation on two independent harnesses that agree. It is not a fraction of a validator run — `md internal-link validate` is 38.2 ms, so the bootstrap costs more than the check it wraps. **The check says fix the caching; the caching is not what is wrong.** All 54 ms is safety: 16.5 ms re-hashing the cached executable, 2.3 ms re-reading its embedded identity, and ~35 ms of the wrapper's own process spawns for the lock, the platform, the release claim, and the install guard. Each is a property the corpus asserts, and dropping one to save less than one leaf of the gate would be trading tamper defence for noise. The addressable half is the call site: `test:repo` invokes `./rhino` six times and pays it six times, serialized on the exclusive guard, so the parallel gate is **1.283 s** through the bootstrap against **1.127 s** through the cached executable. A resolve-once caller would pay 54 ms once; that needs a new mode in the wrapper and is filed as a follow-on rather than done here.

  **What the measurement actually found — and it is a blocking finding for Phase 8.** `governance directory-map validate` costs **1,075 ms**, thirty times every other leaf and the entire gate's critical path: `repo-config` 2.8 ms, `word-budget` 31.7 ms, `harness parity` 106.4 ms, `internal-link` 33.2 ms, `mermaid` 30.2 ms. It is linear in mapped directories at ~22 ms each — 148 / 131 / 297 / 544 ms for the four trees — and the cause is in the port: `Tree::children()` defaults to calling `self.files()`, a full recursive walk, and `DiskTree` never overrides it, so forty-nine questions about one directory each walk the whole repository forty-nine times. Badakmini's whole eight-invocation gate was 0.392 s. **The replacement gate is currently slower than the gate it replaces, and all of the difference is one leaf.** Per the benchmarking document's own rule, that is a finding rather than a footnote, and the cutover does not proceed over it — the fix is scheduled ahead of Phase 8.

- [x] `[AI] [AC-06]` Input: both binaries. Action: run `md word-count inspect` under each over every governed Markdown file and compare counts. Outcome: the numeric gate is proven not to have drifted. Proof: zero differing files; any difference stops the line until resolved.**Done 2026-09-08.** **Zero differing files.** All 57 governed files agree exactly, and so do their totals: **24,912 words** from each binary.

  **The task's own method did not survive contact with the tool.** Badakmini has no `md word-count inspect` leaf — it has `governance word-budget validate` and nothing else under that heading — so "run it under each" was never possible. This is the second time this plan has assumed a command surface from a gate's wiring rather than from the binary, and the first is already recorded in [learnings](learnings.md); the lesson is the same one and it now has two instances.

  What replaced it is better than the symmetry it lost: `badakmini governance word-budget validate --format json` reports a `wordCount` for **every** governed file, not only for violating ones, so the comparison is against all 57 rather than against whichever happened to be near the limit. RHINO answers the same question through one `md word-count inspect` invocation with 57 repeated `--file` flags. Set difference in both directions is empty — neither binary counted a file the other did not — and no file's counts differ.

  One number worth surfacing rather than leaving in the data: `AGENTS.md` is at **749 words against a 750 limit**. Both binaries agree on that too, which is the strongest single point in the comparison — the file where a one-word disagreement would have flipped the gate is the file they agree on to the word.

- [x] `[AI] [AC-12]` **Added during Phase 7 — the replacement gate was slower than the gate it replaces, and one leaf was all of it.** Measuring the consumer half found it: `governance directory-map validate` cost **1,065 ms** against 33 ms for every other leaf. `Tree::children` has a default that derives the answer from `files()`, a full recursive walk — correct for any implementation, affordable only for the in-memory one — and `DiskTree` never overrode it, so answering forty-nine questions about one directory each walked the whole repository forty-nine times. The cost grew with the repository rather than with what was being inspected, which is why four trees were not enough to expose it and the largest one was. **This had to land before the cutover:** [the benchmarking document](tech-docs/05-benchmarks.md) says a rewrite slower than what it replaced is a finding rather than a footnote, and Phase 8 deletes the faster implementation. **Proof:** RED — a source-level cost test failed with `DiskTree does not override 'children', so listing one directory walks the whole repository`, written that way because no single run can show that the next one does not walk, and it sits beside the network and subprocess absences that are checked the same way for the same reason. A second test pins the equivalence as a differential against the definition the default derives, over a fixture carrying a filesystem link, an excluded directory, and an empty directory — it passed before the change, which is the point of writing it first. GREEN — `directory-map validate` **1,065 → 87 ms** and the six-leaf parallel gate **1,150 → 178 ms**, over two independent runs; every other leaf unchanged within noise. Both binaries built from the same `[profile.release]`, so the comparison is not a profile artifact. Output is byte-identical: eighteen runs, six leaves against three real repositories, every JSON document and exit code the same. Whole corpus green at all three adapters, coverage held, `cargo xtask test-quick` clean. Released upstream at `38ab4ea` as `v0.1.1`.
- [x] `[AI] [AC-04, AC-08, AC-12]` **Added during Phase 7 — publish `v0.1.1` and move the pin.** A fix nobody runs is not a fix, and the plan's own rule is that a published tag is never replaced, so the cost defect needed its own release rather than an amendment to `v0.1.0`. **Proof:** tagged at `38ab4ea` and published by the release workflow in 2m11s with all five assets and `prerelease: false`. `rhino.lock` moved to `v0.1.1`, the peeled tag's commit, and four checksums fetched from the release's own `checksums.txt` rather than transcribed from the build — the same discipline as the first pin. From an empty cache the wrapper reported `{"schemaVersion":1,"version":"v0.1.1","commit":"38ab4ea3592573b7ae10766139a86f33b8dc2bf3"}` in 1.6s, the sixteen-scenario bootstrap suite passes against the new pin, and the repository gate is clean on identical counts. **The wired comparison, both gates measured the same way — `--skipNxCache` under one HIPPO lease, first run discarded as cold:** `badakmini-cli:test:repo` 938 and 985 ms; `rhino-consumer:test:repo` **523 and 608 ms**. The replacement gate is now about 1.7× faster than the gate it replaces, through the same task runner, which is the figure the regression finding above was waiting on.
- [x] `[AI] [AC-05, AC-06]` **Added during Phase 7 — the mandated Gherkin review failed three rows of the consumer suite, and all three were bindings that could not fail.** [The review workflow](../../../repo-governance/workflows/gherkin-implementation-review.md) runs over changed Gherkin and adapters, and it returned 13 PASS and **3 FAIL** over sixteen rows. **F1, concurrent stale reclaimers:** the fake `rm` choreographed a race the wrapper cannot enter, because `acquire_install_guard` takes a kernel lock before `acquire_install_lock`; the reviewer instrumented a copy and found all three race-branch entries came from one caller PID, and the fixture's wait gave up silently after two seconds (`[ "$attempt" -lt 40 ] || break`). A wrapper in which a second reclaimer destroys a replacement live owner passed it unchanged. **F2, release ranking:** a source-shape grep, not a proof — the reviewer wrote the exact fallback-chain bug across two lines and the binding passed, and the counters required `stat` to begin the line, so any command-substitution call site was invisible. **F3, bootstrap disjointness:** passed on three verified counterexamples, including moving the install guard to `TMPDIR` and dropping the tool-name segment from the default cache root.

  **The fix is the same in all three: observe, do not read the source.** Contention is now choreographed through gates rather than timing — transport announces it is in flight and blocks until the test releases it, so the second consumer is started and confirmed running (it publishes its release claim before it contends) while the first provably holds the install, and every wait is bounded and fatal rather than silent. The timestamp row simulates both platforms on one host: a `stat` fixture presenting the GNU interface reproduces the prose-then-failure that a fallback chain captures, a stubbed lock tool lets the other platform's branch run, and retention must reclaim the same set under both. Disjointness runs both wrappers under one identical machine environment with transport replaced by a recorder that fails, then compares what each actually touched — path sets, cache roots derived from where each release landed, guard placement, asset URLs, and both directions of each cache-root override. Nothing reaches the network.

  **Four assertion blocks were reachable from no scenario, so the corpus grew from sixteen to twenty:** cold-then-warm cache, argument passthrough, a downloaded archive whose digest misses the pin, and concurrent cold callers. Two scenarios were reworded to the property actually held — the disjointness Then had claimed no environment variable is shared, which was false, since both wrappers read `HOME` and `XDG_CACHE_HOME`. **Every `PARTIAL` was resolved:** exit 78 in the platform refusal is now attributed by its message, the traversal assertion targets the path the escape actually resolves to, the release-claim producer and reader are exercised by a real consumer holding a real claim instead of a record the test wrote, the malformed-identity cases are digest-consistent so they reach the identity check rather than an earlier gate, and the missing `SLEEP_FAIL` seam is set.

  **Proof — six mutants of `./rhino`, each run against the old adapter and the new one, one scenario at a time:**

  | Mutant                                            | Old    | New       |
  | ------------------------------------------------- | ------ | --------- |
  | Reclaimer drops the ownership it published        | passes | **fails** |
  | Timestamp read becomes a two-line fallback chain  | passes | **fails** |
  | Install guard moves out of the cache root         | passes | **fails** |
  | Cache root falls back to HIPPO's override         | fails  | **fails** |
  | Default cache root loses its tool-name segment    | passes | **fails** |
  | Published claim spells its identity in upper case | passes | **fails** |

  Five of six escaped the bindings they were supposed to fail; all six are killed now, and the unmutated wrapper passes both. Twenty of twenty scenarios green, `shellcheck` clean, `nx run rhino-consumer:test:bootstrap` green in 14.4s under a HIPPO lease. **Filed against BeaverNest, not fixed here:** `.github/scripts/test-hippo-bootstrap.sh` carries both defects verbatim — the same silent `|| break` at line 151 and the same same-line-only grep at line 704 — and is outside this plan's boundary.

- [x] `[AI] [AC-05, AC-06]` **Added during Phase 7 — the same three defects were sitting in the HIPPO consumer suite, inherited in the other direction.** The RHINO suite was forked from `.github/scripts/test-hippo-bootstrap.sh`, so every finding above was a finding about its origin too: the same `[ "$attempt" -lt 40 ] || break` that made the reclaim race a silent no-op, the same same-line-only `stat -[fc] .*|| stat -[fc]` grep, the same hand-written release claim, the same escape assertion pointed at a path the traversal never reaches, the same digest-shadowed malformed-identity case, and the same missing `SLEEP_FAIL` seam. It also carried **five** assertion blocks reachable from no scenario — cold-then-warm cache, the worker-settings mapping, a download digest mismatch, concurrent cold callers, and the platform refusal — and a fourteen-line comment about `stat` attached to the wrong function entirely. Originally left alone as out-of-boundary; fixed on the user's instruction.

  **Ported whole, with HIPPO's own vocabulary:** goos/goarch platforms rather than target triples, and the worker-mapping scenario in place of RHINO's argument passthrough, since HIPPO's wrapper deliberately prepends `--concurrency-env` settings where RHINO's passes the vector through untouched. Corpus **15 → 20**, every assertion now inside a scenario. **Proof — four mutants of `./hippo`, old adapter against new, one scenario at a time:** the reclaimer dropping the ownership it published, the two-line fallback chain, a claim published in upper case, and the platform refusal losing its operating-system branch. All four are killed by the new suite; **three of the four passed the old one** — the fourth was caught only by the unbound trailing block that is now a bound scenario. Twenty of twenty green in 14.0s against RHINO's 14.3s, `shellcheck` clean, and a structural check confirms all twenty dispatch arms resolve to defined functions in corpus order with nothing left undispatched.

  **One mistake worth recording:** the first attempt at this port silently deleted most of the file, because a text anchor used to cut the trailing unbound block also matched a comment the same script had just inserted. The suite still reported `hippo bootstrap tests passed` — with four bindings left and no dispatch loop at all. What caught it was the **runtime**: 1.5s where the sibling suite takes 14. Every later edit went through anchors asserted unique before use, and the post-condition above exists because a green run could not distinguish a passing suite from a mostly-deleted one.

- [x] `[AI] [AC-05, AC-06]` **Added during Phase 7 — the re-review passed every row and then found the branch no row covered.** Twenty of twenty `PASS`, nothing `FAIL`, nothing exempt. All three original findings were verified closed by reconstructing each prior counterexample as a mutant and killing it — the two-line fallback chain, the read spelled in a command substitution, the deleted platform branch, the guard moved to `TMPDIR`, the variable renamed with its old name left in a comment, the dropped tool-name segment, a fallback onto the neighbour's override, an asset fetched from the wrong tool's releases, and an install into the other tool's root. All six `PARTIAL` concerns confirmed resolved; forty-seven mutants run in total.

  **Three new findings, all acted on.** **A — the exact-owner check in `release_install_lock` was covered by nothing.** Replacing the whole identity-and-digest comparison with an unconditional `rm -f -- "$install_lock"` passed the entire suite, and an adapter comment of mine asserted the opposite: _"had the second removed it instead, the first's exact-owner check would have left it behind."_ It would not have — a second consumer is serialized at the kernel install guard and meets a warm cache, so it never reaches the branch at all. The false comment is deleted and a scenario now reaches the branch directly: an installing consumer is held inside transport, a different live owner's record is substituted over its lock, and on the way out it must leave the replacement alone. **Proof:** the unmutated wrapper passes; the unconditional-`rm` mutant passes the **whole** previous suite and fails the new scenario, in both consumers. Without that check, a consumer whose lock had already been reclaimed would delete its successor's and admit a third installer.

  **C — a scenario whose `When` never happened.** `Install guard storage stays bounded` installed four pins and asserted one guard, but retention found no candidates: all four sat inside the seven-day idle window, so "retention prunes superseded versions" was never exercised and the guard count was only ever observed on a cache nothing had reclaimed. Three pins are now aged past the window with distinct stamps and a fifth is installed to trigger a real prune; the row asserts the oldest was reclaimed and the rest survived before it counts guards. **D — a `Given` that described a state the fixture never reached.** It said one consumer _is publishing an install_; the holder had finished installing and was executing under a live claim. Renamed to `Retention never deletes a release another consumer is using` and reworded to say that. Both fixes are in both consumers, whose corpora are now **21 scenarios each**, and a structural post-condition holds in both: every dispatch arm resolves to a defined function, in corpus order, with nothing left undispatched.

- [x] `[AI] [AC-05, AC-06]` **Added during Phase 7 — a focused re-review of the delta closed the two open findings and made both halves of the exact-owner check load-bearing.** Eight of eight `PASS`. **Finding A closed:** the survivor is not the literal mutant the finding named — replacing `release_install_lock`'s whole body with an unconditional `rm -f` is already killed by _Matching live install-lock owner remains protected_ — but a surgical `if true` over the exact-owner comparison, which survives all twenty other rows in both suites and is killed only by the new one, at `[ -f "$install_lock" ]`. The `rm`+`ln` substitution was judged boundary-valid and product-reachable: it is what `reclaim_atomic_install_lock` followed by `acquire_install_lock` actually does. **Finding C closed:** the mutant in which `prune_cache` never runs is killed by _Install guard storage stays bounded across release versions_ in both suites, and re-applying it to a reconstructed pre-fix body passes — so the finding was real and this change is what closes it. **Then the residual the re-review left standing was closed too.** Dropping either half of the exact-owner comparison on its own survived the whole corpus, because the fixture's replacement differed in both. The row now runs three substitutions: a foreign owner (fails both), a rewrite in place (same inode, different digest — only the digest can notice), and a byte-identical copy (same digest, different inode — only the identity can notice). Each single-conjunct mutant now dies at that row in both wrappers, as does the whole-condition one. **Also fixed:** the product comment at `rhino`/`hippo` claimed retention runs under the guard because releasing first "would let it delete a release a peer is publishing" — an unbacked necessity, since what actually protects that release is the peer's claim, published _before_ it takes the guard. Reworded to the guarantee the guard does supply: one pruner at a time. **And the suites now name each scenario as it runs** — under `set -e` a failure was otherwise silent, which is part of why a mostly-deleted suite had to be caught by a stopwatch. Both corpora report 21 scenarios executed.
- [x] **Blocking checkpoint — Phase 7:** `[AI] [AC-02, AC-04, AC-05, AC-06, AC-12]` Confirm declared policy is complete, the bootstrap suite passes, the pinned binary verifies from a cold cache, distribution cost is measured and its warm overhead acceptable, both gates agree, and word counts match exactly. **Proof:** **Declared policy complete** — `rhino repo-config validate` reports `checked 1 configuration file, no findings`; every section RHINO owns is present and no key is defaulted. **Bootstrap suite passes** — both consumer corpora grew from sixteen and fifteen scenarios to **21 each**, and both run green: `rhino bootstrap tests passed` and `hippo bootstrap tests passed`, 21 scenarios named and executed in each, every dispatch arm matching a feature title in exact text and order, no dispatched-but-undefined or defined-but-undispatched binding, every wait bounded and fatal. **Pinned binary verifies from a cold cache** — from an empty cache the wrapper emits `{"schemaVersion":1,"version":"v0.1.1","commit":"38ab4ea3592573b7ae10766139a86f33b8dc2bf3"}` in 1.6 s, digest-checked against `rhino.lock` before anything executes. **Distribution cost measured, warm overhead acceptable** — warm-cache dispatch is the `exec` of an already-verified binary; the wired gate comparison under one HIPPO lease with `--skipNxCache` is `badakmini-cli:test:repo` 938/985 ms against `rhino-consumer:test:repo` **523/608 ms**. **Both gates agree** — re-run back to back on the final tree under `./hippo run --class ephemeral --disk-path .`, exit 0: Badakmini reports 57 governed files within 750 words, 161 Markdown files with all local links resolving, 49 directory maps across five trees, 33 Mermaid diagrams, and a 3-harness / 7-skill / 2-agent contract; RHINO reports 576 links, 57 files, 49 directories, 33 diagrams, 3 harnesses, and the same reconciliation. The two link counts differ because they count different things — Badakmini counts files scanned, RHINO counts links resolved — which was reconciled in the Phase 7 comparison row above. **Word counts match exactly** — file-by-file across all 57 governed surfaces, zero divergence, recorded in the comparison row above. **Standing caveat, recorded rather than papered over:** a mutant that moves `prune_cache` outside the install guard still survives both corpora. The second reviewer's reading is that it is an equivalent mutant — `publish_release_claim` runs before `acquire_install_guard`, so any installing peer already holds a live claim, and a just-created release is inside the idle window regardless. The comment on those lines now says only what the suite can show.

## Phase 8 — BeaverNest Cutover and Retirement

- [x] `[AI] [AC-05]` Input: the verified consumer. Action: repoint `.husky/pre-push` — validation paths lose the Badakmini projects and gain `repo-config.yml` and `apps/rhino-consumer/**`, a RHINO consumer predicate is added, that predicate also runs `shellcheck` over the owned `rhino` fork and its suite, and the gate command becomes the new target — in one edit. Outcome: no commit exists at which neither validator gates a push. Proof: a simulated push over a governed-content range runs the new gate; a range touching only the consumer boundary runs only the bootstrap suite. **Proof:** one edit. The validation path set drops `apps/badakmini-cli/**` and `apps/badakmini-cli-e2e/**` and gains `repo-config.yml` and `apps/rhino-consumer/**` in all three of its spellings; the existing consumer predicate is renamed `hippo_consumer_changes_for_range` now that there are two; a `rhino_consumer_changes_for_range` predicate covers `rhino`, `rhino.lock`, its suite, and `specs/tools/rhino-consumer/**`; its block runs `shellcheck rhino .github/scripts/test-rhino-bootstrap.sh` before the suite, because `rhino` is this repository's own fork rather than a vendored file and no other gate reads it; and the gate command becomes `nx run -p rhino-consumer -t test:repo`. `shellcheck -s sh` is clean and no `badakmini` reference remains in the hook. **Routing proved on two synthetic commit objects built off `HEAD`** — real Git objects, no branch or worktree touched, each isolating one boundary. **Governed-content range** (`repo-governance/README.md` only): the repository gate ran and reported `Successfully ran target test:repo for project rhino-consumer`; no bootstrap suite, no `shellcheck`. **Consumer-boundary range** (`rhino` only): `shellcheck` then `rhino bootstrap tests passed` with 21 scenarios executed, then `Skipping repository validation: pushed commits do not affect governed content or its validators`; the HIPPO suite correctly did not run. Hook exit 0 in both. The `nx affected -t test:quick` graph still schedules the Badakmini projects in both runs — that is the affected graph, not the hook's gate routing, and it goes away with the deletion in the next row.
- [x] `[AI] [AC-05]` Input: the working consumer. Action: delete `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, `specs/apps/badakmini/`, and `beaver-nest.sln`; remove the `badakmini-test-symphony` job and its .NET setup from `.github/workflows/scheduled-quality-gates.yml`; and update `.gitignore`, `apps/bnest-app/tools/release.mjs`, and `RTK.md`. Outcome: .NET leaves this repository entirely. Proof: an exact search for `badakmini`, `Badakmini`, `dotnet`, and `.fsproj` returns hits only in `plans/done/` and `.nx/` cache. **Proof:** all four paths deleted, the `badakmini-test-symphony` job and its `actions/setup-dotnet` step removed whole from `.github/workflows/scheduled-quality-gates.yml`, and the three named peripheral files updated — `.gitignore` drops the coverage directory of a project that no longer exists, `RTK.md` and `apps/bnest-app/tools/release.mjs` are repointed to `rhino-consumer:test:repo` rather than emptied. **One file outside the row's list was also changed, as a direct consequence rather than a widening:** `.vscode/settings.json` carried `"FSharp.workspacePath": "beaver-nest.sln"`, a pointer to a file this row deletes. `nx show projects` now returns `["rhino-consumer","bnest-app-e2e","bnest-app","ex-bdd"]`, and an exact search finds **no live `dotnet` invocation anywhere**. The only surviving `dotnet` token is `DOTNET_PROCESSOR_COUNT` in `hippo` and its suite, which is HIPPO's generic worker-settings vocabulary rather than a toolchain this repository depends on. **The row's own proof condition — hits only in `plans/done/` and `.nx/` — does not hold yet, and cannot at this row:** the deletion leaves exactly six dangling references (five links and one directory-map entry, in `plans/in-progress/rhino-cli-extraction/README.md`, `tech-docs/README.md`, `repo-governance/conventions/markdown-visualizations.md`, and `specs/apps/README.md`), plus the prose references that are the explicit subject of the propagation and reference rows below. The full search is recorded there, where it is true.
- [x] `[AI] [AC-05]` Input: the deleted projects and Git history as the recovery source. Action: rehearse the rollback before relying on it — restore `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, `beaver-nest.sln`, and the previous `.husky/pre-push` into a scratch worktree under `local-tmp/`, run the retired gate there, and confirm it reports the same clean result it gave before deletion. Outcome: the recovery path is proven while it is still cheap to prove, rather than discovered to be broken during an incident. Outcome check: if the restore cannot reproduce the prior clean result, the deletion is not final until it can. Proof: the retired gate's clean output from the scratch worktree, and the worktree removed afterwards. **Proof:** rehearsed as the operation an incident would actually run, not as a checkout of an old commit. A detached worktree was added at `HEAD` under `local-tmp/`, then `git revert -n` was applied to the deletion and the hook repoint together — which restores `specs/apps/badakmini/` as well, a path the row's own list omits and a real rollback needs. **The restored tree is byte-identical to the pre-deletion commit:** `git write-tree` equals `ea8bbe46e^{tree}`. The retired gate then ran there under one HIPPO lease with `--skip-nx-cache`, exit 0, and reproduced the prior clean result: 57 governed Markdown files within 750 words, 49 directory maps (13 specs, 6 repo-governance, 5 docs, 25 plans), 33 Mermaid diagrams, and harness contract `e889551a05b93ad1c5fcd20223f72c68eb284f4af604094b4befcb1d0b14560c` reconciling 3 harnesses, 7 skills, 2 agents, and 1 capability declaration — every figure identical. **One figure differed and is fully accounted for:** the link validator scanned 155 Markdown files against the main tree's 161. The six are untracked, ignored scratch files that exist in a working tree and not in a fresh worktree — `generated-reports/` (2) and `local-tmp/` (4). 155 + 6 = 161, so the restored tree checks exactly the same repository. Worth recording separately: this means both validators scan `local-tmp/` and `generated-reports/`, which `scan.exclude-directories` does not list. Pre-existing and identical in both, so it is not a cutover regression — but it does mean the rehearsal worktree had to be removed before any further gate run, or the gate would have scanned a second whole copy of the repository. The worktree was removed with `git worktree remove --force`; `git worktree list` shows only the main tree.
- [x] `[AI] [AC-05]` Input: the retired tree. Action: perform the authority cutover verification — clear the RHINO cache, start a fresh process, run the full gate from the persisted configuration and pin with no fallback present, then inject one violation of each kind into a scratch copy under `local-tmp/` and confirm each is detected. Outcome: "clean" is proven to mean "checked". Proof: the cold-cache clean run plus five detected injected violations; the scratch copy is removed afterwards. **Proof:** the machine-wide cache at `~/Library/Caches/rhino` was removed outright, and no `rhino` or `badakmini` executable is on `PATH` — so there was nothing to fall back to. A fresh process then ran the whole gate from `repo-config.yml` and `rhino.lock` alone: **clean in 3.4 s including the download**, and the cache afterwards held exactly one directory, `v0.1.1`, the release the lock names. Counts: 1 configuration file, 57 governed files, 540 links, 46 directories, 30 diagrams, 3 harnesses, digest `4c7ebaa86e042e92b8716ee9fcd38fc9dc9026d2b99401c82edfabd513c2739d`. **Then "clean" was proved to mean "checked."** A `git archive HEAD` copy under `local-tmp/` — confirmed clean first — took one injected violation of each kind, and each was detected with exit `1` and a finding naming the exact path: **word budget**, `repo-governance/conventions/language.md: is longer than its declared word budget (words=886, limit=750)`; **directory map**, `repo-governance/conventions/README.md: missing map entry for repo-governance/conventions/language.md`; **internal link**, `docs/README.md:14: ./no-such-document.md does not exist`; **Mermaid**, `docs/tutorials/README.md:13: a node label is longer than the declared limit (segment=node, measured=66, limit=32)`; **harness parity**, `.claude/agents/web-researcher.md: agent-prompt-divergence: body is not the canonical route to web-researcher`. Two injections also moved the reported totals in the direction they should: the dropped map bullet was itself a link, so the link count fell by one as the added broken link raised it by one, and the extra diagram took the Mermaid count from 30 to 31. A validator that had quietly stopped looking would have reported neither. The scratch copy was removed, `local-tmp/` holds only pre-existing fixtures, `git status` is clean, and the main tree re-runs clean.
- [x] `[AI] [AC-07]` Input: the changed enforcement mechanism, with the new target already working. Action: run one [rules-propagation](../../../repo-governance/workflows/rules-propagation.md) transaction across `AGENTS.md`, `repo-governance/README.md`, and the fifteen affected rule files — including the eleven that embed the literal `nx run -p badakmini-cli -t test:repo` command, one of which is `rules-propagation.md` itself — and give `coding-harness-contract.md` and `directory-maps.md` the treatment described in [the cutover design](tech-docs/03-beaver-nest-cutover.md) so neither becomes a second copy of a declared value. Also carry the `governance harness-contract validate` to `harness parity validate` rename through every rule that spells it. Outcome: this repository's rules name RHINO as what enforces them, and the repaired workflow can verify itself. Outcome check: the transaction's own step-4 verification must run the repaired command, not the deleted one. Proof: the recorded terminal result — `PASS_CHANGED` or a specific named blocker, never an unrecorded pass. **Proof:** one transaction, terminal result **`PASS_CHANGED`**. **Ledger:** seventeen files — `AGENTS.md`, `repo-governance/README.md`, the `coding-harness-contract`, `database-audit-columns`, `directory-maps`, `documentation-architecture`, `markdown-links`, `markdown-visualizations`, and `push-hook-verification` conventions, the `end-to-end-testing`, `software-quality-enforcement`, and `specification-maintenance` standards, and the `coding-harness-contract-change`, `coding-harness-parity-verification`, `plan-quality-gate`, `rules-propagation`, and `rules-quality-gate` workflows. **Mechanical:** the embedded gate command rewritten 11 times and `badakmini-cli:test:repo` 3 times; `nx show project badakmini-cli` repointed; `governance harness-contract validate` renamed to `harness parity validate` where the parity workflow asserts the target contains it. **The two changes that are more than a rename, per [the cutover design](tech-docs/03-beaver-nest-cutover.md):** `coding-harness-contract.md` now states the parity requirement and points at `repo-config.yml` as where the roster is declared, instead of restating the roster as prose fact; `directory-maps.md` keeps the 750-word rule as a rule and names `repo-config.yml` as where the enforced value is written down. Neither loosens anything; both stop a document from being a second, drifting copy. **Three more were structural rather than nominal, because the validator has left the repository:** the contract-change workflow's five-step "change Badakmini at the narrowest layer" became a four-step route that tries `repo-config.yml` first and otherwise goes upstream, releases, and moves the pin; the parity workflow's step 4 no longer claims this repository can evaluate the validator, and runs the bootstrap suite instead while saying plainly that a verdict here is a claim about the pinned release; and `end-to-end-testing.md` lost a sentence about a process-E2E project that no longer exists. `push-hook-verification.md` and `repo-governance/README.md` now describe two separately routed consumer boundaries rather than one. **Step 4 ran the repaired command** — `./hippo run --class ephemeral --disk-path . -- npm exec -- nx run -p rhino-consumer -t test:repo` — **exit 0**, which is the outcome check this row asked for: the workflow verified itself with the command it now names, not the deleted one. **One transaction-caused finding was repaired under step 5:** `coding-harness-contract-change.md` reached 777 words against its 750 limit. Repaired by tightening the sentences this transaction had added rather than cutting pre-existing rules; the failing-check measure went 1 → 0 with no new failure class, and step 4 was rerun once.
- [x] `[AI] [AC-07]` Input: the remaining references. Action: update `README.md`, `docs/reference/glossary.md`, `docs/how-to-guides/coding-harnesses.md`, `libs/ex-bdd/test/integration/ex_bdd/boundary_policy_test.exs`, `plans/backlogs/family-learning-engine/delivery.md`, `specs/apps/README.md`, and `specs/tools/README.md`; discover by exact search whether anything under `.agents/skills/**` also names the retired target and update it if so. Outcome: no document contradicts the repository. Proof: an exact search finds no live reference to the retired projects or targets outside `plans/done/`. **Proof:** every named file updated. `README.md` drops two retired suites from its command block and replaces two layout lines with `apps/rhino-consumer/`; `docs/reference/glossary.md` retitles the entry from **Badakmini** to **RHINO** and says it is now an external tool pinned by `rhino.lock`; `docs/how-to-guides/coding-harnesses.md` and four proof commands in `plans/backlogs/family-learning-engine/delivery.md` are repointed; the `libs/ex-bdd` boundary-policy comment now names a sibling that still exists. Two references inside this plan's own documents pointed at paths the deletion removed, so they became prose rather than links — the context section's opening now reads in the past tense, which is what it is. **`.agents/skills/` was searched exactly and names neither the retired projects nor the retired target**; its only near-hit is a generic `.fsproj` entry in the Nx-import heuristics, which is about importing any .NET project rather than about this one. **Exact search result:** no live reference to `badakmini` remains outside `plans/`, `.nx/`, and two deliberate historical mentions — the consumer README's past-tense comparison of invocation counts, and this plan's own record of what was replaced. Both are history, not instruction.
- [x] `[AI] [AC-07]` Input: the completed cutover. Action: update this repository's affected specifications under the [plan specification-change convention](../../../repo-governance/conventions/plan-specification-changes.md) — the removed Badakmini tree, the new consumer specification, and both affected directory maps. Outcome: `specs/` matches the as-built repository. Proof: the specification map gate passes. **Proof:** `specs/apps/badakmini/` went with the deletion, and `specs/apps/README.md` no longer maps it — leaving one child, which the tree's own opening sentence still describes correctly. `specs/tools/README.md` already mapped both consumers, and `specs/tools/rhino-consumer/` holds the new consumer specification with its 21-scenario corpus. Both affected directory maps are therefore consistent. The specification map gate passes: `directory-map` checks 46 directories with no findings, down from 49 because the removed tree carried three of them. `internal-link` moved 576 → 539 and `mermaid` 33 → 30 for the same reason — the retired C4 model's diagrams left with it.
- [x] `[AI] [AC-12]` Input: the Phase 1 BeaverNest baseline and the completed cutover. Action: re-measure every metric by the same method on the same host, and compare. Outcome: the change is quantified for this repository. Outcome check: if the warm gate is slower or peak memory higher than baseline, stop the line — investigate and fix, or obtain and record an explicit justified acceptance; never pass it silently. Proof: the before-and-after table in `learnings.md` dated 2026-09-08, measured by both the Phase 1 bracketed harness and a direct one. Wired gate **223/228 ms -> 220/229/221 ms** (parity), peak resident **64.4 MiB -> 8.4 MiB**, startup **0.277 s -> 0.252 s**, toolchain **674 MB SDK -> one verified archive**. Neither gated metric regressed. Two things got worse or failed to improve and are recorded as such: the gate is not faster, and the bracketed harness reads 0.392 s -> 0.423 s because it charges the bootstrap inside 190 ms of its own overhead. A correction to the Phase 7 entry that claimed 1.7x is recorded in the same file: it did not reproduce.
- [x] **Blocking checkpoint — Phase 8:** `[AI] [AC-05, AC-07, AC-12]` Confirm the hook routes correctly, both projects and the solution are gone, the rollback was rehearsed and reproduced the prior clean result, the cold-cutover run is clean and provably checking, injected violations are detected, propagation returned a recorded terminal result after repairing its own verification command, and no live reference remains. **Proof, 2026-09-08, each claim checked live rather than recalled.**

  **Hook routing.** `.husky/pre-push` names `.github/scripts/test-rhino-bootstrap.sh` in all three validation-path spellings, runs `shellcheck rhino .github/scripts/test-rhino-bootstrap.sh && ./.github/scripts/test-rhino-bootstrap.sh`, and gates on `npm exec -- nx run -p rhino-consumer -t test:repo`. No Badakmini path survives in it.

  **Deletion.** `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, and `specs/apps/badakmini/` are all absent, and no `.sln`, `.fsproj`, or `.fs` file is tracked. **One artifact this row's earlier proof missed and this checkpoint found: `global.json`, pinning .NET SDK 10.0.107, was still tracked.** Nothing referenced it, which is exactly why an invocation search did not catch it — it is a toolchain declaration, not an invocation, and the earlier proof searched only for invocations. It is deleted here. The two provenance comments in `repo-config.yml` that pointed at `apps/badakmini-cli` paths are reworded to name the validator as retired rather than to cite a path that no longer resolves. The only surviving `dotnet` token anywhere outside `plans/` is `DOTNET_PROCESSOR_COUNT` in `hippo` and its suite, which is HIPPO's generic worker vocabulary.

  **Cold cutover.** Cache deleted entirely, then one gate run: **exit 0 in 1.17 s**, cache afterwards holding `v0.1.2` and nothing else. The gate reports 540 links, 57 word-budgeted files, 46 directory maps, 30 Mermaid diagrams, 3 harnesses over 7 skills, 2 agents, and 3 reconciled capability declarations, all clean — so it is provably checking rather than provably quiet.

  **Injected violations.** A broken relative link produced exit `1` naming `docs/zz-checkpoint-probe.md:3` and the missing target exactly; a competing instruction source at `docs/AGENTS.md` produced `unexpected-instruction-source` against that path; extra prose appended to `.claude/agents/ci-monitor-subagent.md` produced exit `1`. Each returned to exit `0` on removal. The last two are the arms the `v0.1.2` read-set narrowing could have silently disabled, which is why they were re-run here rather than trusted from the earlier row. Every prohibited glob this repository declares is Markdown, so the non-Markdown half of that rule is proved by the upstream corpus rather than here, and is stated that way rather than claimed.

  **Rollback and propagation** stand on the proofs recorded in their own rows above: the rehearsal reproduced the prior clean result with the 155-versus-161 file count explained exactly, and propagation returned `PASS_CHANGED` with step 4 running the command it had repaired.

  **Gate as wired.** `npm exec -- nx run -p rhino-consumer -t test:repo` succeeds in **244 ms** (critical path 241 ms).

## Phase 9 — HIPPO: Additive Adoption

HIPPO has no validator to retire, so nothing here can regress. It is sequenced between the two cutovers deliberately, as a cheap confirmation that a repository with no prior validator adopts cleanly.

- [x] `[AI] [AC-01, AC-04, AC-11]` Input: the Phase 5 draft and the published release. Action: add `repo-config.yml`, `./rhino`, and `rhino.lock` to HIPPO, reusing the bootstrap pattern established for this repository. Outcome: HIPPO consumes the pinned binary. Proof: `./rhino version --json` matches the lock after a cold-cache install; `rhino repo-config validate` passes. **Done 2026-09-08 at `ba4f502`, pushed.** All three added at HIPPO's root, the wrapper copied unchanged from this repository -- it names no consumer, so nothing had to be adapted. Cold-cache proof: the cache deleted entirely, then `./rhino version --json` returned `v0.1.2` at commit `cbaed68...`, matching `rhino.lock` exactly, with the cache afterwards holding `v0.1.2` and nothing else. `rhino repo-config validate` reports `checked 1 configuration file, no findings`. **Two values are empty rather than borrowed, and that is the substantive decision in this row.** HIPPO's contributor rules set no length limit on anything, so `surfaces` is `[]` and the budget check reports `checked 0 files`; HIPPO documents with ASCII art and holds no Mermaid diagram, so the three palettes are `[]`, which means the first `classDef` anyone adds fails until this repository decides its own colours. Both are policy statements HIPPO can read, not gaps. `governance-directory-map` declares `specs` alone, because that is the one tree here whose READMEs carry `## Directory Map` sections; `docs/` READMEs are Diataxis landing pages, a different contract, and declaring them would impose a rule HIPPO never adopted.
- [x] `[AI] [AC-02, AC-11]` Input: the adopted tool. Action: run every validator over HIPPO and reconcile findings against its written rules — fixing HIPPO's content only where it genuinely violates its own documented standard, and never loosening a rule to obtain a pass. Outcome: HIPPO's documentation contract is mechanically enforced for the first time. Proof: a clean run, with any content fix recorded and justified against the rule it violated. **Done 2026-09-08.** **Clean on the first run, with no content fix required -- so there is nothing to justify.** All six leaves exit `0`: `repo-config` 1 file, `word-budget` 0 files, `directory-map` 2 directories, `internal-link` 152 links, `mermaid` 0 diagrams, `harness-parity` 0 harnesses with contract digest `426f9728a00e3da3...` (now `3aceda87aee32128...`, since this row's own `AGENTS.md` edit changes the canon it digests). The two zeros are reported rather than silent, which is the property that makes this a clean run rather than an empty one. **The check that matters most here is the one with nothing to reconcile:** HIPPO has no `CLAUDE.md`, no `.agents/`, no `.claude/`, and an empty harness roster, and the prohibition on a second always-on instruction source still fires -- a `CLAUDE.md` written at the root is reported as `unexpected-instruction-source` and exit `1`, and removing it returns exit `0`.
- [x] `[AI] [AC-11]` Input: HIPPO's existing gates. Action: wire RHINO into its quick gate and pre-push, and confirm every gate HIPPO already had still passes unchanged. Outcome: adoption is additive. Proof: HIPPO's full existing suite passes; an exact search shows nothing was removed. **Done 2026-09-08.** Wired into `scripts/test-quick.sh`, which is what `.husky/pre-push` already runs, so pre-push needed no edit -- the additive change reaches the hook through HIPPO's own composition rather than by adding a second entry point. The six leaves run together and the block waits for the slowest, resolving the pinned binary once via `--bootstrap-exec`. `rhino` was added to the `shfmt` list in `scripts/format-check.sh` because it is a root shell entrypoint exactly as `hippo` is, and it passes. **Additive, checked by diff rather than asserted:** the only removed lines anywhere are the one comment sentence and the one `shfmt` argument list that were replaced in place; every existing command survives untouched. **HIPPO's full quick gate passes end to end** -- format, `go test -run '^$' ./...`, golangci-lint with 0 issues, unit tests, 99.17% selected production line coverage (597/602), all three BDD adapters over 223 scenarios, the artifact policy suite, and then the new documentation block, all clean. **One measurement caveat worth recording: HIPPO's own suite cannot run inside a HIPPO lease.** Wrapping the gate in `./hippo run --class ephemeral` failed `TestPayloadCannotInheritLifetimeIdentityDescriptors` with "reservation capacity remained exhausted through the bounded wait", because the suite spawns HIPPO payloads that contend with the outer lease. That is an artifact of how it was invoked, not a defect, and it is why HIPPO's own `pre-push` calls `npm run test:quick` directly.
- [x] `[HUMAN] [AC-11]` Input: the verified HIPPO change. Action: authorize the commit and push in that repository. Outcome: the change lands under its own history. Proof: the pushed commit. _This is `[HUMAN]` because it is a separate repository and authorization does not carry across repository boundaries._ **Done 2026-09-08 at `ba4f502`, pushed to `wahidyankf/hippo`.** Authorization was requested and given explicitly for this repository rather than inferred from this plan's own approval. The full pre-push gate ran on the push and passed, including the new documentation block.
- [x] **Blocking checkpoint — Phase 9:** `[AI] [AC-04, AC-11]` Confirm HIPPO validates clean, its existing gates are untouched and passing, nothing was removed, the mutual pin between HIPPO and RHINO is documented on both sides, and the change is pushed. **Proof, 2026-09-08, at `ba4f502` in `wahidyankf/hippo`.**

  **Validates clean.** Six leaves, all exit `0`, each reporting what it inspected: 1 configuration file, 0 word-budgeted files, 2 directory maps, 152 links, 0 diagrams, 0 harnesses.

  **Existing gates untouched and passing.** `npm run test:quick` runs green end to end: format check, `go test -run '^$' ./...`, golangci-lint 0 issues, unit and release tests, 99.17% selected production line coverage, three BDD adapters over 223 scenarios and 679 steps, and the artifact policy suite.

  **Nothing removed.** The complete set of deleted lines across the whole change is two: one comment sentence and one `shfmt` argument list, both replaced in place rather than dropped. Everything else is new files and appended lines.

  **The mutual pin is documented on both sides.** HIPPO's `README.md` now names RHINO, its lock file, and the fact that RHINO guards its own builds with a pinned `./hippo`; RHINO's `README.md` already named HIPPO for the same reason. Neither is an accident either reader can "resolve".

  **Pushed**, with the full gate running on the push.

  **One thing this phase did not do, recorded rather than left implicit:** the wrapper's 25-scenario behavioural suite lives in BeaverNest, so HIPPO ships an identical `rhino` guarded there by `shellcheck` and `shfmt` only. If HIPPO's copy is ever edited in place, BeaverNest's suite will not notice. grind-in-public inherits the same gap in Phase 10, and it is the clearest follow-on this adoption surfaces.

## Phase 10 — grind-in-public: Adoption, Cutover, and Retirement

The second cutover. Its validator was written independently in Go, so its findings are the ones most likely to disagree in interesting ways — which is why it runs last, with three repositories of evidence already behind the tool.

- [x] `[AI] [AC-01, AC-04]` Input: the Phase 5 draft and the published release. Action: add `repo-config.yml`, `./rhino`, and `rhino.lock` to grind-in-public. Outcome: the pinned binary is available alongside the existing validator. Proof: cold-cache install verified against the lock. **Proof:** All three added at that repository's root, the wrapper copied unchanged from here -- it names no consumer, so nothing had to be adapted. Cold-cache proof at the final pin: the cache deleted entirely, then `./rhino version --json` returned `{"schemaVersion":1,"version":"v0.1.3","commit":"7d14ff5..."}` in **0.77 s**, matching `rhino.lock` exactly, with the cache afterwards holding `v0.1.3` and nothing else. The full six-leaf gate then ran clean: **116 word-budgeted files, 895 links, 3 harnesses over 1 skill and 2 agents, 0 diagrams, 0 mapped directories**. **Three values are empty rather than borrowed.** `governance-directory-map.trees` is `[]` because badak-mini has no directory-map check and this plan does not impose one; the three Mermaid palettes are `[]` because badak-mini has no diagram check, so the first `classDef` anyone adds fails until that repository picks its own colours; and no harness declares a `capability` or `required-mcp`, because that repository reconciles adapters and routes but has never declared a capability server. Each is a policy statement it can read, not a gap.
- [x] `[AI] [AC-02, AC-05]` Input: both validators. Action: run each over the repository and reconcile every difference, fixing the tool or the configuration but never the governed content, and investigating each disagreement rather than assuming a RHINO defect. Outcome: two independent implementations agree on one tree. Proof: the recorded reconciliation; any tool change re-verified against the other three trees. **Proof:**

  **Baseline, badak-mini.** All three hygiene checks clean; `capability-parity` reports **3 harnesses, 1 skill, 2 agents**, digest `a01c95f28fc51a88…`.

  **RHINO, after one configuration fix.** Identical counts, identical digest inputs, clean. The fix was mine, not the repository's: my `skill-route` and `agent-route` templates omitted the backticks that badak-mini's Go format strings put around `{path}`, so seven adapters read as drifted. The routes in the repository were never touched.

  **Injected-violation sweep, both validators, eight kinds.** Over-budget file 1/1; broken internal link 1/1; drifted adapter 1/1; second `AGENTS.md` 1/1; canonical `mode` changed 1/1; undeclared adapter 1/1; codex pins a model 1/1; opencode grants what the canon denies 1/1.

  **Two asymmetries, both investigated rather than assumed.**

  | Injected                                                  | badak-mini | RHINO                    | Disposition                                                                                                                                                  |
  | --------------------------------------------------------- | ---------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
  | An adapter grants **less** than the canon requires        | exit `0`   | exit `1`                 | RHINO is stricter, and correctly: a non-weakening check is the whole point of the translation table. Recorded as a gain, not a difference to reconcile away. |
  | `opencode.json` declares a non-empty `instructions` field | exit `1`   | exit `0` (before v0.1.3) | A real gap in badak-mini's favour. Closed upstream rather than dropped — see the v0.1.3 row below.                                                           |

  No governed content was changed in either direction.

- [x] `[AI] [AC-06]` Input: both validators. Action: compare word counts file by file across every governed file in that repository. Outcome: the numeric gate is proven not to have drifted there either. Proof: zero differing files; any difference stops the line. **Proof:** **116 files, zero differing, equal totals at 36,235 words.** `AGENTS.md` reads 749 against a 750 budget in both. The comparison is against Go's real `strings.Fields`, which is literally badak-mini's one-line definition of a word, so the two are not being reconciled through a third interpretation of the rule.
- [x] `[AI] [AC-02, AC-05]` **Added during execution.** Input: the one gap the reconciliation found in badak-mini's favour. Action: close it upstream as `harness-parity.prohibited-instruction-fields` rather than dropping it, release RHINO v0.1.3, and move all three consumer pins. Outcome: the cutover loses no check. Outcome check: the rule is general, not repository-shaped -- "a harness's settings key carrying always-on instructions" is "no second `CLAUDE.md`" in that vendor's syntax -- so it belongs in the tool rather than in one repository's script. Proof: the scenarios, the mutants that kill them, the published release, and the three moved pins. **Proof:**

  **Ten scenarios, twelve mutants, every mutant killed by exactly one of them.** The corpus gained two Scenario Outlines over the shapes a configuration value can take -- a non-empty list, string, table, and bare number are a source; an empty list, blank string, empty table, and `null` are the repository saying it carries none -- plus a field nobody wrote, a settings file nobody wrote, a file unreadable in its declared format, and both directions in TOML. Coverage: **99.38%** against the 99% floor, up from 98.97% while the new arms were unproved.

  **Two scenarios exist because a mutant survived.** Reading a TOML settings file with the JSON parser still produced `unexpected-instruction-source` -- via the "not readable in its declared format" arm, the same kind for a different reason -- so the TOML violation scenario passed for the wrong reason until a clean-direction sibling was added. Separately, narrowing the read set in v0.1.2 had left "a file a prohibited glob names that holds no text is skipped rather than refused" carried by a comment and no test; that arm is now a scenario.

  **Released and pinned.** `v0.1.3` at commit `7d14ff5...`, four platforms, checksums verified. All three consumers moved in one step and each `./rhino version --json` matches its lock. Every repository re-ran clean afterwards: BeaverNest **540 links / 57 files / 46 maps / 30 diagrams / 3 harnesses**, HIPPO **152 links / 2 maps**, grind-in-public **895 links / 116 files / 3 harnesses**.

  **The gap is closed where it was found.** With `"instructions": ["extra.md"]` injected into grind-in-public's `opencode.json`, both validators now exit `1`: badak-mini with `unexpected-instruction-source: opencode.json:instructions: must be empty`, RHINO with ``opencode.json: unexpected-instruction-source: `instructions` carries instructions that compete with the canon``. Restoring the file returns both to `0`.

- [x] `[AI] [AC-05]` Input: the reconciled result. Action: repoint grind-in-public's Nx targets and hooks from `badak-mini`'s three hygiene commands to the RHINO equivalents, leaving its project-contract, governance-structure, workflow-contract, spelling, HIPPO bootstrap, and `harness rule-change` checks untouched. Outcome: only general hygiene changes owner. Outcome check: `rule-change` reads staged paths and stdin payloads and triggers that repository's own workflows, so it is outside RHINO's read-only, process-free posture and is not a candidate for replacement. Proof: an exact search shows every non-hygiene check still invoked — including `badak-mini harness rule-change validate` and its `hook` form — and the repository gate passes. **Proof:**

  **Three commands changed owner, and only three.** `scripts/rhino-gate.sh` now holds the six-leaf gate under one `./rhino --bootstrap-exec`, beside that repository's other repository-level scripts. `package.json`'s `check:governance`, `check:harness-parity`, and `check:markdown-links` name RHINO leaves directly (with a new `check:hygiene` for all six at once); `test:repo`'s first three commands collapse to one; `.husky/pre-push`'s whole-repository link check is now `./rhino md internal-link validate` under the same HIPPO guard; and the three `badakmini-cli` Nx targets they used are gone. **No fifth Nx project was created**: that repository's `scripts/project-contract.mjs` declares a fixed four-project workspace, so a new project would either sit outside its own governance or force eleven meaningless targets onto a wrapper. A repository-level script needs neither.

  **Everything else still runs, by exact search.** `test:repo` still invokes project-contract, governance-structure, and workflow-contract (test and check halves each), the HIPPO bootstrap suite, the pre-push contract suite, the Go suite, and the British-spelling sweep. `badak-mini harness rule-change validate` is still wired at six places -- `.husky/pre-commit`, `package.json`, its Nx target, `.claude/settings.json`, `.codex/hooks.json`, and the opencode plugin -- and its `hook` form with it. The gate passes: `nx run badakmini-cli:test:repo` succeeded in **17.8 s** under HIPPO.

  **One test moved with the hook it tests.** `.github/scripts/test-pre-push-contract.sh` asserts the hook's exact invocation lines, so its `markdown-links` expectation became `./rhino md internal-link validate`; that suite passes, and it is the reason the hook change is checked rather than assumed.

- [x] `[AI] [AC-05]` Input: the working consumer. Action: delete only the three retired hygiene packages — `internal/governance/`, `internal/markdownlinks/`, and `internal/parity/` with their tests, their CLI wiring, their Nx targets, and their bindings in both modules — then perform the authority cutover verification: cold cache, fresh process, the retired checks absent, then one injected violation of each kind detected in a scratch copy. Outcome: RHINO is authoritative for hygiene there and provably checking. Outcome check: `internal/rulechange/` shares the module, so neither Go module, neither `go.mod`, nor the Go toolchain setup is removed; a task that deletes them has deleted a check this plan never intended to replace. Proof: the clean cold run plus each injected violation detected; `badak-mini harness rule-change validate` still passing afterwards; the scratch copy removed.

  **Only the three packages went, and the module stayed.** `internal/governance/`, `internal/markdownlinks/`, and `internal/parity/` are gone with their tests, their integration tests, their three `.feature` files, their bindings in both modules, and their three Nx targets; `internal/rulechange/`, both `go.mod` files, and the Go toolchain setup are untouched. `go vet ./...` and `go test ./...` are clean in both modules, `nx run-many -t test:quick` succeeds for all four projects, and `nx run badakmini-cli:test:coverage` reports **100.0%** unit and **100.0%** integration statement coverage against the unchanged 99% floor.

  **The deletion broke six links, and RHINO said so.** `specs/apps/badakmini-cli/README.md` and `specs/apps/badakmini-cli/behaviours/README.md` still indexed the three removed features; `[internal-link] checked 895 links, 6 findings` named each line. Removing the six stale entries took the tree to `checked 889 links, no findings` -- the retirement's own documentation rot, found by the tool that replaced the validator.

  **A retired name exited `0`, and that was the real hazard.** Probing the three retired commands against a fresh build returned exit `0` with the group's help on stdout: Cobra answers an unknown subcommand of a group that only holds subcommands with that group's help, because it returns `ErrHelp` for a non-runnable command _before_ it validates arguments. A stale hook still calling `badak-mini harness instruction-size validate` would have reported a green gate having checked nothing. Fixed under the project rule -- scenario `A command name this CLI does not have is an invalid invocation` written first, bound in both adapters, red at the unit and integration boundaries, then `commandGroup` given `Args: cobra.NoArgs` **and** a `RunE` that prints help, since the argument rule is unreachable while the group cannot run. All three retired names now exit `2` with empty stdout; `harness`, `harness --help`, and every live leaf are unchanged, and `harness rule-change bogus` exits `2` for the same reason.

  **Cold cache, fresh process, nothing to fall back on.** `~/Library/Caches/rhino` was removed outright and no `rhino`, `badak-mini`, or `badakmini` executable is on `PATH`. A fresh process then ran the whole six-leaf gate from `repo-config.yml` and `rhino.lock` alone: **clean in 1 s including the download**, with the cache afterwards holding exactly one directory, `v0.1.3`. Counts: 1 configuration file, 116 governed files, 889 links, 0 directories, 0 diagrams, 3 harnesses, digest `00e495ebc7eff35fddf1708cc3e7418d8b995c0aa9ba3425eae9a3e0e6873633`.

  **Then "clean" was proved to mean "checked."** A working-tree copy under `local-tmp/cutover` -- the working tree, not `git archive HEAD`, because the deletions are uncommitted -- was confirmed clean first, then took one injected violation at a time, each restored before the next: **word budget**, `repo-governance/conventions/language-policy.md: is longer than its declared word budget (words=800, limit=750)`; **internal link**, `docs/README.md:36: ./no-such-document.md does not exist`; **Mermaid**, `README.md:88: a node label is longer than the declared limit (segment=node, measured=67, limit=32)`; **harness parity**, `.codex/agents/repo-explorer.toml: agent-prompt-divergence: developer_instructions is not the canonical route to repo-explorer`; **prohibited instruction field**, `opencode.json: unexpected-instruction-source: instructions carries instructions that compete with the canon`. Each exited `1` naming the exact path, and the tree returned to exit `0` after every restore. **Directory map is dormant by declaration, not by failure:** that repository declares `trees: []` on purpose, so there is nothing for it to check and no violation to inject -- recorded rather than glossed, because a check with an empty subject reporting "no findings" is the exact confusion this verification exists to prevent.

  **The check that was never in scope still works.** In the same scratch copy the three retired names exited `2` with empty stdout while `badak-mini harness rule-change validate` printed both notices for a staged `AGENTS.md` and `harness rule-change hook` returned its 628-byte `PreToolUse` response. The scratch copy was then removed, `local-tmp/` holds only pre-existing fixtures, and the main tree re-runs the gate clean.

- [x] `[AI] [AC-05]` Input: grind-in-public's deleted hygiene packages and its Git history. Action: rehearse that repository's rollback on the same terms — restore the three deleted packages, their bindings in both modules, and the previous hooks into a scratch worktree, run its retired Go gate there, and confirm the prior clean result. Outcome: the second retirement's recovery path is proven too, independently of the first. Proof: the retired gate's clean output from the scratch worktree, and the worktree removed afterwards.

  **The rollback target is `HEAD`, because the cutover is not committed yet.** `git worktree add --detach local-tmp/rollback HEAD` restored the whole pre-cutover tree in one step: `internal/governance/`, `internal/markdownlinks/`, and `internal/parity/` back beside `internal/cli/` and `internal/rulechange/`, the bindings in both modules, the three Nx targets (`go run ./cmd/badak-mini harness instruction-size|markdown-links|capability-parity validate`), `package.json`'s `check:governance`, `check:harness-parity`, and `check:markdown-links` pointing at them, and `.husky/pre-push` back to `nx run -p badakmini-cli -t markdown-links`.

  **The retired gate returned its prior clean result there.** `harness instruction-size validate` -> exit `0`, _Governance word counts are within the 750-word limit._; `harness markdown-links validate` -> exit `0`, _Repository-local Markdown links are valid._; `harness capability-parity validate` -> exit `0`, _Harness contract passed: 3 harnesses, 1 skill, 2 agents, `sha256:a01c95f28fc51a88f4939de079abb1b756a4ff33be5757ff4f1be59125cabbd0`._ The two digests differ by definition and not by disagreement -- Phase 10's reconciliation already established that the Go contract hash and RHINO's `00e495eb...` canon digest hash different inputs; the finding sets are what were compared, and they matched. Both Go modules were green in the worktree as well: **205 tests** across nine packages in the CLI module, and the E2E module's **2 tests** against the restored binary.

  **The worktree is gone.** `git worktree remove --force local-tmp/rollback`; `git worktree list` shows only the repository itself, and `local-tmp/` holds no rollback or cutover directory.

- [x] `[AI] [AC-07]` Input: the changed enforcement mechanism there, with its new target already working. Action: run one transaction of grind-in-public's own `repo-governance/workflows/rules/rules-propagation.md` — its inventory, canonical-home, conflict-resolution, and idempotency-gate sequence — across the thirteen surfaces naming its Go validator, including its own workflow's embedded gate command. Outcome: that repository's rules name RHINO, under its own governance rather than this plan's. Outcome check: closed against its hierarchy, not this repository's; the two transactions are not required to reach the same wording. Proof: the recorded terminal result and an exact search finding no live reference.

  **Terminal result: `PASS_CHANGED`.** One transaction of `repo-governance/workflows/rules/rules-propagation.md`, run under that repository's own hierarchy.

  **The ledger came out much smaller than the thirteen surfaces, and the reason is the point.** The inventory found 188 matches in 48 files, but most were already closed by construction: every `npm run check:governance`, `check:harness-parity`, and `check:markdown-links` invocation across the workflows, the word-limit policy, the naming policy, the architecture-specification policy, and the how-to guide is still correct, because the script names did not change -- only what they run. `repo-governance/conventions/document-word-limit-policy.md` needed **no edit at all**: it names the check and the governed surfaces and never named the implementation. A repository that documents its rules by the command rather than by the binary absorbs a validator swap almost entirely in one file.

  **Nine rows were `OPEN`; all nine closed.** `README.md` (Badak Mini described as the CLI behind four checks; RHINO undocumented anywhere in the repository despite being wired at five places); `apps/badakmini-cli/README.md` (three commands it no longer has, plus their three describing paragraphs and their three `Run and Verify` lines); `repo-governance/development/badakmini-cli-policy.md` (scope claimed instruction-size and Markdown-link validation); `repo-governance/development/workspace-commands.md` (a narrower-run line naming the deleted `./internal/governance`; the undocumented `check:hygiene`; and the closing sentence assigning every `check:` command to Badak Mini); `repo-governance/conventions/agent-vocabulary.md` (its example of the `harness` group's naming was `harness instruction-size validate`); `repo-governance/conventions/harness-capability-parity-policy.md` (its Deterministic Verification paragraph described the Go validator's output and exclusions); `specs/apps/badakmini-cli/architecture.md` (a three-way component fan-out to validators that no longer exist); and `specs/README.md` plus `specs/apps/README.md` (both said "five-feature corpus"; it is two).

  **Four rows were `NOT_APPLICABLE`, each for a stated reason.** `plans/done/**` is archived history and is not repaired retroactively. `repo-config.yml`'s comments name `internal/governance`, `internal/markdownlinks`, and `internal/parity` deliberately -- they record where each declared value was transcribed from, which is the provenance that makes the transcription auditable. `internal/cli/run_test.go`, `tests/integration/coverage_test.go`, and `cli-contract.feature` each name a retired command on purpose, to prove it is now rejected. And the nine harness READMEs linking to the parity policy stay correct because the policy itself stays -- only its verification paragraph changed.

  **The canonical home moved with the authority.** `workspace-commands.md#repository-checks` now states that RHINO implements the hygiene checks from `repo-config.yml` through the `./rhino` consumer pinned in `rhino.lock`, and that Badak Mini implements `check:rule-change`; every other surface links there instead of repeating it. The badak-mini README's copy of the `harness`-group naming rationale was dropped rather than corrected, because `agent-vocabulary.md` is its canonical home.

  **One observation for the owner, not repaired here.** `workspace-commands.md` was already at 709 of its 750 words before this transaction and is now **733** -- inside the policy's own 700-word headroom band. It is under the cap and the gate passes; a split is a separate rule transaction, not this one's minimum repair.

  **Step 4 and the idempotency gate.** `nx run -p badakmini-cli -t test:repo` under HIPPO: **17.4 s, cold cache, successful**, and the RHINO gate reports `checked 890 links, no findings` -- one more link than before, the new pointer from the architecture model to the repository-checks reference. Exact searches then find **no live reference** to `internal/governance`, `internal/markdownlinks`, or `internal/parity`, and **no live reference** to the three retired Nx targets. The single surviving mention of a retired command name is the scenario that asserts it is rejected.

- [x] `[HUMAN] [AC-05]` Input: the verified grind-in-public change. Action: authorize the commit and push in that repository. Outcome: the cutover lands under its own history. Proof: the pushed commits. _This is `[HUMAN]` for the same repository-boundary reason as Phase 9._

  **Authorized in advance and pushed: `3116d61..fb03e69`.** Three thematic commits, each carrying its own reason:
  `d6abe35 feat(rhino): adopt the pinned RHINO consumer for documentation hygiene` (the configuration, the consumer,
  the lock, the gate script, the scripts and hooks that call them); `ce9ac2f refactor(badakmini-cli)!: retire the three
hygiene validators` (the deletion, the retired-name fix, and the two coverage gaps the shrunken denominator exposed);
  `fb03e69 docs(governance): name RHINO where the Go validator was named` (the propagation transaction). The breaking
  change is declared where it happened, on the commit that removed the commands, and names the replacement.

  **The pre-push hook caught what my own gate run had not.** The first push attempt failed on `badakmini-cli:lint`: a
  122-character line in `tests/integration/coverage_test.go`, added _after_ the last full quick gate and touched by
  neither `test:coverage` nor `test:repo`. Nothing had been pushed, so the fix went into the commit that introduced the
  line rather than a follow-up: `git reset --soft` to the first commit, repair, uncached `nx run-many -t test:quick`
  green for all four projects, then the two commits rebuilt from their saved messages. No hook was bypassed at any
  point. The second push ran the whole hook -- affected `test:quick`, guarded `test:repo` in **16.5 s**, and
  `[internal-link] checked 890 links, no findings` -- and succeeded.

- [x] `[AI] [AC-12]` Input: the Phase 1 grind-in-public baseline and the completed cutover. Action: re-measure by the same method and compare, holding the same two cutover thresholds — warm gate no slower, peak memory no higher. Outcome: the Go-to-Rust comparison is recorded honestly. Outcome check: a comparable-performance result is the expected outcome there and is recorded as such — the win in that repository is one shared implementation instead of its own, not raw speed. Proof: the before-and-after rows in `learnings.md`.

  **Both thresholds hold.** Warm gate as wired: **0.373 s -> 0.271 s (0.73x)**, doing six checks where badak-mini did three. Peak resident memory: **12.1 MiB -> 8.2 MiB (0.68x)**, also across six checks rather than three. Measured with `bench-grind-after.sh`, which reuses the baseline's own `python3` timestamp brackets, one `./hippo run --class ephemeral` lease, and the discard-the-first-two rule, so the two sides are comparable to each other and to nothing measured a different way.

  **The speed figure is not a language win, and is not recorded as one.** The baseline predicted exactly this: the wired path compiled through `go run` on every invocation, and a pinned binary removes that step. Against the _built-binary_ baseline of 0.236 s, RHINO is 15% slower -- and that comparison is itself generous to RHINO, because it is six checks against three. The comparable-performance outcome the plan set as the expectation for this repository is what happened.

  **The number that matters is the maintenance one.** `apps/badakmini-cli` went from 41 files and 8,878 lines (7,264 Go) to 32 files and 4,895 lines (3,315 Go): **3,949 lines of Go retired**, against 836 lines of consumer added, 597 of which are the `rhino` bootstrap three repositories now share. Net **-3,147 lines**. The Go toolchain is unchanged on disk by design, because `rule-change` still needs it.

- [x] `[AI] [AC-05, AC-06]` **Recovery, only if triggered:** trigger is grind-in-public's gate failing after cutover for a reason not attributable to genuinely non-compliant content, or a word count that changed there. Action: restore the three deleted hygiene packages, their bindings in both modules, and the previous hooks from that repository's Git history, then re-run its prior gate. This rollback touches only grind-in-public and never a published RHINO tag. **Disposition: not triggered.** The trigger is that repository's gate failing after cutover for a reason not attributable to genuinely non-compliant content, or a word count that changed there. Neither occurred: the gate is clean cold and warm at `fb03e69`, and Phase 10's word-count comparison found no file whose count moved. The two failures that did occur during delivery were both genuine and both fixed forward -- six spec-README links to deleted `.feature` files, which is non-compliant content and exactly what the check exists to find, and one 122-character line caught by `badakmini-cli:lint` on the first push attempt. Recovery was nevertheless rehearsed rather than assumed: see the rollback row above.
- [x] **Blocking checkpoint — Phase 10:** `[AI] [AC-02, AC-05, AC-06, AC-07, AC-12]` Confirm both validators agreed before retirement, word counts matched, only hygiene checks changed owner, the three hygiene packages are gone while `rule-change` and its module remain, that repository's rollback was rehearsed, injected violations are detected, that repository's own propagation returned a recorded terminal result, and the change is pushed. **Proof:** each of the eight confirmed against the tree rather than against this document.

  **Both validators agreed before retirement.** Phase 10's reconciliation compared finding sets, not messages, and the one gap it found -- badak-mini catching an `opencode.json` `instructions` field that RHINO could not see -- was closed upstream in v0.1.3 rather than waived. With that field populated, badak-mini reports `unexpected-instruction-source: opencode.json:instructions: must be empty` and RHINO reports ``opencode.json: unexpected-instruction-source: `instructions` carries instructions that compete with the canon``; both exit `1`, both return to `0` on restore.

  **Word counts matched**, file by file, under both binaries, with both definitions declared rather than inherited: `count: whitespace-separated` here because `internal/governance/check.go` used `strings.Fields`, against BeaverNest's different definition.

  **Only hygiene changed owner.** `test:repo` still runs project-contract, governance-structure, workflow-contract, the HIPPO bootstrap suite, the pre-push contract suite, the Go suite, and the British-spelling sweep; the repository-specific checks the plan scoped out are all still there.

  **The three packages are gone and `rule-change` is not.** `apps/badakmini-cli/internal/` contains exactly `cli/` and `rulechange/`; both `go.mod` files survive; and `harness rule-change` is still wired at all six places -- `.husky/pre-commit`, `package.json`, its Nx target, `.claude/settings.json`, `.codex/hooks.json`, and `.opencode/plugin/rule-change-notice.js`.

  **The rollback was rehearsed** in a scratch worktree, which returned the retired gate's prior clean output and was then removed.

  **Injected violations are detected** -- one of each of the five checkable kinds, each exiting `1` and naming its path, with directory-map recorded as dormant by declaration rather than passed off as clean.

  **The propagation returned `PASS_CHANGED`**, with nine `OPEN` rows closed, four `NOT_APPLICABLE` rows each given a reason, and exact searches afterwards finding no live reference to any deleted package or retired Nx target.

  **The change is pushed:** `3116d61..fb03e69`, three thematic commits, working tree clean and `HEAD` equal to `origin/main`, with the full pre-push hook run and no hook bypassed at any point.

## Phase 11 — Verification, Cleanup, and Reconciliation

- [ ] `[AI] [AC-05, AC-07, AC-09]` Input: the complete change. Action: run the full uncached repository gate, the affected `test:quick` graph, and the bootstrap suite here; upstream's full suite including its scheduled integration and E2E targets; and each sibling repository's own full gate. Then confirm the routed origin still serves the baseline revision and is healthy. Outcome: all four repositories are green and the household service is untouched. Proof: each gate's result per repository, and the routed health and revision.
- [ ] `[AI] [AC-05, AC-09]` Input: the working trees and machine. Action: remove every scratch copy and rehearsal worktree under `local-tmp/` in each repository, any throwaway upstream tag or release, temporary caches created for cold-start testing, and any watcher or server started during delivery; confirm no non-production process remains. Outcome: nothing temporary survives the plan. Proof: exact searches showing each artifact absent.
- [ ] `[AI] [AC-12]` Input: every measurement taken during delivery. Action: assemble one before-and-after table in [the benchmark design](tech-docs/05-benchmarks.md) covering both cutover repositories, every metric, and the four-platform distribution figures. Outcome: the plan closes with its evidence in one place instead of scattered across dated entries. Proof: the completed table, with each figure traceable to a dated `learnings.md` entry and its conditions.
- [ ] `[AI] [AC-01]` Input: delivery evidence. Action: capture learnings, then search `plans/ideas/` for overlap with `ose-public` and `ose-private` adoption — including the unresolved question of merging with `ose-public`'s own `rhino-cli` — and either merge into an existing brief or create one distinct mapped brief, linked from `learnings.md`. Outcome: the follow-on work is recorded where the next reader will find it. Proof: the merged or created brief and its map entry.
- [ ] `[AI] [AC-01 … AC-12]` Input: final evidence. Action: reconcile every acceptance criterion, checklist item, specification, document, rule, and map with what was actually done; then archive by collision-safe dated move with the stage-index and map updates in the same change. Outcome: delivery history and as-built truth agree. Proof: the destination collision check and the final repository gate accompanying the archive move.
- [ ] `[AI] [AC-04, AC-05, AC-06]` **Recovery, only if triggered:** trigger is the repository gate failing after cutover for a reason not attributable to genuinely non-compliant content, a bootstrap that cannot verify the pinned release, or a governed word count that changed across the rewrite. Action: restore `apps/badakmini-cli/`, `apps/badakmini-cli-e2e/`, `beaver-nest.sln`, and the previous `.husky/pre-push` from Git; re-run the Badakmini gate; diagnose before retrying. Never edit or delete a published upstream tag — a defect becomes a new patch version and a new pin. **Disposition:** —
- [ ] **Final checkpoint:** `[AI] [AC-01 … AC-12]` Confirm every acceptance criterion has evidence, all four repositories are green, both retirements are complete with their rollbacks dormant and dispositioned, the routed service is unchanged on its baseline revision, nothing temporary remains, and the archive move is verified. **Proof:** —
