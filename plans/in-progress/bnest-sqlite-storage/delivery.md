# Delivery

All product work remains pending until execution is explicitly underway. Each checkbox is independently verifiable; a failed phase checkpoint blocks later phases.

## Phase 1 — Baseline and Contracts

- [ ] `[AI] [AC-01] [AC-08]` Input: active Bnest route, Caddy state, current revision, storage root, specifications, and this plan. Action: run the plan quality gate, record value-free local/routed health and revision, inventory active/previous slots and supported source templates without values, and stop if health or inventory is unsafe. Outcome: implementation begins from a healthy, known flat-primary baseline. Proof: safe checkpoint note, `proxy:status`, ready endpoint, and structural inventory categories.
- [ ] `[AI] [AC-01] [AC-04] [AC-05] [AC-08]` Input: PRD and Specification Changes. Action: update C4 and Gherkin first, implement every new/changed scenario in unit and integration behavior bindings, add E2E bindings, then run behavior coverage to capture the required red evidence before production code. Outcome: executable contracts fail only for missing SQLite behavior. Proof: exact failing scenarios and `bnest-app:test:coverage:behaviour` result.
- [ ] `[AI] [AC-03]` Input: current Elixir version and official Ecto SQLite compatibility. Action: resolve and review `ecto_sqlite3`/Ecto/Exqlite dependencies, lock compatible versions, and document native build/runtime requirements. Outcome: one supported adapter stack is reproducible from `mix.lock`. Proof: dependency tree, license/security review, clean compile, and unused-dependency check.
- [ ] **Blocking checkpoint — Phase 1:** `[AI] [AC-01] [AC-03] [AC-08]` Confirm green baseline remains routed, specifications and bindings are mapped, expected red is understood, dependencies are approved, and no real data entered evidence.

## Phase 2 — Schema and Repository Expansion

- [ ] `[AI] [AC-03]` Input: SQLite Schema Contract. Action: add `SQLiteRepo` and committed `20260829000000_create_bnest_storage.exs` with exact tables, constraints, indexes, explicit guarded down behavior, and migration checksum projection. Outcome: an empty database reaches schema v1 once and a second run is idempotent. Proof: DDL-twice integration test, migrated-version query, schema inspection, and checksum fixture.
- [ ] `[AI] [AC-02] [AC-07]` Input: Storage Configuration Contract. Action: implement path expansion, private pointer encoding, permissions, ownership/symlink/overlap checks, fixed filename, and value-free errors. Outcome: only an authorized safe server-local directory can be persisted. Proof: unit boundary matrix and integration permission tests.
- [ ] `[AI] [AC-03] [AC-05]` Input: current repository facade and schemas. Action: add backend behavior, canonical JSON, SQLite record mapping, transactions, stale-revision checks, and dynamic storage coordinator while preserving public return shapes. Outcome: all record types pass the same repository contract on flat and SQLite adapters. Proof: shared contract suite, concurrent-writer tests, and schema validation read-back.
- [ ] `[AI] [AC-01] [AC-03] [AC-08]` Input: managed release migration boundary. Action: extend artifact manifests and release tests with default-path resolution, DDL checksum, adapter ID, schema range, flat-primary compatibility, lock exclusion, SQLite readiness, and guarded rollback eligibility. Outcome: migration uses `~/.config/bnest/` without UI when no custom pointer exists and is never hidden in ordinary startup. Proof: release state-machine unit tests and `bnest-app:release:test`.
- [ ] **Blocking checkpoint — Phase 2:** `[AI] [AC-02] [AC-03] [AC-05]` Confirm the committed migration reproduces schema v1, both backends pass shared contracts, unsafe paths mutate nothing, and ordinary startup applies no DDL.

## Phase 3 — Idempotent Backfill and Recovery

- [ ] `[AI] [AC-04] [AC-06]` Input: exact source inventory and key mapping. Action: implement `flat-files-v1-to-sqlite-v1` with ordered inventory, immutable byte checksums, source fingerprint, transactional per-item evidence, normal read-back, retry, and safe unsupported/invalid/changed outcomes; expose the same module through the UI and `bnest-app:storage:migrate` Nx target. Outcome: every supported source is accepted once or blocks activation visibly without mutation, and the run is reproducible without the browser. Proof: all-record fixture, interruption at every transition, second-run no-op, collision, changed-byte, malformed, unknown-version, headless-task, and privacy tests.
- [ ] `[AI] [AC-04] [AC-05] [AC-06]` Input: completed backfill. Action: implement full parity, `PRAGMA quick_check`, SQLite-native backup/`VACUUM INTO`, isolated restore, and exact cleanup guards. Outcome: cutover cannot proceed without equivalent normal reads and a usable restored copy. Proof: parity mismatch injections, integrity result, restore read suite, and verified removal of only the marked test destination.
- [ ] `[AI] [AC-05] [AC-08]` Input: overlap and rollback constraints. Action: implement bounded final-write gate, atomic phase pointer, compatibility mirror or transactional outbox, pending-parity health, and rollback guard. Outcome: acknowledged writes cannot silently make a retained rollback reader stale. Proof: fault injection before/after primary commit, concurrent old/new writers, mirror retry, restart, and rollback eligibility tests.
- [ ] **Blocking checkpoint — Phase 3:** `[AI] [AC-04] [AC-05] [AC-06]` Confirm complete deterministic inventory disposition, idempotent retry, parity, integrity, isolated restore, and no authority switch on any injected failure.

## Phase 4 — Storage UI

- [ ] `[AI] [AC-01] [AC-02] [AC-07]` Input: selected ledger assets and current auth boundaries. Action: implement an authenticated admin-only storage LiveView for pre-migration folder override and post-start status, with server-host explanation, 404/denial before rendering for every non-admin, CSRF, and immutable initialized-location state. Outcome: default migration needs no UI; only users whose current server-side roles include `admin` can open the route or learn its path/status. Proof: LiveView tests and unit/integration behavior bindings for unauthenticated, children, parents, mixed non-admin, revoked session, and admin states.
- [ ] `[AI] [AC-04] [AC-06]` Input: migration coordinator states. Action: render readiness, copy, verify, blocked, retry, and active states with value-free summaries and consistent action labels. Outcome: an admin can understand and recover without private data exposure. Proof: state rendering tests and log/output privacy assertions.
- [ ] `[AI] [AC-01] [AC-02] [AC-04] [AC-06] [AC-07]` Input: all 12 reviewed assets. Action: implement responsive ledger styling, keyboard order, visible focus, live regions, alert focus, zoom/reflow, contrast, reduced motion, and no-color-only status. Outcome: selected design works on desktop, tablet, and mobile. Proof: focused accessibility assertions plus manual AI checks at three devices, 200% zoom, keyboard-only, dark/light, loading, error, retry, and reduced motion.
- [ ] **Blocking checkpoint — Phase 4:** `[AI] [AC-01] [AC-02] [AC-04] [AC-06] [AC-07]` Confirm selected-design fidelity, safe copy, authorization, all UI states, and affected-device accessibility proof.

## Phase 5 — Automated and Manual Verification

- [ ] `[AI] [AC-01] [AC-03] [AC-04] [AC-05] [AC-06] [AC-07]` Input: synthetic storage fixtures. Action: run unit, integration, behavior coverage, typecheck, lint, coverage, and `test:quick` through Nx; resolve all failures. Outcome: application gates are green. Proof: recorded target results without runtime values.
- [ ] `[AI] [AC-01] [AC-02] [AC-04] [AC-05] [AC-06] [AC-07]` Input: isolated E2E runtime and unique `test-user-` identities. Action: run focused exact-origin E2E for default migration without visiting storage UI, optional custom path, interruption/retry, denial, restart, and chat/learning/theme/session continuity; never use production. Outcome: both headless-default and optional-UI journeys are green against a real SQLite file. Proof: focused `bnest-app-e2e:test:e2e` result and clean exact run-root teardown.
- [ ] `[AI] [AC-03] [AC-08]` Input: release and repository changes. Action: run `bnest-app:release:test` and `badakmini-cli:test:repo`, check Markdown links/maps, inspect migration manifest checksum, and manually run `wc -l plans/in-progress/bnest-sqlite-storage/tech-docs.md`. Outcome: release and plan quality gates pass with one technical shape. Proof: target outputs and line-count evidence.
- [ ] `[AI] [AC-05] [AC-06]` Input: isolated migrated database. Action: rehearse forward migration, restart, SQLite-primary reads/writes, guarded app rollback, and restore without a destructive down migration. Outcome: recovery procedure is proven before production mutation. Proof: safe transition IDs and pass/fail categories.
- [ ] **Blocking checkpoint — Phase 5:** `[AI] [AC-01] [AC-03] [AC-04] [AC-05] [AC-06] [AC-07] [AC-08]` Confirm every required automated gate, focused browser journey, schema reproduction, restart, rollback guard, and restore rehearsal is green.

## Phase 6 — Continuity-Safe Production Transition

- [ ] `[AI] [AC-08]` Input: clean authorized `main`, healthy route, machine-local configuration, and verified expand artifact. Action: prepare an independent inactive-slot candidate while flat files remain primary; require candidate revision, readiness, compatible schema range, and direct connected-LiveView proof. Outcome: candidate is safe to promote without storage mutation. Proof: direct health headers and connected exact-origin journey.
- [ ] `[AI] [AC-08]` Input: verified candidate. Action: promote through Caddy, prove local Caddy and routed Tailnet revision, exercise LiveView/WebSocket reconnect without refresh, monitor the five-minute drain, retire the incompatible prior slot, and clean temporary resources. Outcome: only the dual-capable revision can write before migration UI is enabled. Proof: proxy state, routed revision, reconnect state/draft proof, drain result, no inactive listener, and no temporary worktree.
- [ ] `[AI] [AC-01] [AC-02] [AC-04] [AC-05] [AC-06]` Input: healthy dual-capable route and optional pre-migration custom pointer. Action: resolve the custom pointer or default `~/.config/bnest/` without UI, take the migration lock, inventory value-free structure, apply committed DDL, backfill, verify parity/integrity/restore, switch authority, and exercise isolated authenticated reads/writes. Outcome: production SQLite is primary with rollback compatibility; sources remain retained. Proof: migration/checksum/schema IDs and generic outcome categories only.
- [ ] `[AI] [AC-05] [AC-08]` Input: SQLite-primary route. Action: verify restart-independent readiness, local/routed health and intended revision, connected LiveView/WebSocket recovery, chat/learning/theme/login/logout with an isolated authorized test identity, rollback eligibility, and resource headroom. Outcome: active behavior and revision are proven after cutover. Proof: safe routed evidence with no household record use.
- [ ] `[AI] [AC-08]` **Recovery, only if triggered:** Input: candidate/routed/reconnect failure. Action: run managed Caddy rollback to the last healthy compatible backend, verify route and revision, retire failed candidate, and record safe diagnosis before retry. Outcome: service is restored. Proof: rollback status and healthy routed journey. If not triggered, reconcile as `Not triggered` with dated health evidence.
- [ ] `[AI] [AC-05] [AC-06]` **Recovery, only if triggered:** Input: migration or SQLite integrity failure. Action: keep/restore flat-primary authority when safe, preserve all evidence and sources, release bounded locks, verify the current route, and retry only after diagnosis; never delete or overwrite. Outcome: no unsafe cutover or source loss. Proof: authority state, generic failure category, and healthy route. If not triggered, reconcile as `Not triggered` with dated verification evidence.
- [ ] **Blocking checkpoint — Phase 6:** `[AI] [AC-02] [AC-04] [AC-05] [AC-06] [AC-08]` Confirm routed intended revision, SQLite-primary normal journeys, LiveView reconnect, rollback compatibility, retained sources, bounded drain, and cleanup.

## Phase 7 — Documentation and Reconciliation

- [ ] `[AI] [AC-01] [AC-03] [AC-05]` Input: final as-built behavior. Action: update root and project READMEs, configuration commands, storage ownership, migration/recovery operation, `.gitignore`, and exact resolved Nx targets. Outcome: maintainers can reproduce schema and operate storage without exposing paths or data. Proof: README impact checks, ignored runtime files, and link gate.
- [ ] `[AI] [AC-01] [AC-02] [AC-03] [AC-04] [AC-05] [AC-06] [AC-07] [AC-08]` Input: all delivery evidence. Action: rerun the plan quality gate, reconcile acceptance criteria, File Impact, specifications, maps, learnings, recovery dispositions, active services, and all checkboxes. Outcome: the plan truthfully matches repository and runtime state. Proof: clean recursive inventory, green gates, and only the active route plus intentional rollback capacity remains.
- [ ] `[AI] [AC-05]` Input: accepted completion, final local date, and queued flat-file retirement plan. Action: move this folder—not copy it—to collision-free `plans/done/YYYY-MM-DD__bnest-sqlite-storage/`, update stage maps/status/links, verify one destination with no active old-path references, and mark the retirement dependency ready for immediate execution without starting it inside this plan. Outcome: historical delivery is archived and destructive contraction remains separately gated. Proof: destination/source checks, direct archived links, repository diff, dependency link, and final map gate.
- [ ] **Final checkpoint:** `[AI] [AC-01] [AC-02] [AC-03] [AC-04] [AC-05] [AC-06] [AC-07] [AC-08]` Confirm acceptance, data preservation, routed service, cleanup, documentation, learnings, and archive verification before reporting completion.
