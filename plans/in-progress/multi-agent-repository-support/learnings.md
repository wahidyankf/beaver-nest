# Learnings

## Capture Rule

Record only repository-relative paths, public tool versions, official documentation conclusions, aggregate counts, commands, pass/fail, and non-sensitive diagnostics. Never record credentials, provider account data, machine paths, hostnames, session content, model prompts/responses, user-global configuration, or private MCP output.

## Observations

- **2026-08-28 — Current instruction baseline.** Root `AGENTS.md` is the only tracked repository instruction file. `CLAUDE.md` and OpenCode-specific instruction config are absent. Disposition: retain `AGENTS.md` as canonical and add only a strict Claude import adapter.
- **2026-08-28 — Current skill baseline.** Seven tracked skills plus supporting resources live under `.agents/skills/`: `link-workspace-packages`, `monitor-ci`, `nx-generate`, `nx-import`, `nx-plugins`, `nx-run-tasks`, and `nx-workspace`. Disposition: keep this corpus canonical and adapt Claude without copying full bodies.
- **2026-08-28 — Current capability baseline.** `.codex/config.toml` declares the local `nx-mcp` command and one `ci-monitor-subagent`; its detailed prompt is Codex-specific. Disposition: preserve Nx MCP as the only required MCP capability and extract custom-agent intent to a harness-neutral canonical file.
- **2026-08-28 — Harness binaries are locally present.** Read-only version checks reported Codex CLI 0.150.1, Claude Code 2.1.221, and OpenCode 1.18.7. Disposition: use them for later smoke proof if authentication is already available; do not pin versions or record user state.
- **2026-08-28 — Official instruction discovery differs.** Codex and OpenCode load root `AGENTS.md`; Claude Code explicitly recommends a `CLAUDE.md` importing `AGENTS.md`. OpenCode V1 gives `AGENTS.md` precedence over its Claude fallback, while V2 recognizes `AGENTS.md` directly. Disposition: one exact root import adapter avoids rule duplication.
- **2026-08-28 — Skill discovery differs.** Codex and OpenCode support `.agents/skills/`; current Claude Code documentation exposes project skills under `.claude/skills/` and compatible commands under `.claude/commands/`. Disposition: use thin Claude command adapters and validate one-to-one coverage instead of symlinks or copied workflows.
- **2026-08-28 — “Same” needs an honest boundary.** Harness-owned system prompts, models, provider auth, permissions UI, local memory, global plugins, and personal MCP sources cannot be made identical by repository files. Disposition: Badakmini guarantees only the committed repository contract and performs no model/runtime check.
- **2026-08-28 — Existing Badakmini shape fits.** Badakmini already owns read-only filesystem inspection, stable text/JSON output, injectable unit boundaries, local-only integration, process E2E, and `test:repo`. Disposition: add one focused governance leaf and reuse existing architecture rather than creating a standalone script or service.
- **2026-08-28 — OSE Public supplies the requested web-research baseline.** Its Claude, Codex, and OpenCode adapters share a repository-first, primary-source, citation-rich, gap-reporting, read-only workflow while using native metadata per harness. Disposition: adapt those quality invariants into one Beaver Nest canonical `web-researcher` prompt, omit OSE-only skills/links and pinned provider models, and validate thin adapters plus the strongest native read-only controls.
- **2026-08-28 — Phase 0 plan quality gate passed.** Recursive review found the six required plan documents and no overlapping idea/backlog. After the content-parity clarification, the single technical document is 274 lines, below the 401-line split threshold. Uncached `badakmini-cli:test:repo` passed all links, governed word budgets, recursive maps, and Mermaid checks; the staged secret/path scans and `git diff --check` passed after removing Markdown hard-break whitespace. Disposition: planning is complete, implementation remains unauthorized, and every delivery phase after Phase 0 stays unchecked.
- **2026-08-28 — Planning only is authorized.** The user explicitly deferred implementation. Disposition: all delivery work after Phase 0 remains unchecked until a separate execution instruction.
- **2026-08-28 — Parity must cover effective content.** The user clarified that matching paths and names alone are insufficient for rules, skills, and agents. Disposition: normalize and digest canonical bodies/resource bundles, restrict adapters to exact routes, compare native agent configuration through a shared semantic projection, and fail on extra instructions or weakened constraints.
