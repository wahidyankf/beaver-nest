# Delivery

## Execution Rules

Follow [plan execution](../../../repo-governance/workflows/plan-execution.md) and [live-service continuity](../../../repo-governance/development/live-service-continuity.md). The [Caddy deployment workflow](../../../repo-governance/workflows/development-caddy-deployment.md) remains mandatory while Caddy is the active route. The user authorized plan/governance documentation and its commit/push on 2026-08-27, then explicitly deferred implementation. No release source, browser behavior, hook, proxy, process, or active route may change in this iteration.

## Acceptance Traceability

- `[AC-01]` Current condition is accurately recorded.
- `[AC-02]` Alternatives are compared without an unsupported decision.
- `[AC-03]` An open page survives or automatically recovers from release interruption without manual reload or progress loss.
- `[AC-04]` One revision-bound, fail-closed state machine runs deterministic tests before build and emits concise structured evidence.
- `[AC-05]` The plan gives one ranked, evidence-backed recommendation without implying implementation approval.
- `[AC-06]` Supported Mermaid label segments have deterministic limits and fail pre-push before clipped release text can be accepted.

## Phase 0 — Establish the Assessment Baseline

- [x] `[AI] [AC-01]` Input: resolved `bnest-app` targets, release source, architecture, operating workflows, and project documentation. Action: record the repository's current route, process lifecycle, readiness/revision guard, rollback, and cleanup model. Outcome: a source-backed current condition with no machine-local values. Proof: [current condition](tech-docs.md#current-condition) cites the owned files and exact target set.
  - 2026-08-27 — Completed. Nx resolves `proxy:*`, `release:build`, and `deploy:*` targets; their launcher uses Caddy plus blue/green Phoenix slots. `proxy:status` could not run in this shell because `BNEST_DEPLOY_ROOT` is absent, so no runtime-health claim is recorded.
- [x] `[AI] [AC-02]` Input: the current condition and one-machine/no-downtime/resource objectives. Action: enumerate feasible operating models and their constraints without conflating recommendation with approval. Outcome: alternatives remain independently evaluable. Proof: [alternatives](tech-docs.md#possible-alternatives) compares all six models.
  - 2026-08-27 — Completed. Six options are described with continuity, steady-state resource, release-time resource, rollback, and complexity consequences.
- [x] `[AI] [AC-05]` Input: repository implementation, primary documentation, user constraints, and alternative trade-offs. Action: recommend one direction and rank fallbacks. Outcome: ephemeral blue/green Caddy plus one deterministic release transaction is recommended; direct single-slot Tailscale is conditional; warm slots, hot upgrades, and containers are not recommended. Proof: [recommendation and ranking](tech-docs.md#recommendation-and-ranking).
  - 2026-08-27 — Completed. The recommendation minimizes new machinery and steady-state Phoenix capacity while retaining the currently implemented promotion and rollback safety boundary.
- [x] `[AI] [AC-06]` Input: the clipped release-diagram screenshot, Badakmini's existing Mermaid validation boundary, and official Mermaid syntax. Action: specify renderer-free label roles, grapheme limits, segment boundaries, exclusions, structured findings, tests, and pre-push triggering. Outcome: implementation has a deterministic contract and does not need to infer readability from rendering. Proof: [Mermaid legibility prerequisite](tech-docs.md#mermaid-legibility-prerequisite).
  - 2026-08-27 — Completed as documentation only. Validator, hook, and corpus changes remain deferred to Phase 2.

### Phase 0 Checkpoint

- [x] `[AI] [AC-01–AC-02, AC-05–AC-06]` Re-read the assessment against the source, continuity rules, and [plan quality gate](../../../repo-governance/workflows/plan-quality-gate.md). Outcome: the plan distinguishes repository evidence from unobserved machine runtime, recommendation from implementation approval, and proposed Mermaid enforcement from as-built validation. Proof: this plan's current-condition language, recommendation, `Status`, and File Impact agree.
  - 2026-08-27 — Passed. `wc -l` reported 235 lines for the single `tech-docs.md`; the six-file inventory has no companion folder; uncached `badakmini-cli:test:repo` passed links, directory maps, word budgets, and 23 compatible Mermaid diagrams. No production command ran with guessed environment values and no source, hook, process, proxy, or active-service state changed.

## Phase 1 — Measure and Decide (deferred)

- [ ] `[AI] [AC-01]` Input: the existing authorized machine-local deployment environment. Action: capture a safe baseline of active slot/revision, Caddy readiness, routed HTTPS readiness, a connected synthetic LiveView/WebSocket journey, active processes, and bounded CPU/RSS/disk observations. Outcome: the actual resource and continuity baseline is known without recording hostnames, paths, credentials, session data, or user records. Proof: redacted pass/fail and aggregate measurements in `learnings.md`.
- [ ] `[AI] [AC-03]` Input: current Bnest journeys, persistence contracts, and browser/LiveView state. Action: inventory the user progress held by the server, browser, and LiveView process for each page affected by reconnect. Outcome: every acknowledged and in-progress state has an explicit preservation or reconstruction rule. Proof: a safe state matrix in technical documentation linked to affected specifications and tests.
- [ ] `[AI] [AC-03]` Input: the state inventory, the canonical browser harness, and checked-in release-recovery scenario titles. Action: replace manual-reload-only evidence with an isolated deployment-disconnect simulation at the harness's exact served origin, using distinct `test-user-<suite>-<run-id>` identities, marked runtime roots, browser contexts, and synthetic data. Outcome: affected pages reconnect without `page.reload()`, preserve route/progress/input, and avoid duplicate actions. Proof: filtered `bnest-app-e2e:test:e2e` results plus validated `finally` cleanup; cleanup failure fails the task.
- [ ] `[AI] [AC-04]` Input: resolved Nx gates and release source. Action: finalize the fixed command manifest and release state machine from [technical documentation](tech-docs.md#deterministic-release-constraints), replacing every placeholder with checked-in scenario titles and declaring inputs, exit conditions, warm/cold recovery, and cleanup. Outcome: every transition has no discretionary routine step and artifact creation is unreachable after a failed or indeterminate pre-artifact gate. Proof: reviewed command manifest and state-transition table.
- [ ] `[AI] [AC-04]` Input: the state machine. Action: define one concise result contract containing schema version, release revision, stage, outcome, evidence identifiers, durations, next valid transition, and safe error category. Keep verbose logs machine-local and excluded from AI context unless diagnosis is triggered. Outcome: routine success/failure needs one bounded result, while secrets and user data remain absent. Proof: reviewed example results for success, pre-build failure, candidate failure, rollback, and cleanup.
- [ ] `[AI] [AC-04]` Input: every proposed cacheable pre-release task. Action: prove its Nx inputs and outputs include relevant source, dependency, configuration, runtime, environment, arguments, and artifacts; leave network-, time-, random-, active-service-, and production-state checks uncached. Outcome: cache reuse cannot substitute stale release evidence. Proof: resolved target configurations and repeat-run tests for changed inputs.
- [ ] `[AI] [AC-03]` Input: the test-identity standard, deployment topology, authentication boundary, and isolated recovery scenarios. Action: define a verification surface where the same artifact and reconnect path are served from one exact origin while all accounts, sessions, records, and browser profiles remain under one marked `data/test/runs/<run-id>/` root. Keep household-origin checks read-only and anonymous; if equivalence cannot be proved without production user data, mark authenticated live proof blocked. Outcome: no test uses a real session, writes a synthetic account to production, or routes household users to test data. Proof: reviewed verification topology, isolation checks, equivalence limits, and exact cleanup transition in technical documentation.
- [ ] `[HUMAN] [AC-03]` Input: passed isolated recovery proof, approved safe verification surface, healthy read-only baseline, exact production mutation sequence, and recovery triggers. Action: explicitly authorize or reject one routed release measurement against the active household service. Outcome: live-route mutation is never inferred from assessment authority. Proof: dated user direction recorded here. Human ownership is required because this step permits production routing/process mutation.
- [ ] `[AI] [AC-03]` **Activated only by the preceding human authorization and safe-verification proof.** Input: the approved fixed revision, isolated exact-origin scenarios, read-only household-origin checks, and healthy previous slot. Action: exercise one release while isolated synthetic users hold representative pages and work in progress; keep the previous slot through routed proof and bounded drain. At the household origin, collect only anonymous HTTP/revision/WebSocket evidence that cannot create or read user records. Outcome: isolated pages recover with the same route and progress and the household route serves the intended revision without manual intervention; any unproved equivalence remains a failed criterion, not an inferred pass. Proof: routed revision/WebSocket results and safe before/after assertions containing no origin, credentials, cookies, payloads, user identifiers, or user records.
- [ ] `[AI] [AC-02, AC-05]` Input: Phase 1 baseline and the alternative comparison. Action: validate or revise the recommended ephemeral blue/green Caddy model, including removal of Caddy only if its measured cost and proven replacement justify it. Outcome: one measurement-backed direction and explicitly rejected alternatives. Proof: a dated final decision in [technical documentation](tech-docs.md).
- [ ] `[HUMAN] [AC-02]` Input: the evidence-backed candidate direction, exact Specification Changes, and exact File Impact. Action: authorize or reject implementation scope if it changes the release mechanism. Outcome: no source or production change proceeds on an inferred preference. Proof: explicit user direction recorded in this plan. Human ownership is required because it selects architecture and grants implementation authority.

### Phase 1 Checkpoint

- [ ] `[AI] [AC-01–AC-04]` Confirm the selected direction preserves routed revision proof, automatic current-page recovery without manual reload or progress loss, bounded cleanup, and an immediate recovery path. Overlapping-reader designs must retain the previous backend through proof; interruption-tolerant designs must prove bounded automatic recovery and a ready rollback artifact. When Caddy is removed, prove the replacement owns each relevant property. Proof: a passed [plan quality gate](../../../repo-governance/workflows/plan-quality-gate.md).

## Phase 2 — Implement the Deterministic Non-Production Slice (deferred)

- [ ] `[AI] [AC-06]` Input: Badakmini's existing Mermaid parser, supported diagram corpus, and the proposed label contract. Action: add failing Gherkin/binding/adapter coverage, implement renderer-free grapheme limits and structured findings, repair existing findings, and expand pre-push change detection. Outcome: over-limit visible label segments fail deterministically without a renderer, browser, network, or new package. Proof: focused Badakmini unit/integration/E2E results plus a repository-corpus run and a pre-push trigger test.
- [ ] `[AI] [AC-03]` Input: canonical chat recovery scenarios and the exact E2E origin. Action: replace the deployment reload adapter with a real offline/online LiveView reconnect and add unsent-input/current-route recovery. Outcome: the browser reconnects without `page.reload()` and retains the draft and route. Proof: filtered `bnest-app-e2e:test:e2e` results and behavior completeness.
- [ ] `[AI] [AC-04]` Input: the fixed command manifest and existing deployment primitives. Action: add a tested revision-bound release transaction that performs clean-main preflight, fixed uncached gates, immutable build manifesting, inactive-slot preparation, promotion, proof delegation, rollback, drain, retention, cleanup, and bounded JSON results. Outcome: one Nx command owns the routine state machine and refuses missing proof/configuration. Proof: release-tool unit tests plus static Nx/config review.
- [ ] `[AI] [AC-04]` Input: completed code and documentation. Action: run app quick/integration, focused release-recovery E2E, Badakmini repository validation, plan quality gate, and pre-push verification. Outcome: all non-live acceptance evidence is green; live gaps remain explicitly pending. Proof: recorded command results in the completion handoff.

### Phase 2 Checkpoint

- [ ] `[AI] [AC-03–AC-04, AC-06]` Confirm the transaction cannot build after a failed gate, cannot retire before proof, automatically rolls back failed routed proof, retains only two verified artifacts, removes only its validated worktree, and never prints environment values. Confirm the browser adapter has no reload in the deployment step and the repository rejects over-limit Mermaid labels before push. Live execution remains blocked until Phase 1's safe runtime and human routed-measurement prerequisites pass.

## Conditional Recovery

- [ ] `[AI] [AC-01]` **Trigger: any local or routed readiness, WebSocket, or release-revision check fails during later measurement.** Action: stop further release mutation; before retirement, run warm `deploy:rollback` to the still-prepared prior slot; after retirement, keep the current healthy route while the recorded prior artifact is re-prepared, revision-checked, promoted, and routed-proven. Verify the synthetic journey and do not delete unresolved state. Outcome: the last verified backend is restored without an unplanned sole-backend stop. Proof: safe timestamped recovery result in `learnings.md`.
