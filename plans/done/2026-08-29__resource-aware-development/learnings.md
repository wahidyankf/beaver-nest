# Learnings

- 2026-08-29 — Baseline: clean `main` matched `origin/main` at revision `43bf493f1f66d22f0d80e76b866a3cfcbf748a74`; routed Caddy health was ready on blue with SQLite ready. No overlapping active/backlog/idea plan was found.
- 2026-08-29 — Host evidence: exported pressure was normal, `memory_pressure -Q` reported 65%, and Node exposed 12 available execution units. `vm.compressor_pool_size` exceeded physical RAM and therefore cannot represent a usable compressor-capacity denominator.
- 2026-08-29 — Incident evidence: panic reports recorded watchdog timeout with compressed-page and segment-limit state; runtime `vm.compressor_bytes_used` is a different quantity. The prior 37.5% payload/RAM gate is conservative policy, not a like-for-like incident threshold.
- 2026-08-29 — Nx 23 includes migrations explicitly removing custom task runners since Nx 21. The implementation will reuse ordinary target entry points and one shared guard instead of depending on a removed extension seam.
- 2026-08-29 — A real guarded `node --version` smoke was capacity-deferred for the full 15-second window under the proposed 50% CPU admission even though platform pressure was normal and about 21 GiB remained available. Because CPU is not the incident's hard memory signal, development admission now preserves two interval CPU units instead of carrying forward the release-only six-unit budget; memory pressure still owns shedding.
- 2026-08-29 — Primary-source audit: XNU exports dispatch-compatible pressure flags `1/2/4`; its available non-compressed figure is an estimate rather than free RAM; Node CPU time counters support interval deltas; and XNU compressor payload accounting is not compressor capacity. The canonical rule now cites the [XNU pressure export](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/kern/kern_memorystatus_notify.c), [XNU memory-status model](https://github.com/apple-oss-distributions/xnu/blob/main/doc/vm/memorystatus_notify.md), [Node OS API](https://nodejs.org/api/os.html), and [XNU compressor implementation](https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/vm/vm_compressor.c); all numeric thresholds remain explicitly host-local empirical policy.
- 2026-08-29 — Unit fixtures originally left private test roots in `/tmp`; every temporary root now registers exact recursive cleanup with the Node test context, matching the production retention contract without broad deletion.
- 2026-08-29 — Final lifecycle review found detached children lacked the documented terminal-signal forwarding. The guard now forwards `SIGINT` and `SIGTERM` only to its child process group, removes listeners and timers on exit, and has a regression test for exit `130`, exact lock release, and listener cleanup.
- 2026-08-29 — The documented E2E example named a scenario that no longer existed. A no-tests-found run stopped without starting a server; the example now names `A visitor opens a fresh chat`, whose isolated Chromium setup and journey passed 2/2 and cleaned both marked data roots.
- 2026-08-29 — Final verification: five affected project quick gates passed sequentially and uncached in 1m42s; no guard lock, synthetic process, `/tmp` fixture, or isolated test-data child remained; routed blue health stayed ready on the original production revision.
